---
name: blog-write
description: テーマや参考URLから、固有の学びが1つ以上ある公開ブログ記事の下書き（blog/<slug>.md・draft: true）を書く。公開は /blog-publish が担う。例 /blog-write 投稿の時間帯は本当に効くのか ／ /blog-write https://example.com/article
model: inherit
---

# blog-write：テーマを掘って下書きを書く

テーマや参考URL（両方でもよい）を受け取り、**調べて → 固有の学びを1つ決めて → 書く**。成果物は `blog/<slug>.md` の下書き1本。形式の正本は `blog/README.md`（先に読む）、実例は `blog/hello-exos-ai-blog.md`。
**このスキルは公開しない。** `draft: true` のまま保存して終わる。`draft` を外してコミットするのは `/blog-publish`。

## 手順

### 0. 入力を受け取る

| 入力 | 扱い |
|---|---|
| テーマ（自由文） | 「誰が・何に困っていて・読後に何ができるか」を1行にして範囲を絞る |
| 参考URL（1つ以上） | WebFetch で読み、**事実・数字・主張だけ**を箇条書きにメモする。文章は写さず、見出し構成もなぞらない |
| 読めない・薄い | 読めない（403・JS描画・空）か主張を1つも取れないURLは引用元にしない。**確認は挟まず**報告欄に残してテーマで進める |

`grep -h "^title:" blog/*.md` で既存記事の title を一覧し、同じ主張の記事があれば書かずにそのファイルを案内して終わる。

### 1. 掘る（書く前に5つの問いへ箇条書きで答える。応答に出してよい）

| 問い | 答えるべきこと |
|---|---|
| なぜ | その定説が成り立つ**前提**は何か。前提が崩れるのはどんな場面か |
| どうやって | 手順に落とすと何ステップか。どこで詰まるか |
| 反例 | 逆の結果になった例・やめた方法。成功例だけを並べない |
| 数字 | 回数・時間・費用・文字数・反応数。**出典か実測日付き**で書き、無いものは「未計測」と書く（作らない） |
| 自分の運用 | Exos AI で実際にどうしているか。`docs/プロンプト設計書.md`（プロンプトと検証）・`docs/PRD.md`・`tasks/BACKLOG.md`（決定の経緯）・実装を読んで拾う。内部の経緯・未公開機能・費用の内訳は記事に書かない |

答えから**学び（insight）を1文**にする。形は「〜と思われがちだが、実際は〜。理由は〜」。合格条件は2つ: (1) 実測・反例・条件のどれかに根づいている。(2) 「Exos AI」「自分」を消して他社ブログに貼っても成り立つなら一般論なので不合格。2周しても出なければ、不足している実例・数字を**1回にまとめて**運営者に聞く（小出しにしない）。

### 2. 構成する

学びを読者向けの主張1文「○○を△△すると□□になる」にする。これが `description` の元で、`title`・冒頭3行も同じ主張を指す。長さは **1,500〜3,000字**。見出しは `##` を主とし `###` は1段まで。節名はテーマに合わせて変えてよいが、役割と順番は守り、主張に効かない節は削る。

- `## 結論` — 主張と理由を3行以内。最初に書く
- `## なぜそうなるのか` — 前提と仕組み（§1「なぜ」）
- `## 具体例` — 手順・プロンプト・設定値・数字（§1「どうやって」「数字」「自分の運用」）
- `## やらないこと・落とし穴` — 反例と、その条件（§1「反例」）
- `## まとめ` — 持ち帰り1つ＋読者が次にやること1つ

### 3. 書く（文体）

| する | しない |
|---|---|
| 数字・手順・プロンプト・設定値で具体化する | 「適切に」「しっかり」「うまく」「効果的に」「様々な」で済ませる |
| 断定には根拠（実測・出典・仕組み）を隣に置く | 根拠のない断定、根拠のある話のぼかし |
| 事実と意見を語尾で分ける（「〜だった」／「〜と考えている」） | 推測を事実の語尾で書く |
| 1文1内容。専門用語はその場で言い換える | 「必ず伸びる」「フォロワー○倍」「保証」「No.1」など効果の約束・実績の誇示（景品表示法。`docs/marketing/lp-design-brief.md` §5 に従う） |
| 参考URLは自分の言葉で要約。引用は1〜2文まで `> 引用文 — [出典名](URL)` | 段落単位の転載、個人が特定できる情報 |
| Exos AI へのリンクは1回まで。使っていない読者にも成り立つ内容にする | 宣伝記事 |

### 4. 保存して検証する

- slug: 主張を表す英小文字3〜5語をハイフンで（例 `posting-time-is-not-the-lever`）。小文字英数字とハイフンのみ・80字以内。日付・連番は入れない
- `blog/<slug>.md` に保存。front matter に書けるキーは `title`（80字以内）／`description`（200字以内・1〜2文）／`date`（今日 `date +%F`）／`updated`／`draft: true`／`tags`（既存記事と表記を揃える）の6つだけ（他は検証で弾かれる）
- 本文は `##` から始める（`#` は書かない）。生HTMLは書かない。画像が要るなら `public/blog-images/`（無ければ作る）に置き `/blog-images/<name>.png` で参照する
- `npm run blog:check -- <slug>.md` を実行し、`📝 <slug>.md  下書き（draft: true）` と末尾の `✅ 不備なし` を確認する。`❌` なら表示された理由を直して再実行

### 5. 自己チェックして報告する（全部 Yes になるまで直す）

- [ ] §1 の学びが本文に**1文で**入っている（読者がその1文だけ覚えて帰れる）
- [ ] 結論が冒頭にあり、見出しだけで流れが追える。`title`・`description`・冒頭3行が同じ主張を指す
- [ ] 具体例（手順・数字・プロンプト・設定値）が1つ以上ある。未計測は未計測と書いてある
- [ ] §3「しない」列の語で本文を `grep` して0件（曖昧語・効果保証・誇大表現）
- [ ] 誤っていたら読者に損をさせる断定に出典か実測が付き、事実と意見が語尾で分かれている。参考URLの文章を写していない
- [ ] `title` 80字以内・`description` 200字以内・本文 1,500〜3,000字

報告: 作ったファイル／記事の主張1行／確認してほしい点（未計測の箇所・運営者しか知らない実例・読めなかったURL）／次に実行するコマンド `/blog-publish <slug>.md`。

## ルール

- **公開しない。** `draft` を外さない。コミットも `git add` もしない。
- 1回で書くのは1記事。テーマが2つあれば分けて、どちらを先にするか聞く。
- 記事・報告は日本語。コードブロック内のプロンプトや設定値は実物のまま載せる。
