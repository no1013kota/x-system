# 監査記録 2026-08-18（最終監査 2026-08-13 以降の287コミット）

| 項目 | 内容 |
|---|---|
| 更新日 | 2026-08-18 |
| 生成元 | 6領域 × 3観点の並列監査（92エージェント・エラー0） |

`/refactor` 監査（R1〜R38）は 2026-08-13 に打ち切られ、その後 **287コミット・609ファイル**が
未監査で積み上がっていた（T-M8-84〜142）。ドキュメント乖離・リファクタ・空振り検査の3観点を
並列で監査し、各指摘を独立に**反証しようと試みて**残ったものだけを記録する。

- 指摘 86件 → **確定 76件 / 反証 10件**
- うち **7件は着手済み**。残り **69件**が未着手。

**この記録の使い方**: 1件ずつ拾うのではなく、BACKLOG / REFACTOR_PLAN のまとめタスクから
この番号を参照する。振る舞いを変えるものは BACKLOG、内部改善は REFACTOR_PLAN が入口。

## 着手済み（このパスで直した）

- cronの401検査が手書き列挙で canary/doctor をすり抜ける → `T-M8-137`
- docsの更新日の置き去り9件 ／ 存在しないファイル参照 → `T-M8-137`
- 使われなくなった共通部品を検査が読んでいた（campaign-price） → `T-M8-137`
- 既定パターンを保存すると max_posts が8へ跳ね上がる → `T-M8-139`
- 画像プロンプト画面の再読み込みで投稿パターンが壊れる（型プロンプトの保存経路も1本閉じた） → `T-M8-140`
- 請求額と表示額のズレを誰も検出していない（Stripe突き合わせを追加） → `T-M8-141`
- ナビラベルと画面h1の一致検査がh1を読んでいない → `T-M8-142`
- auto modeの post_publish 連鎖が未実装（docsに未実装注記を入れた） → `T-M8-143`

## 未着手 69件（一覧）

| # | 深刻度 | 種別 | 領域 | 指摘 |
|---:|---|---|---|---|
| 1 | high | ドキュメント乖離 | 認証・課金・利用量 | 要件03 §8 の残量JSONが回数制のまま（generations/images・上限100/20）。同じ文書の §7.1 と実装に矛盾 |
| 2 | high | ドキュメント乖離 | 認証・課金・利用量 | 要件03 §1 の法務同意version `2026-07-22-draft` が実装（2026-08-08・draft接尾辞は廃止）と食い違う |
| 3 | high | ドキュメント乖離 | 運営コマンド・診断・メール | deployment.md の認証メール手動手順が「リンク＋token_hash」前提のまま（実装は6桁コード） |
| 4 | high | リファクタ | 画面・UI共通部品 | 画像プロンプト編集画面の「再読み込み」で編集対象が投稿パターン(p1)へすり替わり、保存すると post_patterns.prompt を上書きする |
| 5 | medium | 空振り検査 | 認証・課金・利用量 | リモートSupabaseへ送る `mailer_otp_length` / `mailer_otp_exp` が「必ず一致させる」と書かれた相手（EMAIL_CODE_LENGTH・失敗数えの窓）と機械的に照合されていない |
| 6 | medium | 空振り検査 | 空振り検査 | 法務同意の「3経路が同じ正本を読んでいる」検査に再同意バナーの経路（app/layout.tsx）が入っておらず、写経を復活させても落ちない |
| 7 | medium | 空振り検査 | 空振り検査 | 運営者へ示すコマンドの実在検査が src/lib/ops と scripts のディレクトリ手書き列挙で、新設の portal-features.ts（同じく nextAction を出す）を見ていない |
| 8 | medium | 空振り検査 | jobs・cron・スケジュール | doctorの「定時実行」検査が scheduler_tick だけで、metrics_collector と follower_snapshot は止まっても緑 |
| 9 | medium | 空振り検査 | jobs・cron・スケジュール | scheduler_tick の回収ステップ(recoverSchedule)のSQLがどのテストでも実行されない（正規表現スタブのみ） |
| 10 | medium | 空振り検査 | 運営コマンド・診断・メール | 「運営者へ示すコマンドはそのまま動く」検査の探索先が手書き列挙で、最新のdoctor案内が検査外 |
| 11 | medium | 空振り検査 | 投稿パターン | pattern-spec.test.ts の SEEDED 表は「seed が入れる既定値」と称しながら DB と突き合わせていない（すでに p2 が食い違っている） |
| 12 | medium | 空振り検査 | 投稿パターン | thread-count.test.ts が本番から呼ばれない変換関数を検証している（壊しても本番は壊れない） |
| 13 | medium | 空振り検査 | 投稿パターン | 「設計書§6の全文と一致させ、スナップショットテストで乖離を検出する」がドキュメントを読んでいない |
| 14 | medium | 空振り検査 | 画面・UI共通部品 | インラインバナー検査の検出条件が1つの完全一致リテラルだけで、枠線色・paddingを変えた手書きバナー5箇所を素通しする |
| 15 | medium | ドキュメント乖離 | 認証・課金・利用量 | 要件03 §2.2 と §6 が Portal の「契約が見つからないとき」で矛盾し、§2.2 が実装と逆 |
| 16 | medium | ドキュメント乖離 | 認証・課金・利用量 | 要件03 §1/§5 の「契約状態を先に判定」「details に現在statusと /plans を含める」が、実際に配線されている経路と違う |
| 17 | medium | ドキュメント乖離 | 認証・課金・利用量 | deployment.md の手作業手順が signup をリンク方式（token_hash&type=signup）で説明しており、6桁コード方式と食い違う |
| 18 | medium | ドキュメント乖離 | 認証・課金・利用量 | PRD の原価試算とリスク表に回数制（文章100回・画像20枚・「4枠」）が残存 |
| 19 | medium | ドキュメント乖離 | jobs・cron・スケジュール | 要件04の利用枠記述がAIクレジット制（T-M8-108/109）に追随していない |
| 20 | medium | ドキュメント乖離 | jobs・cron・スケジュール | news_fetch の {{hours}} マッピングが旧・毎時スケジュールのまま（正本内でも矛盾） |
| 21 | medium | ドキュメント乖離 | jobs・cron・スケジュール | news取得結果の分類が news-outcome.ts の1箇所に集約されておらず、cron応答だけ判定が違う |
| 22 | medium | ドキュメント乖離 | jobs・cron・スケジュール | worker が lease 時に canceled にした期限切れ予約は schedule_missed 通知が作られない |
| 23 | medium | ドキュメント乖離 | jobs・cron・スケジュール | generation_jobs.input の正本例が旧pattern列のままで、実際に書くキーと一致しない |
| 24 | medium | ドキュメント乖離 | 運営コマンド・診断・メール | release:check の構成説明が3箇所で古い（check:doc-dates / check:doc-refs が抜けている） |
| 25 | medium | ドキュメント乖離 | 運営コマンド・診断・メール | deployment.md §0.0 のリリースゲート一覧が実装より1段少なく、デプロイ後検証も1本しか書いていない |
| 26 | medium | ドキュメント乖離 | 運営コマンド・診断・メール | 通知メールの正本（要件01 §3.6・要件04 §14）に T-M8-136 の差出人名とメールヘッダが書かれていない |
| 27 | medium | ドキュメント乖離 | 運営コマンド・診断・メール | 要件01 §4 が signup 確認メールをリンク方式（RedirectTo/TokenHash）と説明したまま |
| 28 | medium | ドキュメント乖離 | 運営コマンド・診断・メール | local-development.md のローカル登録手順が「確認メールのリンクを踏む」のまま |
| 29 | medium | ドキュメント乖離 | 運営コマンド・診断・メール | 手元用の値の案内が auth:templates に追いついていない（SMTP_* が表に無く、SUPABASE_ACCESS_TOKEN は .env.example に無い） |
| 30 | medium | ドキュメント乖離 | 運営コマンド・診断・メール | push-auth-templates の rate_limit_anonymous_users のコメントが別設定の説明になっている（総当たり対策として✅を出す） |
| 31 | medium | ドキュメント乖離 | 投稿パターン | 要件06 §4.2 が「p2のみ 自分の考え」「asks_user_opinion で出し分ける」と書いているが、実装はプレースホルダーへ一般化済み |
| 32 | medium | ドキュメント乖離 | 投稿パターン | 要件06 §4.3 が撤去済みの enum キー定数（P-1=6 …）でポスト数上限を規定している |
| 33 | medium | ドキュメント乖離 | 画面・UI共通部品 | 要件06 §1.1 が /plans をまだ「3枚のカード横並び＋ご登録の流れ4ステップ＋初期設定手順」と書いており、同じ節の後段（比較表）と矛盾している |
| 34 | medium | ドキュメント乖離 | 画面・UI共通部品 | 要件06 が廃止済みの support / api-keys / x-accounts / notifications タブを現行タブとして記述し続けている |
| 35 | medium | ドキュメント乖離 | 画面・UI共通部品 | 要件06 の変更履歴が表ヘッダを失った孤立1行になっており、冒頭ヘッダ v1.92 に対し履歴は v1.84 で8版分が欠けている |
| 36 | medium | リファクタ | 認証・課金・利用量 | 本番から呼ばれていない `requireExecutionAccess` が残り、3つのコメントが「再同意の裏付け」としてそれを指している |
| 37 | medium | リファクタ | 認証・課金・利用量 | 「md/premium だけ編集できる」の判定が5箇所に分散し、表示用（PLANS）と拒否用（promptEditablePlan）が別系統 |
| 38 | medium | リファクタ | 認証・課金・利用量 | AIクレジットの列指定が reserve は対応表経由・settle/refund は `ai_credits_used` 直書きで不一致（枠を1本化した残骸） |
| 39 | medium | リファクタ | 認証・課金・利用量 | 確認メールの行き先URLの組み立てが Server Action と doctor で二重定義（コメント自身が「同じ組み立てにすること」と人の記憶に頼っている） |
| 40 | medium | リファクタ | jobs・cron・スケジュール | suggestion の枠返還が到達しない死んだ分岐（正本は「消費しない」） |
| 41 | medium | リファクタ | jobs・cron・スケジュール | 子jobの決定的keyが3箇所・2形式に分かれ、keys.ts の childJobKey は本番から使われていない |
| 42 | medium | リファクタ | 運営コマンド・診断・メール | expectedBranchFor に本番の呼び出し元が無く、release.mjs が同じ判断を再実装している |
| 43 | medium | リファクタ | 運営コマンド・診断・メール | supabase/config.toml と push-auth-templates.mjs の二重持ち（件名・コード桁数・rate limit）に機械検査が無い |
| 44 | medium | リファクタ | 投稿パターン | asks_user_opinion / asksUserOpinion が読まれない死んだ属性として全レイヤに残っている |
| 45 | medium | リファクタ | 投稿パターン | GENERATION_MAX_POSTS / maxPostsFor / PATTERN_MAX_POSTS が本番コードから参照されていない |
| 46 | medium | リファクタ | 画面・UI共通部品 | アカウント.mdエディタとプロンプトエディタで Note 型・ActionError 型・警告バナーのマークアップが丸ごと重複している |
| 47 | low | ドキュメント乖離 | 認証・課金・利用量 | 要件05 §2.2 のエラー応答例の message が実装と違い、廃止語「生成枠」を使っている |
| 48 | low | ドキュメント乖離 | 空振り検査 | navigation-items.test.ts のテスト名が「7項目」だが実際の定義と期待値は6項目 |
| 49 | low | ドキュメント乖離 | 空振り検査 | landing-page.test.ts のテスト名が「規定の5箇所だけ」だが実際の期待値は4（本文コメントは4と書いている） |
| 50 | low | ドキュメント乖離 | jobs・cron・スケジュール | launchd運用メモの news_fetch スケジュールが縮小前（毎時）のまま |
| 51 | low | ドキュメント乖離 | 運営コマンド・診断・メール | doctor の検査項目一覧と層8の説明が最新の実装に追いついていない |
| 52 | low | ドキュメント乖離 | 運営コマンド・診断・メール | テスト本数の記述が3つの文書でばらばらで、いずれも実測とずれている |
| 53 | low | ドキュメント乖離 | 投稿パターン | 要件05 §8 の validation_error 理由一覧が実装と食い違い、画面側にも死んだ分岐が残っている |
| 54 | low | ドキュメント乖離 | 投稿パターン | 要件02 §3.21 の「自作パターンの既定は min(7, max_posts + 2)」が T-M8-130 の上限引き上げ後も直っていない |
| 55 | low | ドキュメント乖離 | 投稿パターン | 要件02 §3.10/§3.9 が撤去済みの旧 `pattern` 列と CHECK を今も制約として書いている |
| 56 | low | ドキュメント乖離 | 画面・UI共通部品 | 要件06 §2 の中で「ナビ7項目」と「ナビ項目は6つ」が併存し、コードのコメントとテスト名も7項目のまま |
| 57 | low | ドキュメント乖離 | 画面・UI共通部品 | 6桁コード方式へ変えたのに、会員登録画面の導入文が「確認メールからメールアドレスを認証してください」とリンク追跡を促している |
| 58 | low | リファクタ | 認証・課金・利用量 | `reserveIfPremium` 用のJSDocが `settleIfPremium` の直前に取り残され、reserve側は無説明になっている |
| 59 | low | リファクタ | 空振り検査 | schema-doc-sync.db.test.ts が docs を cwd 相対で読み、§11 の「走査の基点は import.meta.url」に反する（サブディレクトリ実行で必ず落ちる） |
| 60 | low | リファクタ | jobs・cron・スケジュール | news_fetch 周辺のコメントが「6分野・毎時起動」のまま（実装は3分野・2時間おき） |
| 61 | low | リファクタ | 運営コマンド・診断・メール | judgePortal の nextAction 合成で、error のとき判定側の nextAction が必ず捨てられる |
| 62 | low | リファクタ | 運営コマンド・診断・メール | 存在しない環境変数を指すコメントが呼び出し側に残っている（R30の取りこぼし） |
| 63 | low | リファクタ | 運営コマンド・診断・メール | check.ts を正本にしたのに diagnostics.ts 経由の再exportが残り、import 経路が2つある |
| 64 | low | リファクタ | 投稿パターン | 説明の120字上限はDBのCHECKに存在するのに、コードのコメントは「DBに CHECK は無い」と書き、docs の Constraints にも載っていない |
| 65 | low | リファクタ | 投稿パターン | 移行完了後に意味が反転した／古くなったコメントが投稿パターン周辺に複数残っている |
| 66 | low | リファクタ | 投稿パターン | smoke:live のシナリオが利用者が変更できる表示名でパターンを引いている |
| 67 | low | リファクタ | 画面・UI共通部品 | 認証3画面の外枠（ページ背景・カード・ロゴ＋アプリ名ヘッダ・h1・LegalFooter）が3ファイルへ逐語重複している |
| 68 | low | リファクタ | 画面・UI共通部品 | 画面h1のclass文字列が6ファイル7箇所へ直書きされ、cardTitleClassName と同型の集約が h1 だけ抜けている |
| 69 | low | リファクタ | 画面・UI共通部品 | 参照0件の共通部品と、削除済み画面（ai-settings のタブナビ）向けに残された TabNav の第3引数・テストケース |

## 各指摘の詳細

### 1. [high] 要件03 §8 の残量JSONが回数制のまま（generations/images・上限100/20）。同じ文書の §7.1 と実装に矛盾

- 種別: ドキュメント乖離 ／ 領域: 認証・課金・利用量
- 場所: `docs/requirements/03_auth_billing_usage.md:269`
- 根拠: 03 §8（261-271行）の例が `"generations": { "used": 22, "limit": 100 ... }`, `"images": { ... "limit": 20 ... }` の4枠。一方 同文書 §7.1 は「AIクレジット 1000（1クレジット=1円相当・T-M8-109）」の3枠で、実装 src/lib/usage/usage-summary.ts:20-23 の `UsageSummary` は `ai_credits` / `normal_posts` / `url_posts` の3キー（コメントに「要件03 §8 のJSON形状（3枠・T-M8-109）」と書いてあるが、参照先が旧形状のまま）。usage-summary-server.ts:22-24 が読む列も `ai_credits_used` のみで `generations`/`images` に相当する列は存在しない。v1.27 の変更履歴は「プラン表の月間枠を金額制のAIクレジットへ揃えた」としているが §8 の例が取り残されている。
- 直し方: §8 の JSON 例を実装の3枠（`ai_credits` 上限1000 / `normal_posts` 200 / `url_posts` 20）へ差し替える。表示順は実装コメントと PRD v1.18 に合わせ AIクレジット→通常投稿→URL付き投稿。数値は `PLANS.premium.usageLimits` を出典として明記する。docsのみの変更で振る舞いは変わらない。
- 反証を試みた結果: 反証できず、指摘は実在する。

確認した実ファイル:
- docs/requirements/03_auth_billing_usage.md:263-271（§8 残量表示）の JSON 例は今も4枠で、`"generations": { "used": 22, "limit": 100, ... }` と `"images": { "used": 4, "limit": 20, ... }` を含む。
- 同文書 §7.1（182-188行）は3枠表（通常投稿200 / URL付き投稿20 / AIクレジット1000、「生成・画像の2枠を金額制の1本へ統合した」T-M8-109）。同一文書内で §7.1 と §8 が矛盾している。
- 実装 src/lib/usage/usage-summary.ts:16-20 の `UsageSummary` は `ai_credits` / `normal_posts` / `url_posts` の3キーのみ。`computeUsageSummary`（38-42行）も3枠しか返さず `generations`/`images` は存在しない。15行のコメントが「要件03 §8 のJSON形状（3枠・T-M8-109）」と旧形状の節を参照している点も指摘どおり。
- src/lib/usage/usage-summary-server.ts:19-21 が select するのは `normal_posts_count` / `url_posts_count` / `ai_credits_used` の3列のみ。
- DBレベルでも列自体が消えている: supabase/migrations/20260816000001_ai_credits.sql:10-12 が `drop column generations_count` / `drop column images_count` / `add column ai_credits_used`。よって §8 の `generations`/`images` は「仕様レベル

### 2. [high] 要件03 §1 の法務同意version `2026-07-22-draft` が実装（2026-08-08・draft接尾辞は廃止）と食い違う

- 種別: ドキュメント乖離 ／ 領域: 認証・課金・利用量
- 場所: `docs/requirements/03_auth_billing_usage.md:33`
- 根拠: 03 §1 の33行は「現行versionはコード定数を正とし、法務確認前の開発版は利用規約・プライバシーポリシーとも`2026-07-22-draft`とする」。実装 src/lib/legal.ts:9-10 は `CURRENT_TERMS_VERSION = "2026-08-08"` / `CURRENT_PRIVACY_VERSION = "2026-08-08"` で、同ファイルのコメントは「`-draft` のような内部向け接尾辞は付けない（利用者に露出する・T-M8-72）」と明示している。`consentVersionLabel`（legal.ts:26-30）も `YYYY-MM-DD` 前提。docsの記述通りに戻すと `requiredLegalConsents`（legal-consent.ts:52-60）が全ユーザーを不一致と判定し、生成・投稿が `legal_consent_required` で止まる。docs/marketing/lp-design-brief.md:12 にも同じ旧値が残っている。
- 直し方: 33行を「現行versionは `src/lib/legal.ts` の定数（現在 `2026-08-08`）を正とし、利用者に露出するため `-draft` 等の内部接尾辞は付けない（T-M8-72）」へ改める。具体値の重複を避けたいならファイル参照だけにする。lp-design-brief.md:12 の `2026-07-22-draft` も同時に直す。docsのみ。
- 反証を試みた結果: 反証できなかった。指摘は実在する。

確認した事実:
1. `/Users/kotamatsumoto/private-workspace/x-system/docs/requirements/03_auth_billing_usage.md:33`（awk で行番号一致を確認）は「現行versionはコード定数を正とし、法務確認前の開発版は利用規約・プライバシーポリシーとも`2026-07-22-draft`とする」。`git log -L 33,33` の結果、この行は 2026-07-22 の `9f2abe6 feat(T-M1-03)` 以降**一度も更新されていない**。文書ヘッダは v1.27 / 更新日 2026-08-18 なので、他を更新しながらこの行だけ取り残されている。
2. 実装 `/Users/kotamatsumoto/private-workspace/x-system/src/lib/legal.ts:9-10` は `CURRENT_TERMS_VERSION = "2026-08-08"` / `CURRENT_PRIVACY_VERSION = "2026-08-08"`。同ファイル冒頭コメントに「形式は `YYYY-MM-DD`」「`-draft` のような内部向け接尾辞は付けない（利用者に露出する・T-M8-72）」と明記。
3. `tasks/BACKLOG.md:1084` の T-M8-72 実装メモが「版を `2026-07-22-draft` → `2026-08-08` へ。**暫定版バナー3箇所を撤去**し、内部値の `-draft` が利用者に露出していた問題も解消」と記録。つまり docs の記述は**廃止された仕様**。
4. 「別の節に正しい記述があるか」→ `docs/requirements/06_screens_onboarding_posting.md:445` は「同意versionは `YYYY-MM-DD` 形式とし、`-draft` 等の内部向け接尾辞を付けない」と正しく書いている。ただし

### 3. [high] deployment.md の認証メール手動手順が「リンク＋token_hash」前提のまま（実装は6桁コード）

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `docs/operations/deployment.md:190`
- 根拠: deployment.md:190 は「Confirm signup と Reset password を `supabase/templates/confirmation.html` / `recovery.html` と同じ内容へ貼り替える（`{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=signup` / `&type=recovery`）」と案内し、§2.9 の表 208 行も「メールテンプレート …『{{ .RedirectTo }}』を使うためドメインに依存しない」と書く。しかし実物の supabase/templates/confirmation.html は `{{ .Token }}`（6桁コード）だけで RedirectTo/TokenHash を一切持たない。scripts/push-auth-templates.mjs:52 は confirmation の必須目印を `requires: "Token"` とし、src/lib/ops/auth-url-status.ts:145 は本文に `{{ .Token }}` が無ければ error（「このままでは新規登録を完了できません」）を返す。要件03 §（25行目）も「確認メールは6桁コード方式（T-M8-121）」が正本。つまりdocsの手順どおりに貼り替えると、doctor が error を出す＝登録できない状態になる。
- 直し方: 189〜191 の下位2行と §2.9 の「メールテンプレート」行を6桁コード方式へ書き換える（confirmation は `{{ .Token }}`、recovery のみ `{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=recovery`）。合わせて「手作業の場合」の説明は `supabase/templates/*.html` の内容を貼る、と実ファイル参照に寄せる。docsのみの修正で実装・コマンドの挙動は変えない。
- 反証を試みた結果: 反証できなかった（指摘は実在する）。実ファイルで確認した内容:

1. /Users/kotamatsumoto/private-workspace/x-system/supabase/templates/confirmation.html は全9行で `{{ .Token }}`（6桁コード）のみ。`RedirectTo`・`TokenHash`・`token_hash` はどこにも無い。recovery.html だけが `{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=recovery` を持つ。

2. deployment.md:190 は「**Confirm signup** と **Reset password** を confirmation.html / recovery.html と同じ内容へ貼り替える（`{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=signup` / `&type=recovery`）」。前半（実ファイルを貼る）は正しいが、括弧内の具体スニペットは signup について実物と食い違う。type=signup のリンク方式は T-M8-121 で廃止済み。

3. 同じ節の直前 189 行は既に正しく「確認メールに入力すべき6桁コード（`{{ .Token }}`）が載らず、登録を完了できない」「パスワード再設定は引き続きリンク方式（`{{ .TokenHash }}`）」と書いている。つまり同一節の中で 189 と 190 が矛盾しており、「別の節に正しい記述がある」ではなく「隣の行と食い違っている」型（=説明できない差）。抽象化ではなく `&type=signup` という具体値なので、意図的な仕様レベル記述でもない。

4. 実装側の正本と検査も6桁コード前提: scripts/push-auth-templates.mjs:52 は confirmation の `requires: "Token"`（コメントに「

### 4. [high] 画像プロンプト編集画面の「再読み込み」で編集対象が投稿パターン(p1)へすり替わり、保存すると post_patterns.prompt を上書きする

- 種別: リファクタ ／ 領域: 画面・UI共通部品
- 場所: `src/app/app/settings/prompt-templates-editor.tsx:82`
- 根拠: この画面は T-M8-129 U4b 以降「画像プロンプト1件だけ」を扱う前提で、src/app/app/settings/page.tsx:247 が `res.templates.filter((tpl) => tpl.kind === "image")` に絞って initialTemplates を渡している。ところが編集対象は state から導出しており、prompt-templates-editor.tsx:82 `const selectedKind = visible[0]?.kind ?? "image"`、visible は同ファイル 77 付近の `templates.filter((t) => t.kind !== "p5" || quotePostEnabled)`。そして reload()（同ファイル 114-124）が `setTemplates(res.templates)` で listPromptTemplatesAction() の戻り＝全kindを入れ直す。src/lib/prompts/prompt-templates.ts:173 の listPromptTemplates は PROMPT_TEMPLATE_KINDS（src/lib/prompts/gen-prompts.ts:269 で p1,p2,p3,p4,p5,p6,image の順）を flatMap して全件返すため、再読み込み後の visible[0] は p1 になる。結果、見出し（kindLabel→legacyPatternLabel("p1")）が型名へ変わり、textarea には画像プロンプトの本文が残ったまま `current` は p1 を指す。この状態で「保存」を押すと updatePromptTemplateAction({kind:"p1", ...}) が呼ばれ、src/lib/prompts/prompt-templates.ts:252 の isPatternKind 分岐で `update post_patterns set prompt = ...` が走る（kindSchema は src/app/actions/prompt-templates.ts:23 で PROMPT_TEMPLATE_KINDS 全体を許容しており p1 を弾かない）。要件06 §3.8（06_screens_onboarding_posting.md:261）の「画像プロンプトの区分にはプルダウンを置かない（対象が1件だけなので…）」という前提が、状態の型で担保されていない。
- 直し方: この部品の境界を型で閉じる: props を `initialTemplate: PromptTemplateView`（単数・kind は "image" リテラル）にし、selectedKind を state 由来の導出ではなく定数 "image" にする。reload() は取得結果から image だけを取り出して差し替える。あわせて不要になる `visible` memo・`quotePostEnabled` prop・kindLabel の legacyPatternLabel 分岐・legacyPatternLabel の import を削除する（呼び出し側 settings/page.tsx は既に image へ絞っているので、正常系の外部振る舞いは変わらない）。
- 反証を試みた結果: 反証できなかった（指摘は実在する）。連鎖をすべて実ファイルで確認した。

1. 呼び出し側は image に絞っている: src/app/app/settings/page.tsx:247 `promptTemplates = res.templates.filter((tpl) => tpl.kind === "image")`、描画は同476-481（`promptSection === "image-prompt"` のときだけ PromptTemplatesEditor に initialTemplates／quotePostEnabled を渡す）。
2. しかし編集対象は state 由来: src/app/app/settings/prompt-templates-editor.tsx:76-83 で `visible = templates.filter((t) => t.kind !== "p5" || quotePostEnabled)`、`const selectedKind = visible[0]?.kind ?? "image"`、`current = templates.find((t) => t.kind === selectedKind)`。
3. reload()（同116-126）は `setTemplates(res.templates)` で全kindを入れ直す。サーバ側に絞り込みは無い: src/lib/prompts/prompt-templates-server.ts:36-41 の listPromptTemplatesForUser は listPromptTemplates をそのまま返し、src/lib/prompts/prompt-templates.ts:173 は PROMPT_TEMPLATE_KINDS を flatMap して全件返す。順序は src/lib/prompts/gen-prompts.ts:269-277 で p1,p2,p3,p4,p5,p6,image。よって再読み込み後の visib

### 5. [medium] リモートSupabaseへ送る `mailer_otp_length` / `mailer_otp_exp` が「必ず一致させる」と書かれた相手（EMAIL_CODE_LENGTH・失敗数えの窓）と機械的に照合されていない

- 種別: 空振り検査 ／ 領域: 認証・課金・利用量
- 場所: `scripts/push-auth-templates.mjs:120`
- 根拠: push-auth-templates.mjs:118-124 の `AUTH_SETTINGS` は `mailer_otp_length: 6`（コメント「`EMAIL_CODE_LENGTH`（画面側）と必ず一致させる」）と `mailer_otp_exp: 3600`（「画面の案内文（1時間）と合わせる」）を持つ。相手側の値は src/lib/auth/email-code.ts:18 `EMAIL_CODE_LENGTH = 6`、src/lib/auth/code-attempts.ts:38 `WINDOW_MS = 60 * 60 * 1000`（コメント「コードの有効期間（1時間）に合わせる」）、src/app/actions/auth.ts:44-45 の利用者向け文言「発行から1時間で期限切れ」、supabase/config.toml:233,235 の `otp_length = 6` / `otp_expiry = 3600` に散っている。`grep -rln "push-auth-templates|AUTH_SETTINGS" src scripts e2e` はテストを1件も返さない（同型の `scripts/setup-stripe-portal.mjs` は portal-configuration.test.ts が PRODUCT_NAMES/PRODUCT_DESCRIPTIONS を `PLANS` と突き合わせている）。T-M8-123 の本番障害はまさに `mailer_otp_length` が8で画面が6桁前提だったことなので、ここを1つ変えても落ちる検査が無いのは同じ穴が残っている。
- 直し方: portal-configuration.test.ts と同じ作法で `scripts/push-auth-templates.mjs` から `AUTH_SETTINGS` を import するテストを追加し、`AUTH_SETTINGS.mailer_otp_length === EMAIL_CODE_LENGTH`、`AUTH_SETTINGS.mailer_otp_exp * 1000 === CODE_ATTEMPT_WINDOW_MS`（code-attempts.ts の `WINDOW_MS` を export する）、および `supabase/config.toml` の `otp_length`/`otp_expiry` との一致を固定する。値を1つずらして落ちることを確認する。検査追加のみで振る舞いは変わらない。
- 反証を試みた結果: 反証できなかった。指摘は実在する。

**確認した事実（すべて実ファイル）**
- `/Users/kotamatsumoto/private-workspace/x-system/scripts/push-auth-templates.mjs:118-136` に `AUTH_SETTINGS` があり、`mailer_otp_length: 6`（コメント「`EMAIL_CODE_LENGTH`（画面側）と必ず一致させる」）、`mailer_otp_exp: 3600`（「画面の案内文（1時間）と合わせる」）。この値はリモートSupabaseへPATCHで送られる（283行目）。
- 相手側の値は別々に散っている: `/Users/kotamatsumoto/private-workspace/x-system/src/lib/auth/email-code.ts:17` `EMAIL_CODE_LENGTH = 6`、`/Users/kotamatsumoto/private-workspace/x-system/src/lib/auth/code-attempts.ts:29` `const WINDOW_MS = 60 * 60 * 1000`（**非export**）、`/Users/kotamatsumoto/private-workspace/x-system/src/app/actions/auth.ts:44-45`「発行から1時間で期限切れ」、`/Users/kotamatsumoto/private-workspace/x-system/supabase/config.toml:233,235` `otp_length = 6` / `otp_expiry = 3600`。
- `grep -rln AUTH_SETTINGS`（node_modules/.git除外）は `scripts/push-auth-templates.mjs` の**1件のみ**。テストも `doctor.mjs` も `audit-check.mjs` も参照してい

### 6. [medium] 法務同意の「3経路が同じ正本を読んでいる」検査に再同意バナーの経路（app/layout.tsx）が入っておらず、写経を復活させても落ちない

- 種別: 空振り検査 ／ 領域: 空振り検査
- 場所: `src/lib/auth/legal-consent.test.ts:145`
- 根拠: `legal-consent.test.ts:145-160` は `app/app/consent/page.tsx` / `app/actions/legal-consent.ts` / `lib/auth/legal-consent-server.ts` の3本を手書きで読み、`LEGAL_CONSENT_SELECT` を使っていることと列リストの直書きが無いことを見る。しかし `grep -rn "LEGAL_CONSENT_SELECT\|requiredLegalConsents" src` の結果、**4本目**として `src/app/app/layout.tsx:89`（`plan, subscription_status, trial_ends_at, stripe_customer_id, ${LEGAL_CONSENT_SELECT}` で読み、109行目で `legalConsentBanner(requiredLegalConsents(...))` を組む＝再同意バナーそのものの経路）が存在し、検査対象に入っていない。**実際に壊して確認した**: layout.tsx の `${LEGAL_CONSENT_SELECT}` を `terms_version, terms_accepted_at, privacy_version, privacy_acknowledged_at` の直書きへ置き換えて `npx vitest run` → `252 passed / 2185 passed`（baselineと一致）。CLAUDE.md が挙げる「バナーは出ないのに投稿は弾かれる」型の食い違いが、まさにバナー側で無検査になっている。検証後に復元済み。
- 直し方: 手書き3本を捨て、`src/app` と `src/lib` を走査して「`@/lib/auth/legal-consent` から `LEGAL_CONSENT_SELECT` / `LEGAL_CONSENT_SELECT_POOLED` / `requiredLegalConsents` を import しているファイル」を対象にし、そのすべてで列リストの直書きが無いことを見る形にする。あわせて「対象が4件以上見つかる」ガードを置く（0件・件数減少で気付ける）。テスト側だけの変更で振る舞いは変わらない。テスト名の「3経路」も実数に合わせる。
- 反証を試みた結果: 反証できなかった。指摘は実在する。

1. src/lib/auth/legal-consent.test.ts:145-160 のテスト「3経路が同じ正本を読んでいる（写経が復活したら落ちる）」は、`../../app/app/consent/page.tsx` / `../../app/actions/legal-consent.ts` / `./legal-consent-server.ts` の3本だけを手書きで列挙している。
2. しかし src/app/app/layout.tsx が4本目の本番利用箇所として存在する（17-18行で `LEGAL_CONSENT_SELECT` と `requiredLegalConsents` を import、89行で `plan, subscription_status, trial_ends_at, stripe_customer_id, ${LEGAL_CONSENT_SELECT}` を select、109行で `legalConsentBanner(requiredLegalConsents(result.data))` を組む）。これは再同意バナーそのものの経路（T-M8-134）で、検査対象に入っていない。
3. 他に代替の検査も無い。scripts/ 配下（audit-check.mjs・check-doc-refs.mjs 等）に法務同意を見るものは無く、eslint.config.mjs にも該当のカスタムルールは無く、app/layout を参照するテスト／E2Eも存在しない（grep で0件）。
4. 実際に壊して確認した。layout.tsx:89 の `${LEGAL_CONSENT_SELECT}` を4列の直書きへ置換し、さらに未使用になる import 自体も削除して（=ファイル内に `LEGAL_CONSENT_SELECT` が1回も現れない完全な写経復活の状態にして）検査を回した結果:
   - `npx vitest run` → 252 files passed / 218

### 7. [medium] 運営者へ示すコマンドの実在検査が src/lib/ops と scripts のディレクトリ手書き列挙で、新設の portal-features.ts（同じく nextAction を出す）を見ていない

- 種別: 空振り検査 ／ 領域: 空振り検査
- 場所: `src/lib/ops/next-action-commands.test.ts:37`
- 根拠: `next-action-commands.test.ts:37` の `SEARCH_DIRS` は `src/lib/ops` と `scripts` の2つだけ。この検査は「T-M8-35 で `stripe:portal:setup` に `--target` を必須化したとき `portal-status.ts` の案内文が旧形式のまま残り、運営者が言われた通り打つとエラーで止まった」ことを受けて作られたもの。ところが 2026-08-13 以降に増えた `src/lib/stripe/portal-features.ts` が同じ役目（`nextAction` の文面生成）を持ち、86行目で `` `npm run dev` を再起動 `` と運営者へコマンドを案内しているが、`src/lib/stripe/` は走査対象外なので検査が当たらない。`grep -rn "npm run " src --include=*.ts --include=*.tsx` で `src/lib/ops` 以外に11箇所ヒットし、すべて無検査。現状はコマンド名が実在するため実害は出ていないが、「案内したコマンドがそのまま動く」という規則は列挙されたディレクトリの外では成立していない。
- 直し方: `SEARCH_DIRS` を捨てて `src` 全体と `scripts` を走査する（`collectFiles` はすでに再帰なので対象ルートを差し替えるだけ）。コメント中の言及も対象に入るが、コメントで存在しないスクリプトを案内すること自体が誤りなので都合がよい。あわせて `found.length > 3` の下限を実測値に合わせて上げ、走査範囲が縮んだら落ちるようにする。テスト側だけの変更。
- 反証を試みた結果: 反証できず、指摘は実在する。実測で確認した内容:

1) next-action-commands.test.ts:37 の SEARCH_DIRS は `src/lib/ops` と `scripts` の2つだけ（collectFiles の拡張子フィルタも /\.(ts|mjs)$/ で .tsx を除外）。

2) src/lib/stripe/portal-features.ts:86 は `` `npm run dev` を再起動 `` を含む nextAction を返し、src/lib/ops/portal-status.ts:58-64 がその nextAction をそのまま doctor の Check（運営者向けの唯一の出口）へ流している。この nextAction は 2026-08-18 の T-M8-128 で追加されたもので、検査（T-M8-49／T-M8-51）より後に増えた。

3) 【壊して確認】portal-features.ts の `npm run dev` を `npm run totally:bogus:script` に書き換えて `npx vitest run src/lib/ops/next-action-commands.test.ts` を実行 → 3 passed（落ちない）。同じ壊し方を範囲内の portal-status.ts に施すと `missing` に検出されて落ちる。検出器自体は生きているが、列挙されたディレクトリの外では原理的に当たらない＝指摘どおり。

4) 件数も一致。src+scripts を全走査すると 49 箇所ヒットし、そのうち 11 箇所が現 SEARCH_DIRS の外（指摘の「11箇所」と一致）。現時点では 11 箇所すべて実在するスクリプト名（dev / logo / icons:generate / smoke:live / doctor / check:csp-nonce / typecheck / release:* 等）なので実害は出ていない点も指摘の記述どおり。



### 8. [medium] doctorの「定時実行」検査が scheduler_tick だけで、metrics_collector と follower_snapshot は止まっても緑

- 種別: 空振り検査 ／ 領域: jobs・cron・スケジュール
- 場所: `src/lib/ops/diagnostics.ts:515`
- 根拠: collectDiagnostics は cron_runs から `job_name='scheduler_tick'`（:515）と `job_name='news_fetch'`（:528）の最終claimed_atしか読まない。judgeScheduler(:338) の表示名は「定時実行」で、要件04 §6 は定時トリガーを4本と定義しているのに、metrics_collector / follower_snapshot の鮮度はどこにも出ない（ops/daily-summary.ts の collectDailySummary も cron_runs を見ない）。この2本が止まると実績（表示回数・いいね）とフォロワー推移が静かに欠け続けるが、doctorは緑・サマリも無言。要件04 §6 が警告するとおり『定時実行が止まってもアプリは200を返し画面には何も出ない』状態がこの2本については残っている。vercel-crons.test.ts は vercel.json の定義漏れは検出するが、登録済みで失敗し続ける状態は検出できない。
- 直し方: judgeScheduler を job_name ごとの最終claimed_atを受け取る形へ広げ、4本すべて（tick=15分、news_fetch=既存の時間判定、metrics_collector/follower_snapshot=毎時なので例えば90分）を判定して1行ずつ出す。判定関数は純粋なので単体テストで『1本止めたら error になる』ことを固定でき、意図的に壊して落ちることを確認できる形になる。
- 反証を試みた結果: 反証できなかった。指摘は実在する。

確認した事実:
1. src/lib/ops/diagnostics.ts:513-521 の collectDiagnostics は `from cron_runs where job_name = 'scheduler_tick'` しか読まず、その1値を judgeScheduler（:337、表示名「定時実行」）へ渡す。:528 は `job_name = 'news_fetch'` を judgeNews へ渡す。
2. `grep -rn "cron_runs" src scripts e2e`（テスト除く）の結果は上記2箇所の read と、cron.ts の insert・schedule-cleanup.ts の削除・locks.ts のコメントだけ。metrics_collector / follower_snapshot の最終claimed_atを読むコードはリポジトリ内に存在しない。
3. src/lib/ops/daily-summary.ts は cron_runs を一切参照しない（出力行は job成否・ニュース・止まっている処理・メール・費用・DB使用量のみ）。よってサマリにも出ない。
4. src/lib/jobs/cron.ts の runSchedulerTick は cancel/enqueue/dispatch/stale回収/メール/cleanup/prompt同期/日次サマリ/投稿分析起票のみで、metrics・followerを代替実行しない（tickの鮮度が代わりに担保する形にはなっていない）。
5. 両jobのアカウント単位の失敗は console.error だけに落ちる（metrics-collector-server.ts:30、follower-snapshot-server.ts:26）。通知も検査も無い。
6. src/lib/ops/vercel-crons.test.ts は vercel.json と要件04 §6 の表（定義）の一致のみを検査し、実行時の鮮度

### 9. [medium] scheduler_tick の回収ステップ(recoverSchedule)のSQLがどのテストでも実行されない（正規表現スタブのみ）

- 種別: 空振り検査 ／ 領域: jobs・cron・スケジュール
- 場所: `src/lib/jobs/schedule-recovery.test.ts:8`
- 根拠: schedule-recovery.test.ts は `/with expired as/`・`/from schedule_slots ss/`・`/insert into notifications/`・`/requires_quote_url/` の4本の正規表現でSQL文字列を振り分け、返り値をスタブする makeDb だけで検証している（SQLは一度も実行されない）。`grep -rn recoverSchedule src` の結果、`.db.test.ts` は存在せず、cron.db.test.ts のtickテストも回収ステップを含まない。したがって期限切れ判定（`scheduled_for + interval '10 minutes'`）、未enqueue窓（`time_jst + 10min〜70min`・schedule_run_keyの文字列組み立て）、`status='queued'` 絞り込み、通知の notification_config 参照のどれを壊しても全テストが緑のまま通る。ここは jobs を canceled にし通知を作る書き込み経路なので、壊れると『予約が黙って消える／通知が来ない』に直結する。加えて cancelFeatureDisabledJobs(:137-143) には LIMIT が無く、同ファイル冒頭の docblock と要件04 §6 の『cancel 500/起動』と食い違っている。
- 直し方: schedule-recovery.db.test.ts を追加し、実DBで (a) 期限切れ schedule post_generation の cancel＋schedule_missed通知（dedupe key）、(b) enqueueされなかった過去窓のslotへの通知、(c) requires_quote_url な queued job の cancel、(d) 通知OFFなら行を作らないこと、を検証する。既存の単体テストはdedupe key組み立ての固定に限定する。あわせて cancelFeatureDisabledJobs に `limit 500` 相当（対象idのサブクエリ）を入れて正本の1起動上限へ揃える（複数tickで同じ集合を処理するため最終状態は同じ）。
- 反証を試みた結果: 実測で確認：指摘の中核（回収ステップの判断がどのテストでも固定されていない）は実在する。mutation testingの結果、`src/lib/jobs/schedule-recovery.ts` の (a)期限切れ判定を `interval '10 minutes'`→`'10 hours'`、(b)`and status = 'queued'` 削除、(c)`notification_config->'error'`→`->'zzz'`（誰にも通知しない）、(d)`'slot:'`→`'sloth:'`（schedule_run_key不一致で常に「未enqueue」判定）、(e)未enqueue窓 `'70 minutes'`→`'11 minutes'`、(f)`requires_quote_url`→`requires_quote_urls`（flag OFF中も引用jobを止めない）、(g)`cancelExpiredJobs` を `and false` で完全無効化、のいずれも schedule-recovery.test.ts / cron.db.test.ts / scheduler-tick/route.db.test.ts を14件全緑で通過した（ローカルSupabase稼働・skipなしで実行）。`grep -rn recoverSchedule` の通り `schedule-recovery.db.test.ts` は存在せず、単体テストは4本の正規表現でSQLを振り分ける makeBd スタブのみ。npm scriptの機械検査にも該当するものは無い。ただし指摘の根拠には2点の誇張があり、報告時は文言を直すべき：(1)「SQLは一度も実行されない」は誤り。`runSchedulerTick` の段(1)として cron.db.test.ts と route.db.test.ts が実PostgreSQLに対して実際にSQLを流しており、列名を `gj.slot_id`→`gj.slot_id_typo` に壊すと3件failする（＝スキー

### 10. [medium] 「運営者へ示すコマンドはそのまま動く」検査の探索先が手書き列挙で、最新のdoctor案内が検査外

- 種別: 空振り検査 ／ 領域: 運営コマンド・診断・メール
- 場所: `src/lib/ops/next-action-commands.test.ts:37`
- 根拠: 同テストは SEARCH_DIRS = [src/lib/ops, scripts] の2つを手書きで列挙している（37行）。T-M8-128（2026-08-18）で doctor の nextAction が src/lib/stripe/portal-features.ts:86 に追加され（「`npm run dev` を再起動してください」）、これは走査対象外。実際に同じ collectFiles/SEARCH_DIRS を再現して走らせると走査25ファイル・portal-features.ts は含まれない（コマンド抽出37件）。つまり同ファイルのコマンド名を存在しないものへ壊してもテストは緑のまま＝この検査は最新の運営者向け案内を守っていない。加えて REQUIRED_FLAGS（33行）も手書きで、`--target` が無いと必ず失敗する `auth:templates`（scripts/push-auth-templates.mjs:73 で fail）が未登録。
- 直し方: SEARCH_DIRS を「src 配下＋scripts 全体」に広げ（`npm run` を含む運営者向け文字列を持ち得る範囲すべて）、除外はテストファイルだけにする。REQUIRED_FLAGS へ `auth:templates: "--target"` を追加する。さらに現在の found 件数を下限に上げて空振り検知を効かせる。テストの追加・強化のみで実装は変えない（現状の案内はいずれも実在コマンドなので緑のまま通る）。
- 反証を試みた結果: 反証できなかった。実測で確認: (1) src/lib/ops/next-action-commands.test.ts:37 の SEARCH_DIRS は [src/lib/ops, scripts] のみで、同じ collectFiles を再現すると走査25ファイル・抽出38件、src/lib/stripe/portal-features.ts は含まれない。(2) portal-features.ts:86 の運営者向け案内 `npm run dev` を存在しない `npm run devzz` へ改変して当該テストを実行したが 3 tests passed（緑）のまま＝壊しても落ちない空振り。改変は復元済み、git status はクリーン。(3) この案内は確かに doctor の出口に乗っている（portal-status.ts:3 で judgePortalFeatures を import、:64 で judged.nextAction を check へ渡し、diagnostics.ts:643 が judgePortal を checks へ push）。npm コマンド実在を守る他テストは無い。(4) REQUIRED_FLAGS の欠落も事実（scripts/push-auth-templates.mjs:73-78 で --target 未指定なら fail、auth:templates は未登録）。ただし現行の案内文（auth-url-status.ts:153,178,198）は全て --target を含むため、これは将来の退行に対する穴。(5) 提案どおり src 全体＋scripts へ広げ auth:templates を追加した状態を再現すると 388ファイル・49件抽出で missing 0・incomplete 0 のため、実装変更なしで緑のまま通り、found.length の下限も 49 まで上げられる。

### 11. [medium] pattern-spec.test.ts の SEEDED 表は「seed が入れる既定値」と称しながら DB と突き合わせていない（すでに p2 が食い違っている）

- 種別: 空振り検査 ／ 領域: 投稿パターン
- 場所: `src/lib/post/pattern-spec.test.ts:28`
- 根拠: 28行のコメントは「seed が入れる既定値（supabase/migrations/20260818000001_post_patterns.sql）」と書くが、値は手書きの定数で、DB の seed_default_post_patterns() や pattern_spec_of() と比較する箇所が無い。実際に p2 は `placeholders: []`（44-52行）だが、migration 20260818000008 は p2 に `[{"name": "自分の考え"}]` を入れる＝すでに乖離している。DB 側の seed を突き合わせているのは post-patterns.db.test.ts:101 だけで、そこは max_posts と name しか見ていない。したがって migration の web_search_max_uses（例: p6 の 3）や source_policy、placeholders を書き換えても落ちるテストが1つも無く、「既定6種の振る舞いは移行前と一致する」という SEEDED の目的を守れていない。
- 直し方: SEEDED を post-patterns.db.test.ts 側から実 DB の pattern_spec_of() 出力と1件ずつ比較する形にする（seed_key → 期待 spec の表を1か所に置き、pure テストと db テストの両方が同じ表を読む）。意図的に migration の 1 値を変えたら落ちることを確認する。
- 反証を試みた結果: 反証できず、指摘どおり実在する。(1) pattern-spec.test.ts:27 のコメントは「seed が入れる既定値（migration 20260818000001）」と称するが、SEEDED(28-101行)は手書き定数で、DB の seed_default_post_patterns()／pattern_spec_of() と突き合わせる箇所はファイル内に無い。(2) 実DB（ローカル 54322）で x_accounts トリガ経由の seed をロールバック付きトランザクションで実行し pattern_spec_of() を出力したところ、p2 は placeholders=[{"name":"自分の考え"}]（migration 20260818000008 が設定）だが、テストの SEEDED は placeholders: []（51行）＝既に乖離。他5件は現状一致しているが、検査があるからではなく偶然。(3) 決定的な実験: ローカルDBの seed_default_post_patterns(uuid) を差し替えて p6 の web_search_max_uses 3→1、p1 の source_policy always→never、p2 の placeholders を空に壊し、pattern_spec_of() で新規アカウントへ実際に反映されることを確認したうえで REQUIRE_DB=1 vitest run src/lib/post src/lib/db src/lib/jobs を実行 → 600件中 598 passed / 1 skipped / 1 failed。唯一の失敗 src/lib/jobs/follower-snapshot.db.test.ts:105 は関数を元に戻した状態でも同じく失敗する既存の無関係な失敗（確認済み）。つまりパターン関連のテストは1つも落ちない。実験後、pg_get_functiondef で元定義と完全一致することを確認して復旧済み（DBに残存変更なし）。(4) DB側の突き合わせは

### 12. [medium] thread-count.test.ts が本番から呼ばれない変換関数を検証している（壊しても本番は壊れない）

- 種別: 空振り検査 ／ 領域: 投稿パターン
- 場所: `src/lib/post/thread-count.test.ts:20`
- 根拠: maxPostsFromThreadCount（post-patterns-store.ts:300）と PATTERN_MAX_THREAD_COUNT（同 287）の参照は thread-count.test.ts だけ（grep 済み）。T-M8-130 で「画面はスレッド数で聞き、ここで変換する」形にしたが、T-M8-132 でスレッド数の入力欄をやめてプロンプトから読む方式（maxPostsFromPrompt）へ変えたため、変換の入口が消えた。テストは 20-40行で境界（0→1、99→8、-3→1）まで丁寧に見ているが、これらを壊しても画面・保存・生成のどれも壊れない。同ファイルが守っているつもりの「スレッド数 ⇄ 総ポスト数の変換が1箇所」という不変条件は、実際には maxPostsFromPrompt / threadCountOf / threadCountLabel 側にある。
- 直し方: maxPostsFromThreadCount と PATTERN_MAX_THREAD_COUNT を削除し、テストは実際に使われている threadCountOf／threadCountLabel／threadCountFromPrompt／maxPostsFromPrompt の境界（読み取り不能→8、メインポストのみ→1、上限の頭打ち）へ付け替える。
- 反証を試みた結果: 反証できなかった。指摘は実在する（ただし提案の一部は削りすぎ注意）。

【確認した事実】
1. 参照が本当にテストだけ。`rg` を node_modules 除外でリポジトリ全体（src/e2e/scripts/docs 含む）にかけた結果、`maxPostsFromThreadCount` と `PATTERN_MAX_THREAD_COUNT` の出現は定義2箇所（/Users/kotamatsumoto/private-workspace/x-system/src/lib/post/post-patterns-store.ts:287, :300）と /Users/kotamatsumoto/private-workspace/x-system/src/lib/post/thread-count.test.ts のみ。`export *` の再エクスポート（バレル）は src 配下に1件も無いので、名前ベースの grep で漏れる経路は無い。

2. 実際に壊して確認した（指摘どおり本番は壊れない）。`maxPostsFromThreadCount` の本体を `return 999;`、`PATTERN_MAX_THREAD_COUNT` を `42` に書き換えて `npx vitest run src/lib/post src/components src/app` を実行 → **落ちたのは thread-count.test.ts の4件だけ**（Test Files 1 failed | 43 passed、Tests 4 failed | 458 passed）。他の43ファイル（画面・保存・生成側）は全て緑のまま。検査後にファイルは復元済み（`git status` クリーン）。

3. 入口が消えた経緯も一致。T-M8-130（4840c74）が thread-count.test.ts を新規追加、T-M8-132（eb681d2）が post-patterns-store.ts と pattern-fields.tsx を書き換えて**スレ

### 13. [medium] 「設計書§6の全文と一致させ、スナップショットテストで乖離を検出する」がドキュメントを読んでいない

- 種別: 空振り検査 ／ 領域: 投稿パターン
- 場所: `src/lib/prompts/gen-prompts.ts:3`
- 根拠: ヘッダコメント（3行）は設計書との乖離検出を謳うが、gen-prompts.test.ts:19 の検査は `expect({...SYSTEM_DEFAULT_TEMPLATES}).toMatchSnapshot()` で、比較先は src/lib/prompts/__snapshots__/gen-prompts.test.ts.snap（コード自身の写し）。docs/プロンプト設計書.md を読む処理はリポジトリ全体に存在しない（readFileSync を使うテスト一覧に prompts 配下は無い）。定数だけ直して `-u` でスナップショットを更新すれば緑のまま docs と乖離する。今回の突き合わせでは §6.2〜6.7 と PT_P1〜P6 は一致していたが、それを守っている検査は無い。
- 直し方: warning-docs-sync.test.ts と同じ方式で docs/プロンプト設計書.md の §6.x のコードフェンス本文を読み、SYSTEM_DEFAULT_TEMPLATES と1文字単位で比較するテストへ置き換える（スナップショットは残しても良いが、乖離検出の役はドキュメント比較側に持たせる）。定数を1文字変えて落ちることを確認する。
- 反証を試みた結果: 反証できず、指摘は実在する。(1) gen-prompts.test.ts:19 の検査は `expect({SYS_GEN,...SYSTEM_DEFAULT_TEMPLATES}).toMatchSnapshot()` のみで、比較先は __snapshots__/gen-prompts.test.ts.snap（コード自身の写し）。`rg "プロンプト設計書"` を scripts/・package.json・.github/ に対して実行しても check-doc-dates.mjs の無関係な1件しか無く、docs/プロンプト設計書.md を読むテスト・スクリプトは存在しない。docs 側が言う「コード定数から生成する」ジェネレータも存在しない（`rg gen-prompts` に doc を書き出す処理が無い）。(2) 実際に壊して確認した: PT_P1 の「読者にとっての意味を1つに絞る。」を「2つに絞る。」へ変更（=§6.2 と乖離）し、`npx vitest run src/lib/prompts -u` → Snapshots 1 updated / 31 passed。続けて `npx vitest run` 全体 → 2184 passed（唯一の失敗は無関係な src/lib/supabase/auth.local.test.ts のローカルSupabase接続）。`npm run check:doc-refs` / `check:doc-dates` / `audit:check` も全て緑。つまり定数を変えて `-u` すれば docs と乖離したまま全検査が通る=空振り。検証後に両ファイルを復元し git status クリーンを確認済み。(3) 現時点では §6.1〜6.12・6.14・6.15 のコードフェンス本文と対応定数は1文字単位で一致しており（6.13 PT-L3 は定数なし）、doc_drift は無い。よって欠陥は「乖離を守っている検査が無い」という dead_check。提案どおり warning-docs-sync.t

### 14. [medium] インラインバナー検査の検出条件が1つの完全一致リテラルだけで、枠線色・paddingを変えた手書きバナー5箇所を素通しする

- 種別: 空振り検査 ／ 領域: 画面・UI共通部品
- 場所: `src/components/ui/notice-banner.test.ts:39`
- 根拠: notice-banner.test.ts:39 の判定は `source.includes(`border border-${tone}-fg/25`)` の完全一致1本のみ。そのため tone 背景を使いながら枠線色・padding・文字サイズだけ変えた手書きバナーは全部通る。実在するもの: src/app/app/settings/base-md-editor.tsx:200 と src/app/app/settings/prompt-templates-editor.tsx:175 が `rounded-card border border-hairline bg-warn-bg px-4 py-2 text-sm text-warn-fg`（枠線が hairline・py-2・text-sm）、src/app/plans/page.tsx:143 が `flex items-start gap-2.5 rounded-card bg-warn-bg px-4 py-3`（枠線なし）、src/app/app/settings/api-key-settings.tsx:345 が `rounded-card bg-success-bg p-3 text-success-fg`、src/app/login/page.tsx:56 が `rounded-lg bg-success-bg p-3 text-sm text-success-fg`（角丸も rounded-card ではない）。base-md-editor.tsx:193 は同じファイル内で `<Notice tone="warn">` を使っているので、1ファイルの中に警告バナーの見た目が2系統ある。この検査の「空振りしていない」ガード（同ファイル 51-54）は notice.tsx に4つのtone文字列があることだけを確かめており、検出器が実際に違反を捕まえられるかは見ていないため、盲点が緑のまま隠れる。
- 直し方: 判定を「tone の背景ユーティリティ（bg-warn-bg / bg-info-bg / bg-success-bg / bg-danger-bg）が Notice・Badge・page-state 以外の className に現れたら違反」へ広げる（アイコンチップと App Shell 全幅バーは現行どおり明示的に除外リスト化する。除外はファイル名列挙ではなく「rounded-full/rounded-pill を含む」「border-b を含む」という形の条件で書く）。あわせて「検出器が既知の違反文字列を入れたら落ちる」ことを固定するため、テスト内でダミー文字列を判定関数へ通して true を返すアサーションを1本足す（現行の owner 文字列チェックだけでは空振りが見えない）。検査の変更なので画面の振る舞いは変わらない。
- 反証を試みた結果: 反証できなかった。指摘の中核は実測で確認できた。

**確認1: HEAD で検査は緑のまま、手書きバナーが実在する**
`npx vitest run src/components/ui/notice-banner.test.ts` → 2 passed。その状態で以下が存在する（grep で確認）:
- `/Users/kotamatsumoto/private-workspace/x-system/src/app/app/settings/base-md-editor.tsx:200` `rounded-card border border-hairline bg-warn-bg px-4 py-2 text-sm text-warn-fg`（`role="alert"`）
- `/Users/kotamatsumoto/private-workspace/x-system/src/app/app/settings/prompt-templates-editor.tsx:175` 同一文字列
- `/Users/kotamatsumoto/private-workspace/x-system/src/app/login/page.tsx:56` `rounded-lg bg-success-bg p-3 text-sm text-success-fg`（`role="status"`）
- `/Users/kotamatsumoto/private-workspace/x-system/src/app/plans/page.tsx:143` `flex items-start gap-2.5 rounded-card bg-warn-bg px-4 py-3`（枠線なし・恒常表示のBYOK注意）

前2件は **border＋背景＋文字色の3点セットが揃っており、テスト自身のdocstring（14-16行）が宣言した判定基準を満たしているのに検出されない**。実装が自分の宣言した射程に届いていない。さらに base-md-editor.tsx は同ファイル193

### 15. [medium] 要件03 §2.2 と §6 が Portal の「契約が見つからないとき」で矛盾し、§2.2 が実装と逆

- 種別: ドキュメント乖離 ／ 領域: 認証・課金・利用量
- 場所: `docs/requirements/03_auth_billing_usage.md:64`
- 根拠: §2.2（64行）は「`intent`が無い場合と本人の`stripe_subscription_id`が不明な場合は`flow_data`を付けずPortalのトップを開く」。§6（同文書）は「それでも見つからなければ**黙ってPortalのトップを開かず**`subscription_required`で止める（2026-08-05に利用者が実際に踏んだ）」。実装 src/lib/stripe/portal.ts:139-151 は intent があるときに `resolveSubscriptionForFlow` で補い、それでも null なら `subscription_required` を返す（トップは開かない）。トップを開くのは intent が無いときだけ。つまり §2.2 は T-M8-56 以前の挙動の記述で、同じ判断が2箇所に別々に書かれ片方だけ更新されている。
- 直し方: §2.2 の該当文を「`intent` が無い場合だけ `flow_data` を付けずPortalのトップを開く。`intent` があるのに対象契約が見つからない場合は §6 のとおり `subscription_required` で止める」へ改め、判断の正本は §6 の1箇所に置いて §2.2 からは参照させる。docsのみ。
- 反証を試みた結果: 反証できなかった。実ファイルで3点すべてを確認した。

1. docs/requirements/03_auth_billing_usage.md:64（§2.2 Customer Portal作成）:「`intent`が無い場合と本人の`stripe_subscription_id`が不明な場合は`flow_data`を付けずPortalのトップを開く（Stripeが400を返して「押しても開かない」状態になるのを避ける）」
2. 同文書:156（§6 プラン変更）:「対象のsubscriptionは`profiles.stripe_subscription_id`を正とし、nullのときはStripeからその顧客の変更できる契約（active／trialing／past_due・新しい順）を引いて補う（T-M8-56）。それでも見つからなければ**黙ってPortalのトップを開かず**`subscription_required`で止める」
3. 実装 /Users/kotamatsumoto/private-workspace/x-system/src/lib/stripe/portal.ts:138-152 は `if (intent)` のときだけ `resolveSubscriptionForFlow` で補い、それでも null なら `apiError(new AppError("subscription_required", { details: { settingsPath: "/plans" } }))` を返す。Portalのトップが開くのは `intent` が null のとき（flow_data を組まない）だけ。呼び出し口は /Users/kotamatsumoto/private-workspace/x-system/src/app/api/stripe/portal/route.ts のみで迂回経路は無い（handlePortalRequest の参照は route.ts / route.db.test.ts / portal.ts 

### 16. [medium] 要件03 §1/§5 の「契約状態を先に判定」「details に現在statusと /plans を含める」が、実際に配線されている経路と違う

- 種別: ドキュメント乖離 ／ 領域: 認証・課金・利用量
- 場所: `docs/requirements/03_auth_billing_usage.md:31`
- 根拠: §1（31行）「共通実行ガードは契約状態を先に判定し、契約実行不可なら`subscription_required`を優先する。契約が`trialing|active`の場合だけ規約versionを確認する」。§5（146行）「…`details.missing=[subscription]`、現在status、`details.settingsPath=/app/settings?tab=billing|/plans`を含める」。実装では生成・投稿系Actionが最初に呼ぶのは `requireExecutionUserId`（src/app/actions/_helpers.ts:84-95）→ `requireLegalConsent` で、契約状態を見ずに法務同意だけを先に判定する（src/app/actions/generation-jobs.ts:58 など12箇所）。契約判定はその後 `checkExecutionPrerequisites`（src/lib/execution-prereqs.ts:89-91）で行われ、投げる AppError は `prereqErrorToAppError`（同:167-171）が `{ missing, settingsPath }` だけを載せるため **現在statusは含まれず**、`settingsPath` は `ITEM_PATH.subscription = "/app/settings?tab=billing"`（同:36）固定で `SUBSCRIPTION_ACCESS[status].actionPath`（canceled/incomplete は `/plans`）を使わない。status入りの `requireExecutableSubscription`（subscription-access.ts:71-82）は X連携（oauth-start/oauth-callback）からしか呼ばれていない。
- 直し方: docsを実装に合わせて2点直す。(1) §1 の順序記述を「法務同意を先に確認し、契約状態は実行前提（`checkExecutionPrerequisites`）で判定する」に改める（解約済みで版が古い利用者には `legal_consent_required` が先に出ることも明記）。(2) §5 の details 仕様を経路別に書き分ける——mutation経路は `missing`＋`settingsPath`（現状は課金タブ固定）、X連携経路は現在status入り。`actionPath` を mutation 側でも使う統一は誘導先が変わる＝振る舞い変更なので、別タスクとして要決定に起票する。
- 反証を試みた結果: 反証できず、指摘は実在する。(1)順序: docs/requirements/03_auth_billing_usage.md:31 は「共通実行ガードは契約状態を先に判定し、契約実行不可なら subscription_required を優先する。契約が trialing|active の場合だけ規約versionを確認する」と書くが、実装は src/app/actions/_helpers.ts:84-95 の requireExecutionUserId が requireLegalConsent だけを先に呼び、src/lib/auth/legal-consent-server.ts:27-58 は同意4列しか見ない（自コメントに「ここでは法務同意だけを見る」と明記）。よって canceled/past_due で版が古い利用者には legal_consent_required が先に出る。配線先は generation-jobs.ts:58,82,111,127,143,159 と schedule.ts:66,84,121,141 の計10本（指摘の「12箇所」は数の誤り。docs 06 §1.3 の「生成6本・スケジュール4本」が正しい）。(2)docsの順序を実装しているのは requireExecutionAccess（src/lib/auth/execution-guard.ts:16-17）だが、rg の結果その呼び出し元は legal-consent.test.ts:51 と execution-guard.test.ts:15,34,54 のテストのみで本番経路から呼ばれていない。git log -L 31,31 で line31 は ddf793f feat(T-M1-18) 由来＝この未配線ガードのための記述であり、T-M8-73 は 06 §1.3 だけ更新して 03 §1・05 §1 を残した。(3)details: 03:146 は missing=[subscription]＋現在status＋settingsPath=/app/s

### 17. [medium] deployment.md の手作業手順が signup をリンク方式（token_hash&type=signup）で説明しており、6桁コード方式と食い違う

- 種別: ドキュメント乖離 ／ 領域: 認証・課金・利用量
- 場所: `docs/operations/deployment.md:190`
- 根拠: 190行「Authentication → Emails → Templates の **Confirm signup** と **Reset password** を…同じ内容へ貼り替える（`{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=signup` / `&type=recovery`）」。実装は signup が6桁コード方式で、テンプレートに必要なのは `{{ .Token }}`（scripts/push-auth-templates.mjs:44-52 の `requires: "Token"`、supabase/templates/confirmation.html、src/lib/ops/auth-url-status.ts:145 の doctor 検査）。同ページ189行自身が「確認メールに入力すべき6桁コード（`{{ .Token }}`）が載らず、登録を完了できない」と書いており、直後の手作業手順だけが旧仕様。さらに §2.9（207行）の表は「メールテンプレート…抜けたときの症状: 確認リンクとパスワード再設定が『リンクを確認できませんでした』になる」と旧症状のままで、2026-08-18 の実際の原因である `mailer_otp_length`／`rate_limit_email_sent`（`auth:templates` が揃える値）が表に無い。
- 直し方: 190行の手作業説明を「Confirm signup は `{{ .Token }}` を含む `supabase/templates/confirmation.html`、Reset password は `{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=recovery` を含む `recovery.html`」へ分けて書く。§2.9 の表の症状欄を「確認メールに6桁コードが載らず登録を完了できない／再設定リンクが失敗する」に直し、`AUTH_SETTINGS` が揃える項目（コード桁数6・有効期間1時間・メール送信数/時30・検証レート）を行として追加する。docsのみ。
- 反証を試みた結果: 指摘は実在する。deployment.md:190 は「Confirm signup と Reset password を…（`{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=signup` / `&type=recovery`）」と旧リンク方式で説明しているが、正本の supabase/templates/confirmation.html は `{{ .Token }}`（6桁コード）のみで token_hash も RedirectTo も含まない（recovery.html だけがリンク方式）。実装も6桁で一貫: scripts/push-auth-templates.mjs:44-52 の requires:"Token"、src/lib/ops/auth-url-status.ts:135-160 の doctor 検査、src/lib/auth/email-code.ts:16 の EMAIL_CODE_LENGTH=6、supabase/config.toml:233 の otp_length=6。直前の189行自身が「6桁コード（{{ .Token }}）が載らず登録を完了できない／再設定は引き続きリンク方式」と正しく書いており、括弧内だけが旧仕様の取り残し（＝意図的な抽象ではない）。§2.9 の表（208行）も「{{ .RedirectTo }} を使うためドメインに依存しない」「症状: 確認リンクとパスワード再設定が『リンクを確認できませんでした』」で signup 側が旧仕様、§4.5（262行）にも同型の旧記述が残る。ただし指摘の一部は不正確: rate_limit_email_sent は210行に「メール送信数/時 | Authentication → Rate Limits | 同じ（30以上）」として既に表にある。表に無いのはコード桁数（mailer_otp_length=6）・有効期間・検証レート。また AUTH_SETTINGS（push-auth-templates.mjs:1

### 18. [medium] PRD の原価試算とリスク表に回数制（文章100回・画像20枚・「4枠」）が残存

- 種別: ドキュメント乖離 ／ 領域: 認証・課金・利用量
- 場所: `docs/PRD.md:232`
- 根拠: PRD:232「…文章100回・画像20枚も上限まで使うと4枠合計約$12.40（約1,984円）となる」、PRD:313「通常投稿・URL付き投稿・文章・画像の4枠で制御し」。同文書の O-4（198行）と §6 表（215行）・§6.1 表（227-228行）は既に「3枠／AIクレジット1000（実費消費・上限1,000円で固定）」へ更新済みで、実装も src/lib/plans.ts:72-76 の3枠（normalPosts 200 / urlPosts 20 / aiCredits 1000）と src/lib/usage/usage-summary.ts:20-23 の3キーだけ。232行の合計額は回数制前提の計算なので、§6.1 の結論（最大約2,120円）とも噛み合わない。
- 直し方: 232行を現行の3枠前提へ書き換える（X書込 約$5.20 ＋ AIクレジット上限 $6.25 の形で §6.1 表の合計と一致させる）。313行の「文章・画像の4枠」を「AIクレジット・通常投稿・URL付き投稿の3枠」に直す。docsのみ。
- 反証を試みた結果: 反証できず、指摘は実在する。

確認した事実:
- `/Users/kotamatsumoto/private-workspace/x-system/docs/PRD.md:232`「…X書き込み約$5.20。**文章100回・画像20枚**も上限まで使うと**4枠**合計約$12.40（約1,984円）となる」。旧扱いの注記（同ファイル234行の「旧見積もりは誤りだった」のような打消し）は付いておらず、現行の上限を述べる文として読める。
- 同 `docs/PRD.md:313`（リスク表）「通常投稿・URL付き投稿・**文章・画像の4枠**で制御し」。
- 一方で同文書の O-4（198行）は「3枠で管理する：AIクレジット1000／通常投稿200／URL付き投稿20」、§6 表（215行）と §6.1 表（227-228行）も「AIクレジット1000（最大1,000円＝$6.25）」「枠で上限が決まる部分の合計 最大約$13.25（約2,120円）」、239行・324行・変更履歴 v1.18（353行・2026-08-16, T-M8-109）も金額制へ更新済み。232行・313行だけが取り残されている。
- 実装も3枠のみ。`src/lib/plans.ts:72-76` は `usageLimits: { normalPosts: 200, urlPosts: 20, aiCredits: 1000 }`、`src/lib/usage/usage-summary.ts:15-19` の `UsageSummary` は `ai_credits` / `normal_posts` / `url_posts` の3キー（コメントに「3枠・T-M8-109」と明記）。
- 回数枠はDBからも消えている。`supabase/migrations/20260816000001_ai_credits.sql:8-15` が `usage_counters` の `generations_count` / `images_count` と各range制約を **drop**

### 19. [medium] 要件04の利用枠記述がAIクレジット制（T-M8-108/109）に追随していない

- 種別: ドキュメント乖離 ／ 領域: jobs・cron・スケジュール
- 場所: `docs/requirements/04_jobs_and_automation.md:143`
- 根拠: §7.1(:143)『premiumは生成枠、画像ONなら画像枠』、§8 step2(:162)『生成枠をreserve』、§9 step1/5(:178,:182)『画像枠をreserve/refund』、§9末尾(:186)『画像枠のreserve/refundはM6で実装する』、§4(:65)『reserveのrefund…usage_counters を -1』『reserveは文章系top-level job（生成・LRN・SUGGEST・MD-MERGE）と画像生成jobで実装済み』が現状と合わない。実装は単一プール: reserve-if-premium.ts:26-29 が generation/image ともに `PLANS.premium.usageLimits.aiCredits` を上限に使い、generation-reserve.ts:18-28 は両typeを `ai_credits_used`／counter_type `ai_credit` へ寄せ、reserve量はモデル別倍数（textReserveEstimate/imageReserveEstimate）、refundUsage(:168-193) は -1ではなく元reserve行のdelta絶対値ぶん戻す。schedule-enqueue.premiumBudgetOk(:118-149) も生成枠/画像枠ではなくクレジット残高1本で判定する。画像reserveは image-generation.ts:267 で実装済み（M6待ちではない）。SUGGESTは `grep -n 'reserve' src/lib/jobs/suggestion*.ts` が0件で、§12 の『生成枠は消費しない』が正しく、§4の記述だけが古い。
- 直し方: §4/§7.1/§8/§9 の『生成枠・画像枠』表現をAIクレジット1本（モデル別倍数消費・成功時settle・失敗時全額返還）へ書き換え、§4の『usage_counters を -1』を『元reserveのdelta分』へ、reserve対象の列挙からSUGGESTを外し、§9末尾の『M6で実装する』を実装済みへ直す。要件03 §7（v1.27でクレジットへ更新済み）を参照先として明記する。ドキュメントのみの修正で実装は変えない。
- 反証を試みた結果: 反証できず、指摘は実在。要件04 v1.34 は「クレジット」を1語も含まず（grep 0件）、T-M8-108(5ded471)/T-M8-109(6a8eafb) が PRD・要件02・要件03 のみ更新して04を触っていないことも git show --stat で確認した。実装は単一プール: reserve-if-premium.ts:26-29 が generation/image とも PLANS.premium.usageLimits.aiCredits を上限に使い（コメントに「上限はAIクレジット1本」と明記）、generation-reserve.ts:18-29 が両typeを ai_credits_used／counter_type='ai_credit' へ寄せ、reserve量は textReserveEstimate/imageReserveEstimate のモデル別倍数、refundUsage(168-195) は -1 ではなく元reserve行のdelta絶対値ぶん戻す（settleUsage(105-162) が成功時の実費精算）。schedule-enqueue.ts の premiumBudgetOk は ai_credits_used + 見積もり > PREMIUM_LIMITS.aiCredits のクレジット残高1本で判定し、生成枠/画像枠の2本立てではない。reserveIfPremium の呼び出しは image-generation.ts:267 / learning-analysis.ts:223 / post-generation.ts:300 / md-merge-server.ts:48 の4箇所のみで、suggestion*.ts（非テスト）には無い。よって04の :65(§4)「usage_counters を -1」「reserveは…SUGGEST…で実装済み」は事実誤りで、しかも同一文書の :244(§12)「生成枠は消費しない（2026-08-15変更）」と矛盾する。:143(§7.1)・:1

### 20. [medium] news_fetch の {{hours}} マッピングが旧・毎時スケジュールのまま（正本内でも矛盾）

- 種別: ドキュメント乖離 ／ 領域: jobs・cron・スケジュール
- 場所: `docs/requirements/04_jobs_and_automation.md:131`
- 根拠: §6(:131) は『プロンプト設計書 §6.10の{{hours}}＝12:00〜20:00は3、当日9:00/10:00/11:00は前日18:00以降を補うため15/16/17』と書き、docs/プロンプト設計書.md:503 も同じ旧マッピングを持つ。実装は news-research.ts:251 `NEWS_FETCH_JST_HOURS = [10,12,14,16,18,20]`（T-M7-55で3分野×2時間おきへ縮小）で、hoursFor は先頭の10時だけ `24-20+10=14`、残りは `FETCH_INTERVAL_HOURS+OVERLAP_HOURS=3`、リスト外の時刻も14を返す。9:00/11:00 起動は存在せず15/16/17という値もどこにも現れない。同じ §6 の表(:103)自身が『初回10時は夜間を埋める14h』と書いているため、正本内で矛盾している。
- 直し方: §6(:131) の括弧内とプロンプト設計書 §6.10(:503) を現在の対応（起動は10/12/14/16/18/20時、10時＝14時間で前日20時以降を補完、それ以外＝3時間ラップ、窓の重なりで1回失敗しても次回が拾う）へ書き換え、変更履歴に追記する。値の正本がコード側（NEWS_FETCH_JST_HOURS と hoursFor）であることも明記する。
- 反証を試みた結果: 反証できなかった。実装 /Users/kotamatsumoto/private-workspace/x-system/src/lib/jobs/news-research.ts:251-273 の newsLookbackHours は「10時→14／12・14・16・18・20時→3（FETCH_INTERVAL_HOURS 2 ＋ OVERLAP_HOURS 1）／リスト外→14」であり、同249-345行で実際に {{hours}} へ差し込まれている（:349 replaceAll）。9:00・11:00 起動も 15/16/17 という値も実装のどこにも存在しない。一方 docs/requirements/04_jobs_and_automation.md:131 と docs/プロンプト設計書.md:503 は旧・毎時スケジュール時代の「12:00〜20:00は3／当日9:00/10:00/11:00は前日18:00以降を補うため15/16/17」を保持しており、実装と食い違う。「別の節に正しい記述がある」は部分的に真だが、それが解消ではなく正本内矛盾の証拠になる: 同じ §6 の表 04:103（初回10時は14h）・PRD.md:134（初回10:00は前日20:00以降を埋める14時間分）・プロンプト設計書:23（2時間おき JST 10〜20時×3分野）は T-M7-55 で更新済みで、:131 の「18:00始点」は PRD の「前日20:00以降」と直接矛盾する。「未実装」注記なし。具体的な起動時刻と整数値を挙げているため仕様レベルの抽象でもない。プロンプト設計書の変更履歴は v1.24（2026-08-17）まであるが {{hours}} の最終記載は v1.4（2026-07-21・旧マッピング）で、T-M7-55（2026-08-02）の追記は PRD.md:340 にのみ存在しプロンプト設計書側には無い。よって指摘は実在する。補足: docs/decisions/0003-cron-window-claim.md:58 も旧記述だが A

### 21. [medium] news取得結果の分類が news-outcome.ts の1箇所に集約されておらず、cron応答だけ判定が違う

- 種別: ドキュメント乖離 ／ 領域: jobs・cron・スケジュール
- 場所: `src/lib/jobs/news-fetch.ts:57`
- 根拠: 要件04 §6(:112) は『判定は src/lib/news-outcome.ts の1箇所だけに置く（T-M8-83）』と書き、doctor(diagnostics.ts) と 日次サマリ(daily-summary.ts) は classifyNewsOutcome を使う。しかし news-fetch.ts:57-63 の emptyReasonOf が独自に `fetched===0 && dropped>0 → all_dropped` と判定しており、classifyNewsOutcome:138-141 が持つ『除外が published_at:too_old だけなら no_match（良性）』の分岐を持たない。結果、窓より古い記事しか無かった分野を cron応答（emptyCategories）は all_dropped、doctorとサマリは no_match と、同じ状況を正反対に報告する。T-M8-83 が解消した食い違いが、経路を1つ変えて再発している（emptyReasonOf の呼び出し元は cron応答と自テストのみ＝`grep emptyCategories` で確認）。
- 直し方: emptyReasonOf を classifyNewsOutcome の薄いラッパにする（failed/no_match/all_dropped をそのまま返し、healthy/mostly_dropped は null）。差が出るのは『除外が too_old だけで0件』の場合の運営者向けcron応答のラベルのみで、正本（news-outcome.ts）側の判定へ揃う。判定を1箇所へ寄せない方針を採るなら、要件04 §6 の『1箇所だけに置く』を実態に合わせて訂正する。
- 反証を試みた結果: 反証できなかった。要件04 §6(112行) は cron応答の `emptyCategories` を doctor と並ぶ報告経路として名指しした直後に「判定は src/lib/news-outcome.ts の1箇所だけに置く（T-M8-83）」と無条件で書いているが、実装は食い違う。news-fetch.ts:57-63 の `emptyReasonOf` は news-outcome を import すらせず（同ファイルの import は news / news-url / news-research / ai/pipeline / ai/raw-error のみ）、`dropped > 0 ? "all_dropped" : "no_match"` と独自判定しており、classifyNewsOutcome:138-141 の「除外が published_at:too_old だけなら no_match（良性）」分岐を持たない。この状態は実際に到達可能で、news-research.ts:229-236 の applyRecencyPolicy は窓外itemを REASON_TOO_OLD だけ（＋onlyOutsideWindow が無視する `_too_old_*` メタ）で落とすため、窓より古い記事しか無かった分野は items=[]・dropped=N となる。結果、同じ状況を cron応答（route.ts:48-54 で emptyCategories をHTTP応答へ展開）は all_dropped、doctor（diagnostics.ts:122）と日次サマリ（daily-summary.ts:239-247、ともに classifyNewsOutcome 経由）は no_match と正反対に報告する。BACKLOG の T-M8-83（1267-1293行）は修正範囲を「スモーク・doctor・サマリ」と明記しており cron応答は移行対象外だったのに、docs だけが集約済みとして書かれている。cron応答を機械的事実として別判

### 22. [medium] worker が lease 時に canceled にした期限切れ予約は schedule_missed 通知が作られない

- 種別: ドキュメント乖離 ／ 領域: jobs・cron・スケジュール
- 場所: `docs/requirements/04_jobs_and_automation.md:157`
- 根拠: §7.2(:157) は『scheduled_for + 10分までにleaseを取得できなければ…canceledにして schedule_missed 通知を作る』と書き、worker.ts:91-92 のコメントは『schedule_missed 通知の作成は scheduler_tick（M4）が担う』としてleaseJob(:99-103)ではcancelのみ行う。しかし tick 側 cancelExpiredJobs（schedule-recovery.ts:59-73）は `status='queued'` の行しか拾わず、worker が既に canceled にした行は対象外。もう一方の notifyUnenqueuedMissed(:107-113) は当該 schedule_run_key のjobが存在しないslotだけを対象にするため、canceledのjobが在る窓は除外される。結果、worker経路で見送られた予約は利用者へ何も届かない（黙って投稿されない・原則1）。
- 直し方: どちらかへ揃える。(a) 実装を正本へ寄せる: cancelExpiredJobs の抽出を『queued を cancel』＋『直近の canceled で通知未作成のもの』へ広げる（通知は dedupe_key `slot:{id}:{date}:{hh:mm}:missed` で冪等なので二重送信にならない）。(b) 正本を実態へ寄せる: §7.2 と §4 に『worker の lease 経路で canceled になった場合の通知は tick が作らない』ことと理由を明記する。いずれも上記 schedule-recovery.db.test.ts で固定する。
- 反証を試みた結果: 反証できなかった（指摘は実在）。docs §4 手順2（04_jobs_and_automation.md:59）は worker の lease 時チェックについて「超過はcanceledにして schedule_missed 通知を作る」と明記し、§7.2(:157) も同じ通知を約束している。一方 worker.ts:91-104 は canceled 更新のみで通知を作らず（コメントで tick へ委譲）、tick 側 cancelExpiredJobs（schedule-recovery.ts:61）は `status='queued'` のみ抽出、notifyUnenqueuedMissed（:107-113）は当該 schedule_run_key の job が存在しない slot のみ対象なので、worker が canceled にした行はどちらからも永久に外れる。通知を作る箇所は insertMissedNotification（schedule-recovery.ts:24）だけで、generation_jobs のDBトリガー（20260720000003 は updated_at のみ）・daily-summary・doctor・画面(*.tsx)にも代替の可視化は無い。到達可能性も確認: tick は (1)cancel→(3)dispatch 順なので queued 放置分は次tickで通知付きcancelされるが、worker 自身の retry 再dispatch（worker.ts:248-251 の requeueJob→backoff→dispatchJob）は期限を見ないため、「初回tickで skipped_conflict/dispatch失敗 → T+5分にlease → retryable失敗(生成は最大180秒) → 即再dispatch → 2回目のleaseが scheduled_for+10分超」という並びで silent cancel に到達する（この間 job は running なので tick(1)は拾

### 23. [medium] generation_jobs.input の正本例が旧pattern列のままで、実際に書くキーと一致しない

- 種別: ドキュメント乖離 ／ 領域: jobs・cron・スケジュール
- 場所: `docs/requirements/02_data_model.md:697`
- 根拠: §4.5 の例(:695-706)は `"pattern": "p1"` を含み、`user_opinion`・`quote_url`・`news_item_id` などを列挙する。しかし T-M8-129 U5 で旧enumと pattern 列は撤去され（migrations 20260818000005_drop_post_pattern_enum.sql、`grep "input->>'pattern'" src` は0件）、パターンは pattern_id / pattern_spec 列で持つ。実際に書かれるキーは generation-jobs.ts buildInputJson(:99-116) が source_url / quote_url / quote_tweet_id / placeholder_values / instructions / theme（必須）/ image_enabled / news_item_id / prompt_override / base_md_override / image_prompt_override / requested_mode、schedule-enqueue.ts:202-217 が instructions / theme / image_enabled / mode / requested_mode / source_url / placeholder_values / prompt_override。さらに `requested_mode` は3箇所（generation-jobs.ts:114,329、schedule-enqueue.ts:207、smoke/scenarios.ts）で書かれるだけで読み手が存在しない（`grep -rn requested_mode src` に参照側なし。mode の解決は terminal.ts:67 の `input->>'mode'` が使う）。
- 直し方: §4.5 の例を現在書き込まれるキー集合（手動経路と schedule 経路の差＝`mode` の有無も含む）へ更新し、`pattern` を削除して pattern_id/pattern_spec 列を参照する旨を書く。あわせて書き込み専用で誰も読まない `requested_mode` は、3箇所の書き込みを削除して§4.5からも外すか、残すなら読み手（用途）を明記する。どちらも外部の振る舞いには影響しない（読み手が無い）。
- 反証を試みた結果: 反証できなかった。指摘は実在する。(1) docs/requirements/02_data_model.md:693-708 の §4.5 例は `"pattern": "p1"` と `user_opinion` を含むが、migration 20260818000005_drop_post_pattern_enum.sql:41-46 で `pattern` 列と `post_pattern` enum は撤去済みで、`input->>'pattern'` は repo 全体で0件。パターンは pattern_id / pattern_spec 列（§3.8:238-239 に正しく記載）で持つ。(2) `user_opinion` は createGenerationJobSchema（src/lib/jobs/generation-jobs.ts:31-58）に存在せず、`placeholder_values`（T-M8-132、post-generation.ts:360 が読む）へ置き換わっている。(3) 実際に書かれる theme（必須・suggestion-timeline-store.ts:31 が `input->>'theme'` で読む）・placeholder_values・prompt_override・base_md_override・image_prompt_override・mode（terminal.ts:67 が `coalesce(gj.input->>'mode', pj.input->>'mode')` で読む）・parent_draft_id・previous_posts が例に無い。(4) `requested_mode` は repo全体で6箇所（docs 1 + 書き込み5: generation-jobs.ts:114,329 / schedule-enqueue.ts:207 / smoke/scenarios.ts:200,256）で読み手が存在しない。別節に正しい記述がある可能性も確認したが、04_jobs_

### 24. [medium] release:check の構成説明が3箇所で古い（check:doc-dates / check:doc-refs が抜けている）

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `docs/operations/local-development.md:109`
- 根拠: package.json の release:check は `typecheck && lint && check:doc-dates && check:doc-refs && audit:check && test:db && build && check:csp-nonce && test:e2e`（直近2コミット 41aee10・b7516d6 で2本追加）。しかし docs/operations/local-development.md:109 は「typecheck → lint → 依存監査 → test:db → build → check:csp-nonce → test:e2e」、docs/operations/release-checklist.md:15 も同じ旧構成、.github/workflows/ci.yml:3 のコメントも「typecheck → lint → 依存監査 → DBテスト → build → E2E」。41aee10 は local-development.md 自身を編集しているのに、この行だけ更新されていない。
- 直し方: 3箇所の列挙に check:doc-dates / check:doc-refs を追加する（ci.yml はコメントのみ。`verify` job は release:check をそのまま呼ぶので実行内容は変わらない）。docs/README.md:40-41 には既に両検査の説明があるので、そこへのリンクを添えるだけでよい。
- 反証を試みた結果: 反証できなかった。package.json:18 の release:check は9ステップ（typecheck → lint → check:doc-dates → check:doc-refs → audit:check → test:db → build → check:csp-nonce → test:e2e）で、check:doc-dates は 41aee10、check:doc-refs は b7516d6（いずれも2026-08-18、HEADの4〜5コミット前）で追加されている。一方、指摘された3箇所はいずれも7ステップのままで両方が欠落している: docs/operations/local-development.md:109（typecheck → lint → 依存監査 → test:db → build → check:csp-nonce → test:e2e）、docs/operations/release-checklist.md:15（同じ旧構成）、.github/workflows/ci.yml:3 のコメント（typecheck → lint → 依存監査 → DBテスト → build → E2E）。反証の観点はいずれも成立しなかった: (1)「別の節に正しい記述がある」は部分的に真で docs/README.md:40-41 と docs/operations/development-and-testing.md:416,428 が「release:check に含む」と明記しているが、列挙そのものは不完全なまま残り、読者が「このゲートが何を回すか」を確認する場所が古い。(2)「意図的な抽象」ではない——列挙は test:db・check:csp-nonce・test:e2e という具体的なスクリプト名を矢印で順序付きに並べており、audit:check だけが「依存監査」と抽象化され位置も package.json と一致する。9本中2本が単に欠けている。(3)「release-checklist.md:15 は2026-07

### 25. [medium] deployment.md §0.0 のリリースゲート一覧が実装より1段少なく、デプロイ後検証も1本しか書いていない

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `docs/operations/deployment.md:36`
- 根拠: src/lib/ops/release-gate.ts の evaluateReleaseGate は ブランチ／未コミット／未push／CI／反映先URL／**データベースの接続先（judgeLinkedProject・supabase link と反映先アプリのproject refの突き合わせ・T-M7-52）**／migration の7段を返すが、deployment.md §0.0 の番号付き一覧（36〜41行）は6段で「データベースの接続先」が無い。さらに 43 行は「すべて通ると、続けてデプロイ後の検証（`smoke:live --base <URL>`）を実行する」だが、scripts/release.mjs は `[1/2] 人間確認（Turnstile）`（check-turnstile.mjs）→ `[2/2] 実物スモーク` の2本を実行し、両方の結果を出してから判断を求める。
- 直し方: §0.0 の一覧へ「6. データベースの接続先（いま `supabase link` で繋がっているプロジェクトが反映先のものか。違えば止める）」を挿入して migration を7番に繰り下げ、43行のデプロイ後検証を「check:turnstile → smoke:live の2本（片方が失敗しても両方の結果を出す）」に直す。docsのみ。
- 反証を試みた結果: 反証できなかった（主要部分は実在）。src/lib/ops/release-gate.ts の evaluateReleaseGate は steps を7段 push する: ブランチ／未コミットの変更／未pushのコミット／自動テスト（CI）／反映先のURL／**データベースの接続先**（`steps.push(judgeLinkedProject(ctx))`・T-M7-52）／データ構造の更新（migration）。一方 docs/operations/deployment.md の36〜41行は6段しかなく、「反映先のURL」から直接 migration へ飛んでいて「データベースの接続先」が無い。このゲートは死んでおらず現に動いている: scripts/release.mjs:111-142 が linkedProjectRef（supabase/.temp/project-ref）と targetProjectRef（デプロイ先のCSPヘッダ）を組み立てて context に渡している。別の節に正しい記述がある可能性も潰した: `grep -rn "T-M7-52|judgeLinkedProject|データベースの接続先" docs/` は**0件**で、記載は tasks/BACKLOG.md:1698-1704（タスク記録であり正本ではない）だけ。§2 の180行の `npx supabase link --project-ref` は初回構築の手順で、毎回のリリースゲートとは別物。§0.0 は「どれか1つでも欠けていれば、そこで止まって理由と次の一手を出す」と一覧を網羅として提示しており、欠けている1段はまさに「本番DBへ誤ってmigrationを流す」事故を止めるもの（judgeLinkedProject は判定不能でも stop する安全側設計）なので、抽象化による省略とは言えない。提案の挿入位置（6番目・migrationを7番へ）は実装の順序と一致する。ただし後半の指摘は弱い: 43行の「デプロイ後の検証（smoke:live）」は不完全な

### 26. [medium] 通知メールの正本（要件01 §3.6・要件04 §14）に T-M8-136 の差出人名とメールヘッダが書かれていない

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `docs/requirements/01_system_architecture.md:131`
- 根拠: src/lib/email/notification-email.ts:31 `withSenderName()` が `EMAIL_FROM` が素のアドレスのとき `Exos AI <...>` へ整形し（正本は src/lib/ops/auth-url-status.ts:31 の EXPECTED_SENDER_NAME）、同 :48 `notificationEmailHeaders()` が `Auto-Submitted: auto-generated`／`List-Unsubscribe: <APP_BASE_URL/app/settings?tab=notifications>`／`List-Unsubscribe-Post`（RFC 8058）を全通知メールへ付ける。要件01 §3.6 の記述（131行）は Message-ID・retry分類・email_error 形式まで細かく書いているのにこの2点が無く、末尾は「実Gmail送信の確認は App Password 発行後に行う（open_questions）」のまま（本番はカスタムSMTP設定済み＝deployment.md §2.9 207行）。要件04 §14 にも記載が無い。docs全体を grep しても List-Unsubscribe / Auto-Submitted / 差出人名 の記載は deployment.md の表1行だけ。
- 直し方: 要件01 §3.6 の段落へ「差出人は表示名 `Exos AI` を付ける（正本は EXPECTED_SENDER_NAME。env が素のアドレスでも付与）」「自動送信・1クリック購読解除ヘッダを付ける（行き先は通知設定タブ）」を追記し、open_questions の一文を現状（GmailカスタムSMTP稼働）へ更新。要件04 §14 の通知メール節にも同じ2点を1行で反映する。
- 反証を試みた結果: 反証できなかった。T-M8-136 のコミット(21f38ab)は docs/ を1ファイルも触っておらず（変更は scripts/・src/lib/email/・src/lib/ops/・tasks/BACKLOG.md のみ）、実装は本番経路に載っている: notification-email-server.ts:92 が withSenderName(env.EMAIL_FROM, EXPECTED_SENDER_NAME) で差出人へ表示名を付け、notification-email.ts:140 が notificationEmailHeaders(appBaseUrl) で Auto-Submitted / List-Unsubscribe(<…/app/settings?tab=notifications>) / List-Unsubscribe-Post を全通知メールへ付ける。docs/requirements/01_system_architecture.md:131 は Message-ID 形式・retry分類・email_error 書式まで細かく書いているのにこの2点が無く、docs/requirements/04_jobs_and_automation.md §14（260行〜）にも無い。docs/ 全体を grep しても List-Unsubscribe / Auto-Submitted / 購読解除 は0件（該当は tasks/BACKLOG.md:2101-2102 のみ）で、別節の正しい記述も「未実装」注記も存在しない。よって「実装済みだが正本に無い」doc_drift は実在する。ただし指摘の根拠に2点の誇張があり、修正提案時は直すべき: (1) deployment.md:208 の「Sender name は Exos AI」は Supabase Auth のカスタムSMTP設定（確認メール）の話で、アプリ通知メールの From とは別物。また要件01:123 が既に EMAIL_FROM=`Exos AI <…>` を記載

### 27. [medium] 要件01 §4 が signup 確認メールをリンク方式（RedirectTo/TokenHash）と説明したまま

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `docs/requirements/01_system_architecture.md:151`
- 根拠: 151行「Supabaseのconfirmation／recoveryメールテンプレートは `RedirectTo`・`TokenHash` からこのrouteへのリンクを生成し、`token_hash` と `type=signup|recovery` をServer側で `verifyOtp` する。signup確認は `/plans` …」。実装は T-M8-121 で signup を6桁コードへ変更済み（supabase/templates/confirmation.html は `{{ .Token }}` のみ、src/app/actions/auth.ts:167 が EMAIL_CODE_LENGTH で検証、要件05 の72行は「signupは6桁コード方式へ移行・`type=signup` も後方互換で受ける」と書いている）。要件01 は v1.14／更新日 2026-08-18 と新しいのにこの文だけ残り、要件05 と食い違っている。
- 直し方: 151行を「recovery はリンク方式（`{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=recovery`）で `/auth/confirm` を通す。signup 確認は6桁コード方式（`{{ .Token }}`）で `/auth/confirm` を経由しない（`type=signup` は後方互換で受ける）」と直す。docsのみ。
- 反証を試みた結果: 反証できなかった。docs/requirements/01_system_architecture.md:151 は HEAD（afbe0b4、作業ツリーclean）でも「confirmation／recoveryメールテンプレートは RedirectTo・TokenHash からこのrouteへのリンクを生成し」と書いているが、supabase/templates/confirmation.html はリンクを一切含まず {{ .Token }} の6桁コードのみ（supabase/config.toml:252-258 でこのファイルが confirmation テンプレートとして紐付いている）。signup確認は src/app/actions/auth.ts の verifySignUpCode が EMAIL_CODE_LENGTH で桁数を見てから verifyOtp({email, token: code, type:"signup"}) で検証し、/auth/confirm を通らない。リンク方式が正しいのは recovery のみ（recovery.html は {{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=recovery）。他節には正しい記述が既にあり、01 が孤立して食い違っている: 要件03:25 は「確認メールは6桁コード方式…リンク方式をやめた理由」と明記、要件05:72 と :105 も「type=signup は後方互換で受ける」「確認自体はコード方式なのでリンクは使わない」と書いている。src/lib/auth/confirm.ts の CONFIRMATION_TYPES にまだ signup が残るため後方互換は実在するが、01:151 の主張は「confirmationテンプレートがそのリンクを生成する」ことであり、それは現在起きていない。01 は v1.14／2026-08-18（変更と同日）でヘッダからは陳腐化が分からず、「未実装」注記も無い。仕様レベルの抽象ではなく 

### 28. [medium] local-development.md のローカル登録手順が「確認メールのリンクを踏む」のまま

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `docs/operations/local-development.md:235`
- 根拠: 235行「**UIサインアップ**: http://127.0.0.1:3000/signup で登録 → 確認メールを **Mailpit http://127.0.0.1:54324** で開いてリンクを踏む。」。実装は6桁コード入力（src/components/auth/email-code-form.tsx、supabase/templates/confirmation.html は `{{ .Token }}` のみでリンクを含まない）。scripts/doctor.mjs:86 の案内は「6桁コードは http://127.0.0.1:54324 を開いて確認してください」と正しく書かれており、doctor と手順書が食い違っている。
- 直し方: 235行を「…→ Mailpit で届いたメールの6桁コードを登録画面に入力する」に直す。docsのみ。
- 反証を試みた結果: 反証できず、指摘は実在する。(1) docs/operations/local-development.md:235 は現状「確認メールを Mailpit で開いてリンクを踏む」のまま。(2) 実装側の確認メール本文 supabase/templates/confirmation.html は `{{ .Token }}` のみで「登録画面に以下の確認コードを入力してください」と書かれており、リンクを一切含まない。supabase/config.toml:252-254 が同テンプレートをローカルへ適用し、otp_length=6（233行）なので、ローカルのMailpitに届くのも6桁コードのみのメール。よって235行の手順は実行不可能。(3) 画面も6桁コード方式: src/app/signup/signup-form.tsx が src/components/auth/email-code-form.tsx（「メールで届いた6桁コードの入力（T-M8-121）」）を描画し、src/app/actions/auth.ts:38 が「届いた6桁の数字を入力してください」を返し 182行で verifyOtp({email, token, type:"signup"}) を呼ぶ。(4) リンク方式の src/app/auth/confirm/route.ts は残るが token_hash 経由で、リンクを出すのは recovery テンプレート（supabase/templates/recovery.html:2「以下のリンクから…」）のみ＝パスワード再設定用。サインアップはリンクを出さない。(5) 同ドキュメントの他の節（19行・132行）は「Mailpitに届く」だけの中立記述で、6桁コードに触れた正しい記述はどこにも無い。「未実装」注記も無い。(6) scripts/doctor.mjs:127「6桁コードは <Mailpit> を開いて確認してください」・133行「新規登録の6桁コードを読む手段がありません」と食い違うことも確認。git log でも本ドキュメ

### 29. [medium] 手元用の値の案内が auth:templates に追いついていない（SMTP_* が表に無く、SUPABASE_ACCESS_TOKEN は .env.example に無い）

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `docs/operations/deployment.md:56`
- 根拠: deployment.md §0.0 は「**`.env.local` へ置く手元用の値**（`.env.example` にも記載がある）」として STAGING/PRODUCTION_BASE_URL・STAGING/PRODUCTION_CRON_SECRET・SUPABASE_ACCESS_TOKEN・PRODUCTION_STRIPE_* を表にしている（52〜60行）。実際には (1) `.env.example` を grep しても SUPABASE_ACCESS_TOKEN は無い（他の手元用値 STAGING_BASE_URL 等は65〜68行にある）ので「.env.example にも記載がある」が成り立たない。(2) scripts/push-auth-templates.mjs は `--apply` 時に SMTP_HOST/SMTP_PORT/SMTP_USER/SMTP_APP_PASSWORD が .env.local に無いと fail する（「SMTP_* を .env.local へ置いて再実行してください」）が、この表に SMTP_* が無い。要件01 §3.6 でも SMTP_* は「preview/prod のアプリ用」としか書かれていない。
- 直し方: `.env.example` へ `SUPABASE_ACCESS_TOKEN=` の行（用途コメント付き）を追加し、deployment.md §0.0 の表に「`SMTP_*`（`auth:templates -- --apply` が Supabase Auth のカスタムSMTP設定に流用する。アプリの通知メールと同じ値）」の行を足す。要件01 §3.6 の SMTP_* 備考にも「Supabase Auth のカスタムSMTP設定にも同じ値を使う」を1文追記。設定の読み方は変えない。
- 反証を試みた結果: 反証を試みたが中核は実ファイルで確認でき成立する（ただし SMTP_* の半分は誤り寄り）。

【成立: SUPABASE_ACCESS_TOKEN】
- /Users/kotamatsumoto/private-workspace/x-system/docs/operations/deployment.md:56 は「`.env.local` へ置く手元用の値（`.env.example` にも記載がある。2026-08-04 に追記…）」と書いた直下の表で BASE_URL・CRON_SECRET・SUPABASE_ACCESS_TOKEN（58行）・PRODUCTION_STRIPE_* を挙げる。
- /Users/kotamatsumoto/private-workspace/x-system/.env.example を全文確認（82行）。手元用セクション64〜82行に STAGING/PRODUCTION_BASE_URL・同 CRON_SECRET・STAGING/PRODUCTION_STRIPE_* は在るが SUPABASE_ACCESS_TOKEN は無い。`git ls-files | grep env` で env テンプレートは .env.example のみ、`git grep -l SUPABASE_ACCESS_TOKEN` も docs/scripts/src のみでテンプレートに無い。
- 経緯も一致: 括弧書きは 68de94f(2026-08-14)、SUPABASE_ACCESS_TOKEN 行は 2026-08-17（T-M8-90）に deployment.md だけへ追記。よって「.env.example にも記載がある」は表の1行について事実と食い違う。
- 副作用確認: scripts/ci-env.mjs は .env.example を土台にキー列挙するが空値は出力しないため、`SUPABASE_ACCESS_TOKEN=`（空）追加は CI env・env-schema に無影響。

【誤り寄り: SMTP_

### 30. [medium] push-auth-templates の rate_limit_anonymous_users のコメントが別設定の説明になっている（総当たり対策として✅を出す）

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `scripts/push-auth-templates.mjs:134`
- 根拠: 134-135行は「/** 5分あたりの登録・ログイン試行（IPごと）。 */ rate_limit_anonymous_users: 30」。supabase/config.toml のコメントでは `anonymous_users` は「匿名サインインの1時間あたり回数。**enable_anonymous_sign_ins = true が前提**」で、同ファイルの `enable_anonymous_sign_ins = false`。「5分あたりの登録・ログイン試行（IPごと）」は config.toml の `sign_in_sign_ups = 30` の説明であり、こちらはリモートへ同期していない。T-M8-124「総当たり対策を入れる」の一部としてこの行が追加され、実行時に `✅ rate_limit_anonymous_users: 30` と表示されるため、運営者・開発者は登録/ログイン試行の上限が揃ったと読む。
- 直し方: コメントを実際の意味（匿名サインインの上限。匿名サインインは無効なので実質no-op）へ直し、「登録・ログイン試行の上限（config.toml の sign_in_sign_ups）はこのコマンドで同期していない」ことを明記する。押す値は変えない（挙動不変）。同期させるかどうかは Management API に対応キーがあるかの確認が必要なので別タスク・要決定として起票する。
- 反証を試みた結果: 反証できず、指摘は実在する（むしろ指摘より一段強い）。

【確認した実ファイル】
- scripts/push-auth-templates.mjs:134-135 = `/** 5分あたりの登録・ログイン試行（IPごと）。 */ rate_limit_anonymous_users: 30`。この値は221-230行のループで `✅ rate_limit_anonymous_users: 30` と表示され、295-299行の反映後確認でも同じ表示になる（運営者は「登録/ログイン試行の上限が揃った」と読む）。
- supabase/config.toml:177 `enable_anonymous_sign_ins = false`、203-204「Number of anonymous sign-ins that can be made per hour per IP address. Requires enable_anonymous_sign_ins = true.」＋`anonymous_users = 30`、207-208「Number of sign up and sign-in requests that can be made in a 5 minute interval per IP address (excludes anonymous users).」＋`sign_in_sign_ups = 30`。同一リポジトリ内の記述だけで既に食い違っている。

【上流（Supabase）で裏取り】
- supabase/auth `internal/api/apilimiter/apilimiter.go`: `AnonymousSignIns = tollbooth.NewLimiter(gc.RateLimitAnonymousUsers/(60*60), …)` → 1時間あたり（5分ではない）。同ファイルの対応表で `fieldAnonymousSignIns ← GOTRUE_RATE_LIMIT_ANONYMOUS_USERS`、`field

### 31. [medium] 要件06 §4.2 が「p2のみ 自分の考え」「asks_user_opinion で出し分ける」と書いているが、実装はプレースホルダーへ一般化済み

- 種別: ドキュメント乖離 ／ 領域: 投稿パターン
- 場所: `docs/requirements/06_screens_onboarding_posting.md:301`
- 根拠: 要件06 §4.2 の 299行は入力の並びを「テーマ → パターン → プロンプト → 参考URL →（p2のみ 自分の考え）→ 追加指示 → 画像生成」と書き、301行は「**意見の入力欄はパターンの`asks_user_opinion`、引用対象URLは`requires_quote_url`で出し分ける**」と書く。実装（create-post-form.tsx:712-733）は `selectedPattern.placeholders` を map して入力欄を出すだけで、asks_user_opinion はどこからも読まれない。要件02 §3.21（590行）は同じ列を「**使っていない**（T-M8-133でプレースホルダーへ一般化した）」と書いており、docs 内でも矛盾している。移行の経緯は migration 20260818000007/000008 に記録されている。
- 直し方: §4.2 の並びを実装どおり「テーマ → パターン → プロンプト → 参考URL → プレースホルダー（そのパターンが持つ項目）→ 追加指示 → 画像生成 → 生成ボタン」に直し、301行を「毎回入力する項目はパターンの `placeholders` から出す（既定の『自分の考え・意見』は `自分の考え` を持つ）。引用対象URLは `requires_quote_url`」へ書き換える。あわせて label 表記も実装の「参考URL（任意）」に合わせる。
- 反証を試みた結果: 指摘は実在する。docs/requirements/06_screens_onboarding_posting.md:299 は入力の並びに「（p2のみ 自分の考え）」を含み、:301 は「意見の入力欄はパターンの asks_user_opinion で出し分ける」と書くが、実装 src/app/app/posts/create-post-form.tsx:712-733 は selectedPattern.placeholders を map して汎用の入力欄を出すだけで、asks_user_opinion / asksUserOpinion は src/app・src/components から一度も読まれていない（grep 0件。定義と select/parse のみが src/lib/post/pattern-spec.ts:30,106 と src/lib/post/post-patterns-store.ts:41,59,69,81,417 に残る）。user_opinion の生成経路も T-M8-133（8df4393）で撤去済みで、src/lib/jobs・src/lib/ai・src/lib/prompts・src/app に存在しない。docs/requirements/02_data_model.md:590 は同じ列を「使っていない（T-M8-133でプレースホルダーへ一般化した）」と明記しており、docs 内でも矛盾している。§4.2 内にプレースホルダーへの言及は1つも無く（プレースホルダーの記述は L221-223 の SC-08 と L252/254 のパターン管理のみ）、「未実装」注記も無いため「別節に正しい記述がある」「仕様レベルの抽象で書いただけ」では説明できない。原因も特定でき、8df4393 の 要件06 への差分はバージョンヘッダ・L33・L254 のみで §4.2 は未更新のまま T-M8-129 当時の記述が残った。提案の並び（テーマ→パターン→プロンプト→参考URL→プレースホルダー→追加指示→画像生成→生成ボタン）は

### 32. [medium] 要件06 §4.3 が撤去済みの enum キー定数（P-1=6 …）でポスト数上限を規定している

- 種別: ドキュメント乖離 ／ 領域: 投稿パターン
- 場所: `docs/requirements/06_screens_onboarding_posting.md:312`
- 根拠: §4.3 の 312行「編集後は1件以上かつP-1=6、P-2=1、P-3=7、P-4=5、P-5=3、P-6=7のpattern別最大数以内」、317行「生成時はより短い上限（P-1=4／P-2=1／P-3=6／P-4=2／P-5=3／P-6=5）」は、いずれも enum ID をキーにした固定表を前提にしている。実装は下書きごとの snapshot（drafts.ts:175 と draft-editor.tsx:37 が `draft.max_posts_edit` を、post-generation.ts:553 が `spec.maxPostsEdit`、capPostCount が `spec.maxPosts` を使う）へ移っており、利用者が作ったパターンには P-n が無い。要件02 §3.9（275行）の「書き込み時はpattern別最大数も検証」も同じ古い語彙。
- 直し方: §4.3 を「編集上限は下書きに写した `drafts.max_posts_edit`、生成上限はジョブに凍結した `pattern_spec.max_posts`」という書き方へ直し、既定6件の実数値は要件02 §3.21／要件06 §4.1 の表1か所だけに置く（2か所に数字を書くと片方だけ古くなる）。§3.9 の文言も `max_posts_edit` へ差し替える。
- 反証を試みた結果: 指摘は実在する。実ファイルで確認した内容:

【docs側（現物）】
- docs/requirements/06_screens_onboarding_posting.md:312「編集後は1件以上かつP-1=6、P-2=1、P-3=7、P-4=5、P-5=3、P-6=7のpattern別最大数以内に収める」
- 同:317「編集の上限（上記のpattern別最大数）とは別に、生成時はより短い上限（P-1=4／P-2=1／P-3=6／P-4=2／P-5=3／P-6=5）」
- docs/requirements/02_data_model.md:275「書き込み時はpattern別最大数も検証」
いずれも「パターンIDをキーにした固定表が検証の規則」という書き方で、既定値であることもsnapshotであることも書いていない。

【実装側】
- 編集上限: src/lib/drafts.ts:175 `const max = draft.max_posts_edit;`（コメント「編集で許すポスト数は生成時に写した値を使う（T-M8-129 U3）」）、UIも src/app/app/posts/draft-editor.tsx:37 で同じ値。
- 生成上限: src/lib/jobs/post-generation.ts:553 が `spec.maxPosts` / `Math.max(spec.maxPostsEdit, finalize.thread.length)` を drafts へ insert。切り詰めは src/lib/post/thread-limits.ts:46 `capPostCount(maxPosts, posts)` で、コメントに「以前はパターンID（p1）で引いていたが、利用者が作ったパターンはIDを持たないため値で受ける（T-M8-129 U2）」と明記。
- 旧enumは撤去済み: supabase/migrations/20260818000005_drop_post_pattern_enum.sql が `post_pat

### 33. [medium] 要件06 §1.1 が /plans をまだ「3枚のカード横並び＋ご登録の流れ4ステップ＋初期設定手順」と書いており、同じ節の後段（比較表）と矛盾している

- 種別: ドキュメント乖離 ／ 領域: 画面・UI共通部品
- 場所: `docs/requirements/06_screens_onboarding_posting.md:44`
- 根拠: §1.1 の line 44 は「3プランを**3枚のカードで横並びに**比較表示し」、line 45 は「カードの特長に書く件数・月間上限は PLANS から導出」、line 46 は「カードの下に『ご登録の流れ』（4ステップ）と、BYOK／プレミアムの初期設定手順を並べる」、line 55 は「狭い幅ではカードを縦に積み」と書いている。実装 src/app/plans/page.tsx にはプランカードが無く、line 132 で `<PlanComparisonTable />`（LPと共通・src/components/billing/plan-comparison-table.tsx）を1つ描き、その下は line 135-138 の CheckoutButton 3つと line 141-152 の BYOK注意ブロックだけ。`grep -n "ご登録の流れ|ステップ|初期設定" src/app/plans/page.tsx` は0件。同じ節の line 47 と line 49 は既に「プラン比較は機能を行見出しにした表で示す（T-M8-125）」「定義リストと初期設定の手順一覧は比較表と重複していたため削除した」と書いており、節の中で前段と後段が食い違っている。
- 直し方: §1.1 の line 44-46・55 を比較表ベースの記述へ書き換える（カード前提の文を削り、横スクロールは表側が持つこと＝line 47 の記述へ統合する）。CTAが3プラン分並ぶこと、BYOK注意ブロックが表の下に来ることを1行で書く。実装は変えない。
- 反証を試みた結果: 反証できなかった。指摘は実在する。(1) docs/requirements/06_screens_onboarding_posting.md:44-46・55 は現在も「3枚のカードで横並び」「カード直下」「カードの特長」「カードの下に『ご登録の流れ』（4ステップ）と初期設定手順を並べる」「狭い幅ではカードを縦に積み」と書いている。(2) 実装 src/app/plans/page.tsx にプランカードは無く、line 132 の <PlanComparisonTable /> 1つ＋line 133-137 の CheckoutButton 3つ（sm:grid-cols-3）＋line 141-151 の BYOK注意 section だけ。grep -nE "ご登録の流れ|ステップ|初期設定" src/app/plans/page.tsx は0件。(3) 「ご登録の流れ」は docs 1箇所のみに残存（grep -rn で doc line 46 だけ）。git log -S"ご登録の流れ" -- src/ より b4f6a2a(T-M8-21) で追加、fb46a4a(T-M8-125) で削除済み。同コミットのメッセージ自身が「削ったもの: 申込前確認の定義リスト（6項目）・初期設定（BYOK）・初期設定（プレミアム）」と書いている。(4) fb46a4a の doc diff は line 47-49 を「+」で挿入し 44-46 を無変更の context として残しており、同期漏れが機械的に確認できる。結果、line 46（初期設定手順を並べる）と line 49（初期設定の手順一覧は削除した）が同じ節で正面から矛盾する。(5) line 55 も実装と食い違う——plan-comparison-table.tsx:45-46 は relative overflow-x-auto ＋ min-w-[560px] で、狭い幅では表が自分の中で横スクロールし、カードを縦に積む挙動は存在しない（line 47 の記述が正しい）。(6) 書き換えは安全: l

### 34. [medium] 要件06 が廃止済みの support / api-keys / x-accounts / notifications タブを現行タブとして記述し続けている

- 種別: ドキュメント乖離 ／ 領域: 画面・UI共通部品
- 場所: `docs/requirements/06_screens_onboarding_posting.md:64`
- 根拠: line 25 は「問い合わせタブは廃止（T-M8-104）。旧タブslug（x-accounts等・supportはbillingへ）は normalizeSettingsTab のエイリアスで受ける（DB保存済み通知リンク互換）」と正しく記録している。しかし line 64 は「`/app/settings?tab=support` は SUPPORT_EMAIL への mailto: リンクを表示する。未契約ユーザーも課金・問い合わせの2タブへ到達できる」、§9 の line 381 は「課金は billing、問い合わせは support を canonical slug とする」と、support をまだ生きたタブとして書いている。実装 src/app/app/settings/tabs.ts:7-13 の SETTINGS_TABS は general/billing/account/purposes/prompts の5つで support は無く、同 :30 で `support: "billing"` のエイリアス扱い。§1.2.1（line 68-79）と §1.2.2（line 82-85）も `?tab=api-keys` / `?tab=x-accounts` / `?tab=notifications` を現行のタブURLとして記述しているが、これらも同 :24-26 でエイリアス→general。実装側もエイリアスに依存しており、コード内リンクは api-keys 35箇所・x-accounts 26箇所・notifications 6箇所・support 1箇所（`grep -rno "/app/settings?tab=[a-z0-9-]*" src e2e scripts`）で、canonical の general は2箇所しかない。src/app/app/tabs.test.ts:60 の許容リストは ACCEPTED_SETTINGS_TAB_SLUGS（エイリアス込み）なので、この状態は検出されない。
- 直し方: §1.2 の見出しと line 64、§9 の line 381 を「問い合わせは廃止し、SUPPORT_EMAIL への mailto は billing タブ内に置く／route guard は billing と旧 support slug を通す（src/lib/auth/route-guard.ts:21-28 の実装どおり）」へ書き直す。§1.2.1・§1.2.2 の見出しと本文は「設定タブ（general）内のXアカウント欄／APIキー欄／通知欄」という表現へ改める。あわせて、docs が「エイリアスは DB保存済みリンク互換のため」と書いている以上、コード内リンクを canonical へ揃える方針かどうかを1行明記する（揃えるなら tabs.test.ts の許容リストを canonical のみへ絞り、エイリアスは normalizeSettingsTab の単体テストで担保する形が取れる）。
- 反証を試みた結果: 反証できなかった（指摘の中核は実在する）。確認内容:

1. 実装（`src/app/app/settings/tabs.ts:7-13`）の `SETTINGS_TABS` は general/billing/account/purposes/prompts の5つで `support` は無く、同 :29 で `support: "billing"` の**エイリアス**。`src/app/app/settings/page.tsx:151` は `normalizeSettingsTab(params.tab)` を通すので `?tab=support` は billing タブへ落ちる。
2. その billing タブ（page.tsx:336-410）に `SUPPORT_EMAIL` の `mailto:` は**存在しない**。`grep -rn "SUPPORT_EMAIL|mailto" src` の結果、アプリ内の mailto は `src/app/plans/checkout-pending.tsx:69`（決済保留画面）と法務ページ（`LEGAL_ENTITY.email`：terms/privacy/commercial-transactions）だけで、`/app/settings` 配下には1件も無い。したがって docs 06 line 64「`/app/settings?tab=support`は`SUPPORT_EMAIL`への`mailto:`リンクを表示する」は**実装と食い違う**（URL自体は200で billing が開くため、URLを叩く確認では見えない型）。同じ行の「未契約ユーザーも課金・問い合わせの**2タブ**へ到達できる」も、到達先は billing 1タブのみで誤り。§1.2 見出し「SC-11 課金・問い合わせ（M1最小実装）」も廃止済みタブを現行として掲げている。
3. §9 line 381「課金は`billing`、**問い合わせは`support`をcanonical slugとする**」は、tab

### 35. [medium] 要件06 の変更履歴が表ヘッダを失った孤立1行になっており、冒頭ヘッダ v1.92 に対し履歴は v1.84 で8版分が欠けている

- 種別: ドキュメント乖離 ／ 領域: 画面・UI共通部品
- 場所: `docs/requirements/06_screens_onboarding_posting.md:446`
- 根拠: ファイル末尾は line 445 が本文の箇条書き、line 446 が `| v1.84 | 2026-08-18 | SC-02を6桁コード入力へ（T-M8-121）… |` の1行だけ。`grep -n "^## " 06_screens_onboarding_posting.md` の最後は `## 11. 公開法務ページ`（line 408）で、変更履歴の見出しも表ヘッダ（`| version | 日付 | 変更内容 |` と区切り行）も存在しないため、この行はMarkdownとして表にならない。冒頭 line 5 は「| バージョン | v1.92 |」で、履歴の最終行 v1.84 との差は8版。git で追うと 9164c9c（docs: 直近の改修とドキュメントの食い違いを解消する）がこの1行だけをヘッダ無しで追加しており、以後 v1.85〜v1.92 の間は履歴が追記されていない（各コミットの当該ファイルの `^| v1\.` 行数は一貫して1）。docs/README.md の更新ルールは「文書内versionと変更履歴を正とし…仕様変更時は冒頭の更新日と末尾の変更履歴を更新する」。T-M8-137 で入った `npm run check:doc-dates` は更新日だけを見るため、この欠落は検出されない。同型の欠落は 05_api_server_actions.md にもある（冒頭 v1.38 に対し末尾行は v1.35）。
- 直し方: 06 の末尾に `## 12. 変更履歴` と表ヘッダを復元し、v1.85〜v1.92 に相当する変更（T-M8-125 比較表、T-M8-129〜135 のパターンマスタ・スレッド数・プレースホルダー・削除導線・再同意バナー・予約の生成入力など、git log で特定できる範囲）を追記する。05 も同様に揃える。さらに check:doc-dates と同じ仕組みで「冒頭ヘッダの version が末尾変更履歴の最新行と一致すること」「変更履歴の表ヘッダが存在すること」を検査に足す（意図的にヘッダを消して落ちることを確認する）。ドキュメント側のみの変更。
- 反証を試みた結果: 反証できなかった。実ファイルとgit履歴で確認した事実は指摘どおり。(1) docs/requirements/06_screens_onboarding_posting.md は446行で、445行が本文の箇条書き（304文字）、446行が `| v1.84 | 2026-08-18 | SC-02を6桁コード入力へ（T-M8-121）… |` の1行のみ。間に空行なし、`変更履歴` の見出しなし（`^## ` の最後は408行 `## 11. 公開法務ページ`）、表ヘッダも区切り行もなし → Markdownでは表にならず、直前の箇条書きへ地の文として連結される。(2) 5行目は `| バージョン | v1.92 |` で履歴行は v1.84、差は8版。git log を辿ると v1.84→v1.92 の各コミットで当該ファイルの `^| v1\.` 行数は一貫して1のまま。(3) `git show 9164c9c~1` では 03/05/06 の `^| v1\.` は0件、9164c9c 後に1件 → この1行は「ヘッダを失った」のではなく、履歴節が存在しない文書へ孤立行として追加されたもの（指摘の「失った」という因果の記述だけは不正確だが、欠陥自体は実在）。(4) 同型は 05_api_server_actions.md:305（冒頭 v1.38 / 行 v1.35）にもあり、03_auth_billing_usage.md:295 も同じ孤立行形式（版は一致）。(5) docs/README.md:39 に「仕様変更時は冒頭の更新日と末尾の変更履歴を更新する」と明記あり。(6) scripts/check-doc-dates.mjs は `| 更新日 | … |` と `最終更新:` の2つの正規表現のみで、versionや変更履歴を一切見ない。`grep -rn 変更履歴 scripts/ src/` にも検査は存在しない → 検出されないのは事実。なお修正時の注意として、requirements/01・02・04 は変更履歴節を持たず（v1.14/v1

### 36. [medium] 本番から呼ばれていない `requireExecutionAccess` が残り、3つのコメントが「再同意の裏付け」としてそれを指している

- 種別: リファクタ ／ 領域: 認証・課金・利用量
- 場所: `src/lib/auth/execution-guard.ts:16`
- 根拠: `requireExecutionAccess` の呼び出し元は execution-guard.test.ts だけ（`grep -rn requireExecutionAccess src` → 定義・テスト・コメントのみ）。実際に配線されているのは `requireLegalConsent`（legal-consent-server.ts:27）＋ `requireExecutableSubscription` で、要件06 §1.3（91行）も「この誘導はServer Action共通の`requireExecutionUserId()`（`requireLegalConsent`）が担い…以前は`requireExecutionAccess`が実装されていながらどこからも呼ばれず」と決着を書いている。にもかかわらず (1) src/lib/legal.ts:5-6「値が変わると `requireExecutionAccess` が生成・投稿・自動実行の前に再同意を要求する」 (2) src/app/terms/page.tsx:19「『再同意を求めます』は `requireExecutionAccess` の配線（T-M8-73）が裏付け」 (3) src/lib/execution-prereqs.ts:8「法務同意は `requireExecutionAccess` が別途担う」 が死んだ関数を機構として指しており、判定式（`termsVersion !== CURRENT_TERMS_VERSION`）も `requiredLegalConsents` と二重に書かれている（settingsPath "/app/consent" も `LEGAL_CONSENT_PATH` と二重）。
- 直し方: `requireExecutionAccess` / `ExecutionGuardProfile` と execution-guard.test.ts を削除し、上記3つのコメントの参照を `requireExecutionUserId`（`requireLegalConsent`）へ差し替える。残す判断をした場合は最低限 `requiredLegalConsents` と `LEGAL_CONSENT_PATH` を使う形へ寄せて二重定義を消す。いずれも呼び出し元ゼロなので外部の振る舞いは変わらない。
- 反証を試みた結果: 反証できず、指摘は実在する（ただし根拠に1点の不正確さあり）。

確認した事実（すべて実ファイル読み・grep）:
1. `/Users/kotamatsumoto/private-workspace/x-system/src/lib/auth/execution-guard.ts:16` の `requireExecutionAccess` と `ExecutionGuardProfile` は、リポジトリ全体で本番からの参照が0。`grep -rn "execution-guard"`（全拡張子・node_modules除外）のヒットは `src/lib/auth/execution-guard.test.ts:9` と `src/lib/auth/legal-consent.test.ts:11` の2行だけで、モジュール自体を import する本番コードが存在しない。
2. 実際に配線されているのは `src/app/actions/_helpers.ts:84` の `requireExecutionUserId()` → `src/lib/auth/legal-consent-server.ts:27` の `requireLegalConsent(userId)`。呼び出し元は `src/app/actions/generation-jobs.ts`（6箇所）と `src/app/actions/schedule.ts`（4箇所）の計10本で、要件06 §1.3（91行）の記述と一致する。docs 側は既に「以前は`requireExecutionAccess`が実装されていながらどこからも呼ばれず」と決着済み。
3. 指摘された3つのコメントは実在し、いずれも死んだ関数を機構として指している: `src/lib/legal.ts:5-6`／`src/app/terms/page.tsx:19`／`src/lib/execution-prereqs.ts:8`。特に terms/page.tsx は「実装と食い違う記載を置かない」と宣言した直後の行で、規

### 37. [medium] 「md/premium だけ編集できる」の判定が5箇所に分散し、表示用（PLANS）と拒否用（promptEditablePlan）が別系統

- 種別: リファクタ ／ 領域: 認証・課金・利用量
- 場所: `src/lib/prompts/prompt-templates.ts:117`
- 根拠: prompt-templates.ts:110-118 は「**判定はここだけに置く。** 保存時の拒否と実行時の判定が別々の条件を持つと『画面には出ないのに生成では効く』状態が生まれる」と書いた上で `plan === "md" || plan === "premium"` を持つ。しかし同じ判断が (1) src/lib/plans.ts:41,53,63,77 の `canEditMdAndPrompts`（コメント「md/premium can edit base_md and prompt templates」。使い手は plan-comparison.ts:58,62 の比較表＝利用者に見せる可否表だけ） (2) src/lib/base-md.ts:62-67 `assertEditablePlan`（`plan !== "md" && plan !== "premium"`） (3) src/app/app/posts/page.tsx:103 (4) src/app/app/schedule/page.tsx:95 のインライン条件、にも独立して書かれている。つまり比較表が「✓」と言う根拠と、サーバーが拒否する根拠が別の場所にあり、PLANS だけを直すと表とサーバーが食い違う（このリポジトリで名指しされている「同じ判断を経路ごとに別々に書いた重複」型）。
- 直し方: `promptEditablePlan(plan)` の実装を `PLANS[plan as PlanId]?.canEditMdAndPrompts === true`（未知planはfalse）へ変え、base-md.ts の `assertEditablePlan` と posts/schedule ページのインライン条件をこの関数呼び出しに置き換える。現在の3プランでは真理値が完全に一致するため振る舞いは変わらない。`canEditMdAndPrompts` を1プランだけ反転させたユニットテストを足して、5箇所が同時に追随することを確認する。
- 反証を試みた結果: 反証できなかった。指摘は実在し、範囲はむしろ報告より広い（5箇所ではなく7箇所）。

【確認した実体】同一の「md/premium だけ編集できる」判断が3つの構文形で独立に書かれている: (1) src/lib/plans.ts:41,53,63,77 `canEditMdAndPrompts`（消費者は src/lib/plan-comparison.ts:58,62 の利用者向け比較表と src/lib/constants.test.ts:61-63 のみ） (2) src/lib/prompts/prompt-templates.ts:117 `plan === "md" || plan === "premium"` (3) src/lib/base-md.ts:64 `plan !== "md" && plan !== "premium"` (4) src/app/app/posts/page.tsx:103 (5) src/app/app/schedule/page.tsx:95 のインライン条件、さらに報告に無い (6) src/app/app/settings/page.tsx:230,244 と (7) 同:443 の `plan !== "standard"` / `plan === "standard"`（同じ境界の否定形。プランが1つ増えた時点で他6箇所と食い違う）。

【反証観点1: 判断が違うので統合すべきでないか】違わない。docs は base_md 編集・prompt_templates 編集・パターンCRUD をすべて同一の「md/premium」境界として書いている（docs/requirements/05_api_server_actions.md:217-227,247、02_data_model.md:180,854、06_screens_onboarding_posting.md:222,300,386）。比較表も plan-comparison.ts:56-63 で「アカウント.md・プロンプトの直接編集」「編集履歴とロー

### 38. [medium] AIクレジットの列指定が reserve は対応表経由・settle/refund は `ai_credits_used` 直書きで不一致（枠を1本化した残骸）

- 種別: リファクタ ／ 領域: 認証・課金・利用量
- 場所: `src/lib/usage/generation-reserve.ts:18`
- 根拠: T-M8-109 で枠が1本になった結果、generation-reserve.ts:18-21 の `COUNTER_COLUMN` は generation/image ともに `"ai_credits_used"`、:22-25 の `COUNTER_TYPE` はともに `"ai_credit"`、reserve-if-premium.ts:26-29 の `RESERVE_LIMIT_BY_TYPE` はともに `PLANS.premium.usageLimits?.aiCredits` と、全て同一値の対応表になっている。`reserveUsage` は `COUNTER_COLUMN[params.type]` をSQLへ補間する（:53）のに、`settleUsage`（:149,151）と `refundUsage`（:191）は `ai_credits_used` を直書きしており、種別追加時に reserve だけが新しい列へ行き settle/refund が別の列を書き換える形になっている。加えて `refundUsage` は `returning ... counter_type`（:184）で取得した `counter_type` を一度も使っていない（型にも残っている）。
- 直し方: 列名の出典を1つにする: `COUNTER_COLUMN` を残すなら `settleUsage`/`refundUsage` も同じ対応表から引く（`COUNTER_COLUMN[params.type]` / 元reserve行の `counter_type` から解決）。1本化を確定させるなら3関数とも `ai_credits_used` 直書きに揃え、対応表は「冪等キーとoperationの区別だけを持つ」ものへ縮める。同時に `refundUsage` の未使用 `counter_type` を returning と型から外す。現在の値では発行SQLが同一なので振る舞いは変わらない。
- 反証を試みた結果: 反証できなかった。実ファイルで全要素を確認した。(1) generation-reserve.ts:18-21 の COUNTER_COLUMN は generation/image ともに "ai_credits_used"、:22-25 の COUNTER_TYPE はともに "ai_credit"、reserve-if-premium.ts:26-29 の RESERVE_LIMIT_BY_TYPE はともに PLANS.premium.usageLimits?.aiCredits で、3つとも全要素同一値の対応表。(2) reserveUsage は COUNTER_COLUMN[params.type]（:53）を :64/:86/:88 のSQLへ補間し counter_type も COUNTER_TYPE[params.type]（:83）をパラメータで渡すのに、settleUsage は :130 で 'ai_credit' リテラル・:149/:151 で ai_credits_used 直書き、refundUsage も :191 で直書き。列とcounter_typeの出典が2系統に分かれているのは事実。(3) refundUsage は :175 の行型と :184 の returning で counter_type を取得しているが :187-194 では user_id/month/delta のみ使用。呼び出し側（jobs/terminal.ts、generation-reserve.db.test.ts、stale.db.test.ts）も戻り値は boolean しか見ておらず、テストも counter_type に依存していないため純粋な未使用。(4) 「将来の拡張点として意図的に残した対応表」という反証は成立しない: migration 20260816000001_ai_credits.sql が generations_count/images_count を drop して ai_credits_used 1本にしており、

### 39. [medium] 確認メールの行き先URLの組み立てが Server Action と doctor で二重定義（コメント自身が「同じ組み立てにすること」と人の記憶に頼っている）

- 種別: リファクタ ／ 領域: 認証・課金・利用量
- 場所: `src/app/actions/auth.ts:68`
- 根拠: src/app/actions/auth.ts:68-70 `confirmationRedirectUrl()` が `new URL("/auth/confirm", env.APP_BASE_URL).toString()` を組み立て、signUp/resend/resetPasswordForEmail の3箇所へ渡す。src/lib/ops/auth-url-status.ts:34-40 に同じ式の `confirmRedirectUrl(appBaseUrl)` があり、コメントは「アプリが渡す確認メールの行き先。**`app/actions/auth.ts` と同じ組み立てにすること。** ここがずれると、実際に渡す値ではないURLを検査してしまう」。つまり doctor の Redirect URLs 検査（T-M8-90、本番で確認メールがlocalhostを指した事故の再発防止）の正しさが、2つの式を人間が一致させ続けることに依存している。auth-url-status.ts は import を型1つしか持たない設計（doctorから直接読める）ため、Action側から import しても循環や server-only の問題は生じない。
- 直し方: auth.ts のローカル関数を廃止し、`confirmRedirectUrl(env.APP_BASE_URL as string)` を `@/lib/ops/auth-url-status` から import して使う。生成されるURL文字列は同一なので振る舞いは変わらず、doctorの検査対象とアプリの実値が構造的に一致する。auth-url-status.ts のコメントは「アプリもこの関数を使う」に更新する。
- 反証を試みた結果: 反証できなかった。実在する重複であり、統合しても振る舞いは変わらない。確認事項: (1) src/app/actions/auth.ts:68-70 のローカル関数 confirmationRedirectUrl() は `new URL("/auth/confirm", env.APP_BASE_URL).toString()` で、102行(signUp)・236行(resend)・276行(resetPasswordForEmail)の3箇所が使う。(2) src/lib/ops/auth-url-status.ts:39-41 の confirmRedirectUrl() は同一の式で、コメント36-38行が明示的に「app/actions/auth.ts と同じ組み立てにすること。ここがずれると、実際に渡す値ではないURLを検査してしまう」と人間の記憶に依存している。judgeAuthUrls(130行)はこれで検査対象URLを作るため、T-M8-90（本番の確認メールがlocalhostを指した事故）の再発防止検査の正しさが手動同期に依存する。(3) 機械的な保証は無い: confirmRedirectUrl をテストで参照するのは src/lib/ops/auth-url-status.test.ts のみ。src/app/actions/auth.test.ts:516,540 はリテラル "http://localhost:3000/auth/confirm" をハードコードしており（3つ目の手書きコピー）、どちらかがずれてもテストは落ちない。(4) 「似ているが判断が違う」ではない。両者はともに「アプリがSupabaseへ渡す確認メールの行き先」を表し、正当に食い違ってよい場合が無い（doctorの検査目的自体がアプリの実値の検査）。(5) 統合は安全かつ前例あり: auth-url-status.ts の import は型のみ（./check も import ゼロ）なので scripts/doctor.mjs:19-21 の直接 import 

### 40. [medium] suggestion の枠返還が到達しない死んだ分岐（正本は「消費しない」）

- 種別: リファクタ ／ 領域: jobs・cron・スケジュール
- 場所: `src/lib/jobs/terminal.ts:239`
- 根拠: RESERVE_TYPE_BY_KIND(:159-165) が `suggestion: "generation"` を持ち、finalizeFailedJob(:239-242) と worker.failJob(:195,:212) が suggestion 失敗時に refundUsage を呼ぶ。しかし SUGGEST は 2026-08-15（T-M8-94）以降 reserve しない: `grep -n 'reserve|settle|credit' src/lib/jobs/suggestion.ts src/lib/jobs/suggestion-server.ts` は0件、reserveIfPremium の呼び出し元も post-generation / image-generation / learning-analysis / md-merge-server の4つだけ。refundUsage は元reserve行が無ければ no-op なので、この分岐は常に何もしない。要件04 §12 も『生成枠は消費しない（premium含む）』と書いており、コード側のコメント（terminal.ts:240『改善提案もLLM実行時に生成枠を1消費するため返還する』）だけが古い前提を語っている。
- 直し方: RESERVE_TYPE_BY_KIND から suggestion を外し、finalizeFailedJob の case suggestion を（返還なしの通知だけの経路として）default 相当へ落とす。reserve行が無い以上 usage_events / usage_counters への影響はゼロで外部の振る舞いは変わらない。過去データに古い reserve 行が残っている可能性を残したいなら、コードは維持してコメントを『歴史的な reserve 行のための保険』へ直し、要件04 §4 の reserve 対象列挙からSUGGESTを外す（どちらでも実装の意味と記述が一致する）。
- 反証を試みた結果: 反証できなかった。指摘は実ファイルで裏取りできる。(1) terminal.ts:164 に `suggestion: "generation"` があり、:239-241 が `case "suggestion": await refundUsage(c, jobId, "generation")`、:240 のコメントが「改善提案もLLM実行時に生成枠を1消費するため返還する」と古い前提を述べている。(2) reserve行を作れるのは generation-reserve.ts:36 の reserveUsage（reason='reserve' の唯一の書き手 :81）だけで、その唯一の呼び出し元 reserve-if-premium.ts:95 reserveIfPremium の呼び出し元は post-generation.ts:300 / image-generation.ts:267 / learning-analysis.ts:223 / md-merge-server.ts:48 の4つのみ。suggestion 系（suggestion.ts / suggestion-server.ts / suggestion-jobs.ts / suggestion-input.ts）に reserve/settle/credit の呼び出しは1件も無く、suggestion.ts:30-31 が明示的に「自動実行のため premium の生成枠は消費しない」と書いている。(3) refundUsage（generation-reserve.ts:168-194）は元reserve行が無いと insert...select が0行で return false し usage_counters に触れない。よって terminal.ts の suggestion 分岐と worker.ts:212 は現状の実装では常に no-op で、usage_events / usage_counters への影響はゼロ（失敗通知は switch の外なので分岐を落としても

### 41. [medium] 子jobの決定的keyが3箇所・2形式に分かれ、keys.ts の childJobKey は本番から使われていない

- 種別: リファクタ ／ 領域: jobs・cron・スケジュール
- 場所: `src/lib/jobs/keys.ts:8`
- 根拠: 要件04 §3/§9 は子jobのkeyを `parent:{parent_job_id}:{kind}:{draft_id}` と定める。keys.ts:8 の childJobKey がその正本だが、`grep -rn 'childJobKey' src` の結果 import しているのは keys.test.ts だけ。実際の生成箇所は post-generation.ts:212 でテンプレートリテラルの手書き（`parent:${parentJobId}:image_generation:${draftId}`）、terminal.ts:126 は別形式の `job:${draftId}:post_publish:auto`。つまり規約が3箇所に散り、keys.test.ts:7-13 は本番で使われない関数の出力を固定しているだけなので、本番のkey形式を壊してもこのテストは緑のまま通る。
- 直し方: post-generation.ts:212 を `childJobKey(parentJobId, "image_generation", draftId)` へ置き換える（生成される文字列は完全に同一）。terminal.ts の auto投稿子jobのkeyは形式が違うので、同じ文字列を返す名前付きヘルパ（例 autoPublishKey(draftId) = `job:{draftId}:post_publish:auto`）として keys.ts へ移し、なぜ parent 形式でないか（親がimage jobでもdraft単位で1件に抑えるため）をコメントに残す。key文字列を変えないため既存の冪等性・DB上の行に影響しない。
- 反証を試みた結果: 実ファイルで確認でき、指摘の中核は実在する。(1) keys.ts:8 の childJobKey は docstring で「子jobキーの正本」を宣言しているが、リポジトリ全体（*.ts/tsx/js/mjs/md、node_modules除く）を grep しても本番の呼び出し元は0件で、import しているのは keys.test.ts のみ（他は tasks/BACKLOG.md:3198 の記述だけ）。(2) 本番の唯一の生成箇所は post-generation.ts:213 の手書きリテラル `parent:${params.parentJobId}:image_generation:${params.draftId}` で、同じ形式が line 181 のdocstringにも散らばり計3箇所。(3) terminal.ts:118-127 は `job:${draftId}:post_publish:auto` の別形式で、この insert は parent_job_id を渡していないためdraft単位の冪等（指摘の理由付けは正しい）。(4) docs/requirements/04_jobs_and_automation.md:52（§3）と :176（§9）が `parent:{parent_job_id}:{kind}:{draft_id}` を規定しており、キー形式は仕様上1つに定められている。提案は生成文字列が完全に同一で、terminal.ts側も統合ではなく別名ヘルパの切り出しなので「判断が違うものを統合する」害はなく、外部の振る舞い・DB契約は変わらない。ただし根拠の一部は不正確で、この点は報告時に削るべき: 「本番のkey形式を壊してもテストは緑」は keys.test.ts 単体には当てはまるが、post-generation.test.ts:129/:167 が `parent:job-x:image_generation:draft1` を、terminal.db.test.ts:231 が `job:${draftId

### 42. [medium] expectedBranchFor に本番の呼び出し元が無く、release.mjs が同じ判断を再実装している

- 種別: リファクタ ／ 領域: 運営コマンド・診断・メール
- 場所: `src/lib/ops/release-gate.ts:52`
- 根拠: release-gate.ts:52 `expectedBranchFor(target)` は export され release-gate.test.ts:32 で staging→stg / production→main を固定している。しかし repo 全体を grep しても呼び出し元はテストのみで、実際の運営コマンド scripts/release.mjs:31 は `const expectedBranch = target === "staging" ? "stg" : "main";` と同じ判断を別に書いている（その値を evaluateReleaseGate の expectedBranch へ渡す）。つまり「反映元ブランチ」というリリースの前提を守っているつもりのテストは、実際に使われる分岐を1つも通っていない。
- 直し方: release.mjs で release-gate.ts を動的importしている箇所（96行付近の分割代入）へ expectedBranchFor を加え、31行をその呼び出しへ置き換える。値は同じなので挙動は変わらず、テストが実経路を守るようになる。
- 反証を試みた結果: 指摘は実在する。HEAD で確認: src/lib/ops/release-gate.ts:52-54 の `expectedBranchFor` は `target === "staging" ? "stg" : "main"` を返すが、node_modules を除く repo 全体の grep で一致するのは定義行・release-gate.test.ts:5 の import・同 32-35 のアサーションの3箇所のみで、本番の呼び出し元は0件。実際の運営コマンド scripts/release.mjs:31 が同一の判断を `const expectedBranch = target === "staging" ? "stg" : "main";` としてインラインに書き直し、その値を line 134 の evaluateReleaseGate へ渡している。`"stg"` の grep でもこの2箇所しかマッピングを持たない。判断は「似ているが違う」ものではなく完全に同一: release.mjs:17 で target は "production" | "staging" のどちらかに絞られ、argv 解析には --apply / --base / --account しかなく期待ブランチを上書きする経路が無い。よってテストが固定している staging→stg / production→main は実経路を1本も通っていない。ただし提案の手順には修正が必要: expectedBranch は line 35（git rev-list）と line 49（gh run list）で、動的 import（scripts/release.mjs:96-104）より前のトップレベルで消費されるため、96行の分割代入へ追加するだけでは TDZ になる。import ブロック自体を31行より上へ移す必要がある（コメント上の制約は「unappliedMigrations() の呼び出し（106行）より前」だけなので移動は安全で、plain node での fall

### 43. [medium] supabase/config.toml と push-auth-templates.mjs の二重持ち（件名・コード桁数・rate limit）に機械検査が無い

- 種別: リファクタ ／ 領域: 運営コマンド・診断・メール
- 場所: `scripts/push-auth-templates.mjs:119`
- 根拠: push-auth-templates.mjs は TEMPLATES の subject（48行 `Exos AIのメールアドレス確認` / 58行 `Exos AIのパスワード再設定`）を「config.toml と同じ文言にそろえる」とコメントしつつ手書きで持ち、AUTH_SETTINGS（119〜136行）も `mailer_otp_length: 6`（コメントに「EMAIL_CODE_LENGTH（画面側）と必ず一致させる」）・`mailer_otp_exp: 3600`・`rate_limit_email_sent: 30`・`rate_limit_verify: 30` を手書きで持つ。実際の正本は supabase/config.toml（253/257行の subject、233行 otp_length、235行 otp_expiry、[auth.rate_limit] の email_sent/token_verifications）と src/lib/auth/email-code.ts:16 の EMAIL_CODE_LENGTH。テンプレート本文だけは同じ .html を共有しているが、これらの値は片方を直しても何も落ちない。T-M8-123 は実際に mailer_otp_length が 8 で本番の登録が止まった事故である。
- 直し方: config.toml をパースして (a) `[auth.email.template.*].subject` と TEMPLATES[].subject、(b) `otp_length` と EMAIL_CODE_LENGTH と AUTH_SETTINGS.mailer_otp_length、(c) `otp_expiry`／`email_sent`／`token_verifications` と対応する AUTH_SETTINGS の値が一致することを検査する単体テストを追加する（0件マッチで落ちるガード付き）。押す値・表示は変えない。
- 反証を試みた結果: 反証できなかった。指摘は実在する。(1) 重複の実在: scripts/push-auth-templates.mjs:48,58 の subject と supabase/config.toml:253,257 の subject が別々の手書きリテラル。AUTH_SETTINGS(118-136行) の mailer_otp_length:6 / mailer_otp_exp:3600 / rate_limit_email_sent:30 / rate_limit_verify:30 に対し config.toml は otp_length=6(233) / otp_expiry=3600(235) / email_sent=30(199) / token_verifications=30(210) を持ち、さらに src/lib/auth/email-code.ts:16 の EMAIL_CODE_LENGTH=6 が3つめの複製。(2) 機械検査の不在を実測で確認: rg で readFileSync(.*toml|supabase/config を *.ts/*.mjs 全体に当てても散文コメント（doctor.mjs:171、auth-url-status.ts:138、storage-bucket.db.test.ts:8,12）だけがヒットし、config.toml をパースするコードは1つも無い。TOMLパーサも依存に無い（0件）。doctor.mjs の authUrlCheck→judgeAuthUrls もリモートの mailer_templates_confirmation_content / site_url / smtp_host / smtp_sender_name / uri_allow_list しか見ておらず、mailer_otp_length もレート制限も検査対象外。(3) 「統合すると害がある」も否定: config.toml の197-198行が「リモートも同じ値へ npm run auth:templates が揃える（T

### 44. [medium] asks_user_opinion / asksUserOpinion が読まれない死んだ属性として全レイヤに残っている

- 種別: リファクタ ／ 領域: 投稿パターン
- 場所: `src/lib/post/pattern-spec.ts:30`
- 根拠: 列 asks_user_opinion は migration 20260818000001:31 で作られ、seed（同 91行・20260818000003:130・20260818000008:22）と pattern_spec_of（20260818000001:137・000003:117・000007:47）に載り、TS 側は PatternSpec.asksUserOpinion（pattern-spec.ts:30/106）と PatternOption.asksUserOpinion（post-patterns-store.ts:41/59/69/81）まで運ばれる。しかし値を**読む**箇所は grep で1つも無い（UI・生成・検証すべて placeholders 経由）。要件02 §3.21 も「使っていない（列の撤去は別途）」と明記している。
- 直し方: TS 側の asksUserOpinion（PatternSpec／PatternOption／SELECT の COLUMNS／parsePatternSpec／toOption）を落とし、pattern_spec_of と seed 関数からも外す migration を1本入れる（列の drop も同時に行う。pattern_spec は snapshot なので過去の job も参照されない）。docs 側は §3.21 の行を削除する。
- 反証を試みた結果: 指摘は実在する（反証できなかった）。書き込み専用の死んだ属性であることを全参照箇所で確認した。

書く側だけが存在する:
- 列定義: supabase/migrations/20260818000001_post_patterns.sql:31（コメントも「旧『pattern === 'p2' のときだけ』（画面の出し分けのみ）」）
- seed: 同 :91、20260818000003:130、20260818000008:22（p2 のみ opinion=true）
- snapshot 生成: pattern_spec_of の jsonb_build_object（20260818000001:137・000003:117・000007:47）
- TS: src/lib/post/pattern-spec.ts:30（PatternSpec 型）・:106（parsePatternSpec で写す）、src/lib/post/post-patterns-store.ts:41（PatternOption）・:59（PatternRow）・:69（SELECT の COLUMNS）・:81（toOption）・:417（applyCreatePattern の insert で定数 false を書く）

読む側が本当に無いこと:
- `rg -ni "opinion" src -g '!*.test.ts'` の結果は上記の定義・代入と、無関係な smoke の `user_opinion` 文字列（src/lib/smoke/scenarios.ts:258）と redact の鍵名（src/lib/observability/redact.ts:43）だけ。`asksUserOpinion` を参照する式は1件も無い。テスト側（pattern-spec.test.ts / post-generation.test.ts など）も fixture に値を置くだけで assert していない。
- 画面: src/components/post/patte

### 45. [medium] GENERATION_MAX_POSTS / maxPostsFor / PATTERN_MAX_POSTS が本番コードから参照されていない

- 種別: リファクタ ／ 領域: 投稿パターン
- 場所: `src/lib/post/thread-limits.ts:17`
- 根拠: grep の結果、GENERATION_MAX_POSTS の参照は thread-limits.test.ts・gen-prompts.test.ts・post-patterns.db.test.ts のみ、maxPostsFor はテストのみ、PATTERN_MAX_POSTS（generation-validation.ts:33）は generation-validation.test.ts・thread-limits.test.ts のみ。本番の分量判定はすべて pattern_spec / drafts の列へ移った（capPostCount は引数で上限を受ける）。thread-limits.ts:13-15 のコメントも「PATTERN_MAX_POSTS = 編集で許す上限と日次枠の見積り」と、現在は max_posts_edit が担う役割を説明したまま残っている。
- 直し方: maxPostsFor と PATTERN_MAX_POSTS を削除し、それを参照するテストも落とす。GENERATION_MAX_POSTS は post-patterns.db.test.ts が seed 値の期待値として使っているので、名前を SEEDED_MAX_POSTS のような「seed の期待値」を表すものへ改め、置き場所も seed 検証の近く（post-patterns 系）へ移す。thread-limits.ts のコメントを現状（capPostCount だけが残る）に合わせる。
- 反証を試みた結果: 反証できなかった。指摘は実ファイルで裏付けられる。(1) thread-limits.ts を import しているのは 4 箇所のみで、本番コードは generation-validation.ts:10 が capPostCount だけを取る。GENERATION_MAX_POSTS は thread-limits.test.ts / post-patterns.db.test.ts / gen-prompts.test.ts のみ、maxPostsFor は thread-limits.test.ts のみ。namespace import（import * as）は src/lib/post・src/lib/jobs・src/app に存在せず、間接参照の逃げ道もない。(2) generation-validation.ts:33 の PATTERN_MAX_POSTS は generation-validation.test.ts:6 と thread-limits.test.ts:3 のみ。同モジュールの本番利用は revalidateEditedThread / finalizeThread / threadBlocksAutoPost だけ。(3) 分量判定は実際にDB列へ移っている（generation-validation.ts:138 は引数で上限を受け、schedule-enqueue.ts:166 は spec.maxPostsEdit、drafts.ts:175 と draft-editor.tsx:37 は draft.max_posts_edit）。(4) thread-limits.ts:13-15 のコメントが古いのも事実で、migration 20260818000003 と 要件06 §4.3（「単一の正は post_patterns の行」）が現状は max_posts_edit の役割だと明記している。反証の試みはいずれも成立しなかった: 定数がseedの正本という説は否定される（migrationはSQLに直値、docs

### 46. [medium] アカウント.mdエディタとプロンプトエディタで Note 型・ActionError 型・警告バナーのマークアップが丸ごと重複している

- 種別: リファクタ ／ 領域: 画面・UI共通部品
- 場所: `src/app/app/settings/base-md-editor.tsx:199`
- 根拠: base-md-editor.tsx:39-47 と prompt-templates-editor.tsx:37-45 に `type Note = {kind:"validation"...} | {kind:"conflict"...} | null` と `interface ActionError { code?; details?; message? }` が逐語で二重定義されている（prompt-templates-editor.tsx:35 のコメントは「判断は base-md-editor.tsx と同じ」と自認している）。描画側も base-md-editor.tsx:198-212 と prompt-templates-editor.tsx:173-190 が同一の class 文字列 `rounded-card border border-hairline bg-warn-bg px-4 py-2 text-sm text-warn-fg` ＋ role="alert" ＋ `note.kind === "conflict"` のときだけ `ml-2 font-medium underline underline-offset-2 disabled:opacity-50` の「再読み込み」ボタンを出す形で重複。要件06 §2.1（06_screens_onboarding_posting.md:157）が「ボタンを伴う案内」＝アカウント.md／プロンプトの競合＋『再読み込み』としてこの2画面を1つの規約で束ねているのに、実装は2本に分かれている（T-M8-134 が AlertDialog を alert-dialog-classes.ts へ寄せたのと同型の残り）。
- 直し方: `Note` 型と表示を1つの部品へ出す（例: src/components/ui/editor-note.tsx に `type EditorNote` と `<EditorNote note={...} onReload={...} pending={...} />`）。見た目は現行の2ファイルが完全に同じ文字列なので、どちらを採っても表示は変わらない。ActionError も既に共通の Server Action 戻り値型（src/app/actions/_helpers.ts の BaseResult 系）があるならそこへ寄せ、無ければ同じ部品ファイルへ1つだけ置く。
- 反証を試みた結果: 反証できなかった（指摘は実在）。実ファイルで確認した内容:

1. 型の逐語重複: `src/app/app/settings/base-md-editor.tsx:39-42` の `type Note = {kind:"validation";text} | {kind:"conflict";text} | null` と `:44-48` の `interface ActionError { code?; details?; message? }` が、`src/app/app/settings/prompt-templates-editor.tsx:37-40` / `:42-46` に文字単位で同一のまま二重定義されている。`rg 'interface ActionError|type ActionError'` の結果はこの2ファイルのみで、共通の置き場は無い（`src/components/ui/` に editor-note 相当は存在しない）。prompt-templates-editor.tsx:34 のコメント「判断は `base-md-editor.tsx` と同じ」も自認どおり。

2. 描画の逐語重複: base-md-editor.tsx:198-215 と prompt-templates-editor.tsx:173-190 が、`className="rounded-card border border-hairline bg-warn-bg px-4 py-2 text-sm text-warn-fg"` ＋ `role="alert"` ＋ `note.kind === "conflict"` のときだけ `className="ml-2 font-medium underline underline-offset-2 disabled:opacity-50"` の「再読み込み」ボタンを出す構造まで完全に一致。差はハンドラだけ（base-md は `startTransition(reload)`、prompt は `void r

### 47. [low] 要件05 §2.2 のエラー応答例の message が実装と違い、廃止語「生成枠」を使っている

- 種別: ドキュメント乖離 ／ 領域: 認証・課金・利用量
- 場所: `docs/requirements/05_api_server_actions.md:37`
- 根拠: 05 §2.2（30-39行）の失敗応答例は `"code": "usage_limit_exceeded", "message": "今月の生成枠を使い切っています。"`。実装 src/lib/observability/errors.ts:37 は `usage_limit_exceeded: "今月の利用上限に達しています。"`。また「生成枠」は T-M8-109 で AIクレジットへ統合された旧呼称（要件03 §7.1・PRD v1.18）で、`portal-configuration.test.ts:167` は Stripe 側の説明文について「生成枠」を廃止語として明示的に禁止している。
- 直し方: §2.2 の例の message を実装の `ERROR_MESSAGES.usage_limit_exceeded`（「今月の利用上限に達しています。」）へ合わせる。例文の出典が errors.ts であることを1行添えて、以後の文言変更で追随先が分かるようにする。docsのみ。
- 反証を試みた結果: 反証できなかった。実ファイルで確認した事実: (1) docs/requirements/05_api_server_actions.md:30-39 の失敗応答例は `"message": "今月の生成枠を使い切っています。"`。(2) src/lib/observability/errors.ts:37 は `usage_limit_exceeded: "今月の利用上限に達しています。"` で、`toUserFacingError`（同:83）は常に `USER_MESSAGES[code]` を返し AppError の message を見ない（src/app/actions/settings.ts:42 のNOTEにも明記）ため、docs の文字列は実装が出し得ない。(3) `git log -S` によると code 側は errors.ts 作成時から一貫して「今月の利用上限に達しています。」で、docs の該当行は 2026-07-20 の初版（89fe0da）以来一度も一致していない。(4) `grep -rn "今月の利用上限" docs/` は0件で、正しい記述を持つ別節は存在しない（他の usage_limit_exceeded 言及は 05:51 のコード表と 06:307 の挙動記述のみ）。

「例文だから仕様レベルの抽象にすぎない」という反証は成立しない。同じ 05 の §239 が `USER_MESSAGES.validation_error` を「入力内容を確認してください。」と逐語で正しく引用しており、この文書の慣行は実装文字列をそのまま書くこと。したがって §2.2 の例が実装と違うのは同文書内の不整合。さらに内容面でも古い: 要件03 §7.1 は生成枠・画像枠を金額制の AIクレジットへ統合済み（T-M8-109）で、`usage_limit_exceeded` は AIクレジット／通常投稿枠／URL付き投稿枠の3枠を束ねるため実装の文言は意図的に総称。例文は存在しない枠を名指しし、枠別メッセージであるかのように読ませる。



### 48. [low] navigation-items.test.ts のテスト名が「7項目」だが実際の定義と期待値は6項目

- 種別: ドキュメント乖離 ／ 領域: 空振り検査
- 場所: `src/components/app-shell/navigation-items.test.ts:8`
- 根拠: 8行目 `it("新デザインの7項目を表示順で定義する（T-M8-04）"...` の直後の `toEqual` は `/app`・`/app/news`・`/app/posts`・`/app/schedule`・`/app/analytics`・`/app/settings` の**6件**。`src/components/app-shell/navigation-items.ts` も6件。T-M8-104 で `/app/ai-settings` が `/app/settings` へ統合された際にテスト名だけ取り残されたと読める。検査自体は完全一致なので機能しているが、落ちたときのメッセージが「7項目」と言うため、読んだ人は「1件足りない」と誤読する。
- 直し方: テスト名の「7項目」を「6項目」に直す。期待値配列は現状のままで正しいので、名前だけの修正で振る舞いも検査内容も変わらない。
- 反証を試みた結果: 反証できなかった。指摘は実在する。

確認したこと:
1. src/components/app-shell/navigation-items.test.ts:8 のテスト名は「新デザインの7項目を表示順で定義する（T-M8-04）」だが、直後の toEqual 配列は /app・/app/news・/app/posts・/app/schedule・/app/analytics・/app/settings の6件。
2. src/components/app-shell/navigation-items.ts の APP_NAVIGATION_ITEMS も6件。19行目に「旧「AI設定」はT-M8-104で「設定」へ統合（/app/ai-settings はリダイレクトのみ）。」と明記されている。
3. 原因もコミットで裏付けられた。git show 922ea64（feat(T-M8-104): 設定とAI設定を1つの「設定」に統合する）が両ファイルから { href: "/app/ai-settings", label: "AI設定" } を削除しており、コミットメッセージにも「ナビは6項目に。」とある。テスト名だけが取り残されたという読みは正しい。
4. 「7項目」が別の意味で正しい余地は無い。APP_NAVIGATION_ITEMS の参照元は app-navigation.tsx（28行目で .map するだけ）と current-screen-title.tsx のみで、7項目目を実行時に足す経路は無い。design_handoff_ui/README.md:47 でも「料金プラン」リンクはナビ項目の外に数えられている。
5. 検査自体は生きている。npx vitest run src/components/app-shell/navigation-items.test.ts は 3 passed で緑。完全一致の toEqual なので空振りではなく、問題は落ちたときのメッセージが誤読を招く点だけ。提案どおり名前のみの修正で振る舞いも検査内容も変わらない

### 49. [low] landing-page.test.ts のテスト名が「規定の5箇所だけ」だが実際の期待値は4（本文コメントは4と書いている）

- 種別: ドキュメント乖離 ／ 領域: 空振り検査
- 場所: `src/app/landing-page.test.ts:216`
- 根拠: 216行目 `it("ブランドグラデーションは規定の5箇所だけ（ロゴはLogoTile側なので数えない）"...` の中で、コメントは「**T-M8-125で4本になった**」と正しく書いてあり、アサーションも `expect(direct.length).toBe(4)`。テスト名だけが5のまま。直前の T-M8-137（99efba2）でこのファイルを触った際も残っている。件数固定の検査なので空振りはしないが、落ちたときに「5箇所のはず」と表示され、正しい規定値（4）と矛盾した指示を出す。
- 直し方: テスト名の「5箇所」を「4箇所」に直す（コメントとアサーションが既に4で一致しているため、名前を合わせるだけ）。検査内容は変えない。
- 反証を試みた結果: 反証できなかった。src/app/landing-page.test.ts:216 のテスト名は「規定の5箇所だけ」だが、直下のコメント（217-220行）は「**T-M8-125で4本になった**」と明記し、アサーション（222行）は `expect(direct.length).toBe(4)`。実測でも走査対象（src/app/page.tsx 3件＋src/components/lp/hero-mock.tsx 1件、faq/figures/pricing は0件）の合計はちょうど4件で、テスト名が言うようにロゴ（LogoTile）は走査範囲外。よって「5」が正しくなる解釈は存在しない。別箇所に正しい記述がある可能性も調べたが、唯一の仕様文である design_handoff_lp/README.md:30・:126 は「規定3種（うち1つは T-M8-125 で消えたプレミアムカード上端バー）」というカテゴリ数の記述で、5という数字はどこにも無い（docs/ 配下に brand-gradient の記述自体が無い）。テストは現在通る（`npx vitest run src/app/landing-page.test.ts -t "ブランドグラデーション"` → 1 passed）ので空振りではなく、落ちたときにだけ「5箇所のはず」と規定値4と矛盾した表示を出す。文字列 "規定の5箇所" はリポジトリ内で他から参照されていないため、名前の修正は検査内容に影響しない。なお同じ T-M8-125 由来の取り残しが 225-231行のコメント（`出現数は5のままで`）にもあり、同時に直すのが妥当。影響は軽微（表示上の矛盾のみ）だが指摘自体は実在する。

### 50. [low] launchd運用メモの news_fetch スケジュールが縮小前（毎時）のまま

- 種別: ドキュメント乖離 ／ 領域: jobs・cron・スケジュール
- 場所: `docs/operations/launchd-to-vercel-cron.md:31`
- 根拠: §2 の表(:31)が『news_fetch | 09:00〜20:00の毎時00分』、同 §2 の本文(:40)も『news-fetch=Hour 9〜20/Minute 0』と書くが、実体の ops/launchd/com.spaceai.news-fetch.plist は Hour 10/12/14/16/18/20・Minute 0 で、src/ops/launchd.test.ts:66 は NEWS_FETCH_JST_HOURS（=[10,12,14,16,18,20]）との一致を検査している。T-M7-55（3分野×2時間おきへ縮小）が要件04 §6 とplistには反映され、この運用メモだけ取り残されている。ロールバック先として残している資産の説明が古いと、切り戻し時に費用が倍になる誤設定を招く。
- 直し方: §2 の表と本文の news_fetch 行を『10:00〜20:00の2時間おき（10/12/14/16/18/20時）』へ直し、値の正本が NEWS_FETCH_JST_HOURS（launchd.test.ts が突き合わせる）であることを明記する。
- 反証を試みた結果: 反証できず、指摘は実在する。docs/operations/launchd-to-vercel-cron.md:31 の表は『news_fetch | 09:00〜20:00の毎時00分』、同 §2 本文 :40 も『news-fetch=Hour 9〜20/Minute 0』と書くが、実体の ops/launchd/com.spaceai.news-fetch.plist の StartCalendarInterval は Hour 10/12/14/16/18/20・Minute 0 の6件のみ。値の正本は src/lib/jobs/news-research.ts:251 の NEWS_FETCH_JST_HOURS = [10,12,14,16,18,20] で、src/ops/launchd.test.ts:66 が plist と一致することを検査している（コメントに「起動時刻は費用に直結する…片方だけ変えて取りこぼす／余計に課金される状態を防ぐ（T-M7-55）」）が、docs 側はどの検査にも掛かっていない。

反証の試みはいずれも失敗した。(1)「別の節に正しい記述がある」→ §4 の vercel.json は `0 1-11/2 * * *`（UTC1〜11/2=JST10〜20）で正しいが、これは Vercel 側のschedule。launchd のロールバック資産を説明するのは §2 だけで、そこが誤り。結果として同一文書内で §2（毎時9〜20）と §4（2時間おき10〜20）が食い違っている。(2)「§2 は『初期launchdスケジュール』なので履歴として意図的に古い」→ 成立しない。§15 が ops/launchd/ の4本を現用のロールバック先（§5）として残すと明記し、:40 は「実体は ops/launchd/」として現在のファイル内容を断定している。文書ヘッダは v1.4／2026-08-15 で、T-M7-55（d1ea51d ニュース取得を3分野×2時間おきへ縮小）より後に更新されているのにこの行だけ取り残されている。(

### 51. [low] doctor の検査項目一覧と層8の説明が最新の実装に追いついていない

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `docs/operations/development-and-testing.md:352`
- 根拠: (1) development-and-testing.md:352 の層8の「見えないもの」欄は「X Developer App のcallback URL・**SMTPの設定は手動確認**」だが、いまは doctor が Supabase 側のカスタムSMTPの有無（src/lib/ops/auth-url-status.ts:171）と差出人名（同 195行）を検査し、未設定なら error にする。同節の「この層で見つかった不具合は**3件**になりました」も、その後 T-M8-120（テンプレート未反映）・T-M8-123（otp桁数8・送信上限2通/時）・T-M8-136（内蔵送信のまま・差出人名既定）が加わっている（auth-url-status.ts:16 のヘッダも「3件目」のまま）。(2) local-development.md:114 の doctor の項目列挙には「定時実行」（judgeScheduler・T-M8-51）と「確認メールの行き先（ローカル＝Mailpit）」（scripts/doctor.mjs:75-100・T-M8-136）が無い。
- 直し方: 層8の表の守るもの欄へ「Supabaseのカスタム SMTP と差出人名」を追記し、見えないもの欄から「SMTPの設定は手動確認」を外す（残すのはX Developer App のcallback URL）。件数は実績に合わせて更新し、auth-url-status.ts のヘッダの「3件目」も併せて直す。local-development.md:114 の列挙に「定時実行」「確認メールの行き先（ローカル・Mailpit）」を追加する。
- 反証を試みた結果: 反証できず、指摘は実在する。実ファイルで確認した内容:

【(1) 層8の表が実装と食い違う — 確認】
- docs/operations/development-and-testing.md:352 の層8行、「見えないもの」欄は現在も「X Developer App のcallback URL・**SMTPの設定は手動確認**」。一方 src/lib/ops/auth-url-status.ts の judgeAuthUrls は (a) 確認メールテンプレートに `{{ .Token }}` があるか（144-160行）、(b) カスタムSMTPが未設定＝Supabase内蔵送信のままか（169-181行、`input.smtpHost !== undefined && !input.smtpHost` → level:"error"）、(c) 差出人名が `EXPECTED_SENDER_NAME`("Exos AI") か（189-200行 → level:"error"）を検査している。scripts/doctor.mjs:195,197 が Management API 応答から `smtp_host` / `smtp_sender_name` を渡しているので doctor 経由で実際に評価される。よって「SMTPの設定は手動確認」は誤り。「守るもの」欄も Site URL・Redirect URLs・CAPTCHA・Stripeポータルのみで、テンプレート／SMTP／差出人名に触れていない。指摘者の行番号（171/195）は実際は 169/189 だが内容は一致。
- 同文書の他節に正しい記述は無い（`grep -n "SMTP|auth:templates|差出人|テンプレート"` のヒットは 304行=doctorの見るもの一覧、352行=当該行、557行=送信ガードのみ）。304行の SMTP は「お知らせメールの滞留」文脈で、Supabase側SMTP設定の検査には触れていない。「未実装」注記も無く、検査対象を具体ファイル名（`captcha

### 52. [low] テスト本数の記述が3つの文書でばらばらで、いずれも実測とずれている

- 種別: ドキュメント乖離 ／ 領域: 運営コマンド・診断・メール
- 場所: `docs/operations/ci.md:56`
- 根拠: ci.md:56「E2Eは27シナリオ（`e2e/` 13ファイル）」、development-and-testing.md:348「E2E（19ファイル58件）」・:354「src 配下のテストファイル227本 = 単体160 ＋ *.db.test.ts 66 ＋ *.live.test.ts 1」・:128「データベース関連のテスト66本」、release-checklist.md:15「DBテスト58本のskipを禁止」。実測（HEAD）は e2e/*.spec.ts 25ファイル・`test(` 87件、src 配下 253本 = 単体182 ＋ *.db.test.ts 69 ＋ *.live.test.ts 2。development-and-testing.md 自身が「この数字は増え続けるので、乖離に気付いたら実測へ直す」と書いている。
- 直し方: 4箇所の数値を実測へ更新し、同じ数字を2箇所以上に書かない（ci.md と release-checklist.md は本数を書かず development-and-testing.md §9 へリンクする）形に寄せる。docsのみ。
- 反証を試みた結果: 反証できず。指摘した4箇所すべてが実在し、いずれの解釈でも実測とずれている（むしろ指摘より箇所が多い）。

【実測（HEAD）】
- `npx playwright test --list` → **Total: 88 tests in 25 files**（`e2e/*.spec.ts` は 25ファイル。行頭 `test(` の grep は87だが Playwright の申告値88が正）
- `find src -type f -name '*.test.ts'` → **252本**（`.test.tsx` は0件）。内訳 = 単体181 ＋ `*.db.test.ts` **69** ＋ `*.live.test.ts` **2**（`src/lib/ai/provider-contract.live.test.ts`・`src/lib/jobs/suggestion.live.test.ts`）
- ケース数では DB統合 344件・単体 1,522件

【docsの記述（実際に該当行を読んで確認）】
- `docs/operations/ci.md:56`「**E2Eは27シナリオ**（`e2e/` 13ファイル）」→ 実測 25ファイル/88件。`git log` では T-M7-26（2026-07-31）以降このファイルの当該記述は更新されておらず、2026-08-13以降に e2e に22ファイルが追加されている（`pattern-manager` `plans` `x-oauth` `notifications` `schedule-generation-inputs` 等）。
- `docs/operations/development-and-testing.md:346`「単体（160ファイル）」→ 181
- `:347`「DB統合（66ファイル）」→ 69
- `:348`「E2E（19ファイル58件）」→ 25ファイル88件
- `:354`「227本 = 単体160 ＋ db66 ＋ live1」→ 252本 = 181 ＋ 69

### 53. [low] 要件05 §8 の validation_error 理由一覧が実装と食い違い、画面側にも死んだ分岐が残っている

- 種別: ドキュメント乖離 ／ 領域: 投稿パターン
- 場所: `docs/requirements/05_api_server_actions.md:249`
- 根拠: 249行は理由として `name_length`／`name_unsafe_chars`／`name_taken`／`max_posts_range`／`prompt_required`／`too_long`／`quote_with_digest`／`last_pattern` を挙げる。実装（validatePatternInput / validatePlaceholders・post-patterns-store.ts:322-386）が返すのは name_length・name_unsafe_chars・name_taken・description_length・prompt_required・too_long・empty・placeholder_name_length・placeholder_name_unsafe・placeholder_duplicated・placeholder_not_used・placeholder_too_many・last_pattern。`max_posts_range` は分量の入力欄をやめた（T-M8-132）ため生成されず、`quote_with_digest` はどこからも投げられない（grep でヒットするのは pattern-fields.tsx:107 の受け側だけ）。`no_system_default`（pattern-fields.tsx:111）も同様に送り手が無い。
- 直し方: §8 の理由一覧を実装が返すものへ揃える（description_length と placeholder_* を追記し、max_posts_range・quote_with_digest を削る）。patternReasonMessage の quote_with_digest・no_system_default の case も削除する。
- 反証を試みた結果: 実在を確認した（反証できず）。docs/requirements/05_api_server_actions.md:249 は validation_error の details.reason として max_posts_range と quote_with_digest を挙げるが、リポジトリ全体を `rg 'reason: *"(quote_with_digest|no_system_default|max_posts_range)"'` で探しても投げ手は0件。実装（src/lib/post/post-patterns-store.ts）が実際に投げるのは name_length(328)・name_unsafe_chars(333)・description_length(337)・prompt_required(342)・empty(222)・too_long(226)・placeholder_too_many(360)・placeholder_name_length(368)・placeholder_name_unsafe(373)・placeholder_duplicated(377)・placeholder_not_used(382)・name_taken(406/461)・last_pattern(509) で、指摘の一覧どおり。migration に出る post_patterns_max_posts_range はDB制約名にすぎず、制約名→reason の対応付けコードは存在しないため producer にならない。履歴でも裏付けられる: `git log -S'reason: "quote_with_digest"'` と `-S'reason: "max_posts_range"'` はいずれも eb681d2（T-M8-132、分量・引用・ニュースの入力欄を廃止）で終わり、no_system_default の throw は 3c89c84 の post-patterns-store.ts:255 が最後。さらに `git show eb

### 54. [low] 要件02 §3.21 の「自作パターンの既定は min(7, max_posts + 2)」が T-M8-130 の上限引き上げ後も直っていない

- 種別: ドキュメント乖離 ／ 領域: 投稿パターン
- 場所: `docs/requirements/02_data_model.md:585`
- 根拠: 585行は max_posts_edit の既定を「自作パターンの既定は`min(7, max_posts + 2)`」と書く。実装は applyCreatePattern（post-patterns-store.ts:433）の `Math.min(PATTERN_MAX_POSTS_LIMIT, maxPosts + 2)` で PATTERN_MAX_POSTS_LIMIT=8、DB の列 default も migration 20260818000006 で `least(7,…)`→default 8 へ引き上げ、CHECK も `max_posts_edit between 1 and 8` になっている（同 migration 18-22行）。同じ行の「not null default 8」とも文中で矛盾している。
- 直し方: 585行を `min(8, max_posts + 2)` に直す（上限8は同じ行の default 8・CHECK と一致させる）。
- 反証を試みた結果: 反証できず、指摘は実在する。docs/requirements/02_data_model.md:585 は「自作パターンの既定は`min(7, max_posts + 2)`」と書くが、実装は src/lib/post/post-patterns-store.ts:428 の `Math.min(PATTERN_MAX_POSTS_LIMIT, maxPosts + 2)`（同 :284 で PATTERN_MAX_POSTS_LIMIT = 8）。DB側も migration 20260818000006_thread_count_range.sql（T-M8-130）で `max_posts_edit` の column default を 7→8 へ、CHECK を `max_posts_edit >= max_posts and max_posts_edit between 1 and 8` へ引き上げており、`least(7, max_posts + 2)` は旧 20260818000003 のバックフィル（当時 default 7）にしか残っていない。000007〜000009 に上限を戻す変更は無い。反証の観点も潰した: (1) docs 内で正しい記述がある節は無い（`min(7|min(8|least(7|max_posts + 2` の rg は 585 行のみ、他の max_posts_edit 言及は 02:273 / 05:249 / 06:278 / ADR-0008:74 で既定式を述べていない）。(2) 仕様レベルの抽象ではない——同じ行が `default 8`・`max_posts`以上8以下 と具体値を書き、598 行の Constraints も 8。(3) 表記ゆれ以上の実害がある——`max_posts` が 8 まで許される今、`min(7, …)` だと `max_posts_edit=7 < max_posts=8` となり同じ行が書く CHECK 自体を満たせず、記述が自己矛盾する。提案どおり 585 行を `mi

### 55. [low] 要件02 §3.10/§3.9 が撤去済みの旧 `pattern` 列と CHECK を今も制約として書いている

- 種別: ドキュメント乖離 ／ 領域: 投稿パターン
- 場所: `docs/requirements/02_data_model.md:321`
- 根拠: §3.10 の Constraints は「patternは`p5`不可、曜日は0〜6で…」と始まるが、`schedule_slots.pattern` 列と `schedule_slots_pattern_not_p5` CHECK は migration 20260818000005（38-42行）で drop 済みで、enum post_pattern 自体も同 migration で削除されている。同じ文の後半は「旧`p5`不可の意図をパターン属性へ移した」と正しい説明をしているので、前半だけが取り残されている。§3.9（275行）の「書き込み時はpattern別最大数も検証」も同じ世代の記述。
- 直し方: §3.10 Constraints の「patternは`p5`不可、」を削り、`requires_quote_url` のトリガ拒否（migration 20260818000001:266-284）だけを制約として残す。§3.9 は `max_posts_edit` による検証へ書き換える。
- 反証を試みた結果: 反証できなかった（指摘は実在する）。docs/requirements/02_data_model.md:321 §3.10 の Constraints は「patternは`p5`不可、曜日は0〜6で…」と始まるが、`schedule_slots.pattern` 列・`schedule_slots_pattern_not_p5` CHECK・enum `post_pattern` は supabase/migrations/20260818000005_drop_post_pattern_enum.sql の39〜46行で全て drop 済みで、以降の migration（000006〜000009）で再追加も無い。後継は 20260818000001_post_patterns.sql:266-284 の `schedule_slots_pattern_usable()` トリガ（`requires_quote_url` なら errcode 23514 で拒否）で、同じ文の後半と docs/requirements/04_jobs_and_automation.md:19（「旧enum `p5` は撤去した」）が正しく説明している。§3.10 の列定義表自体は `pattern_id` へ更新済みなので、Constraints 行の第1句だけが旧世代の取り残し。仕様レベルの抽象表現ではなく、存在しない列と存在しない enum 値を現行制約として名指ししている。§3.9（275行）`thread` の「書き込み時はpattern別最大数も検証」も、実装 src/lib/drafts.ts:175 が `draft.max_posts_edit`（生成時snapshot）で検証しパターンを再読しない（コード内コメントで明示）ため、同じ表の `max_posts_edit` 行の説明と食い違う。提案どおり「patternは`p5`不可、」の削除と §3.9 の `max_posts_edit` への書き換えが妥当。

### 56. [low] 要件06 §2 の中で「ナビ7項目」と「ナビ項目は6つ」が併存し、コードのコメントとテスト名も7項目のまま

- 種別: ドキュメント乖離 ／ 領域: 画面・UI共通部品
- 場所: `docs/requirements/06_screens_onboarding_posting.md:131`
- 根拠: §2 の line 126 は「ナビ項目はホーム、最新ニュース、投稿作成、スケジュール、投稿分析、設定の6つ（AI設定はT-M8-104で設定へ統合）」と正しく書いているが、同じ節の line 131 は「App ShellはPC幅で固定左ナビ（234px）…ナビ7項目のrouteへ遷移する」と7項目のまま。実装 src/components/app-shell/navigation-items.ts:12-22 の APP_NAVIGATION_ITEMS は6件で、同ファイル :7 のコメントは「新デザインは7項目（旧6項目）」、src/components/app-shell/navigation-items.test.ts:8 のテスト名は「新デザインの7項目を表示順で定義する（T-M8-04）」なのに assert している配列は6件。
- 直し方: 要件06 line 131 の「ナビ7項目」を6項目へ直す（line 126 と同じ数字にする）。navigation-items.ts:7 のコメントと navigation-items.test.ts:8 のテスト名も6項目へ直す。いずれも記述のみで振る舞いは変わらない。
- 反証を試みた結果: 反証できなかった。指摘は3点すべてHEADで実在を確認。(1) docs/requirements/06_screens_onboarding_posting.md:126 は「…投稿分析、設定の6つ（AI設定はT-M8-104で設定へ統合）」と正しいが、同じ§2の :131 は「App ShellはPC幅で固定左ナビ（234px）…ナビ7項目のrouteへ遷移する」と7項目のまま。(2) 実装 src/components/app-shell/navigation-items.ts:12-22 の APP_NAVIGATION_ITEMS は6件（/app, /app/news, /app/posts, /app/schedule, /app/analytics, /app/settings）で、同ファイル :6 のコメントは「新デザインは7項目（旧6項目）」。(3) src/components/app-shell/navigation-items.test.ts:8 のテスト名は「新デザインの7項目を表示順で定義する（T-M8-04）」だが assert している配列は6件。

意図的な抽象表現でも既対処でもないことを来歴で確認: git show 922ea64（feat(T-M8-104): 設定とAI設定を1つの「設定」に統合する）は line 126 を「分析・改善、AI設定、設定の7つ」→現在の6項目表記へ書き換え、コミットメッセージ自身も「ナビは6項目に。」と述べているが、line 131 は同コミットで一度も触られていない。repo全体の `grep -rn "7項目"` はこの3箇所のみで、line 131 を補正する正しい記述が別節にあるわけでもなく、「未実装」注記も無い。

「7」が別の何かを数えている可能性も否定: 描画点は AppNavigation（app-navigation.tsx:28）だけで APP_NAVIGATION_ITEMS をそのまま map しており、PC/モバイルどちらの分岐でも項目を追加していない。

なお本指摘の

### 57. [low] 6桁コード方式へ変えたのに、会員登録画面の導入文が「確認メールからメールアドレスを認証してください」とリンク追跡を促している

- 種別: ドキュメント乖離 ／ 領域: 画面・UI共通部品
- 場所: `src/app/signup/page.tsx:29`
- 根拠: src/app/signup/page.tsx:29 のリード文は「登録後、確認メールからメールアドレスを認証してください。」。実際の実装は T-M8-121 でコード方式へ移行しており、src/components/auth/email-code-form.tsx:38-43 は「確認コードを入力してください」「<email> に6桁の数字を送信しました」と表示する。要件06 §11（06_screens_onboarding_posting.md:433）は「signup成功後は同じ画面が6桁コードの入力へ切り替わる（T-M8-121。**メールのリンクを追わせない**——リンクは先読みで使い切られる・スマホで別ブラウザになる・テンプレート未反映で必ず失敗する、の3つで壊れた）」と明記している。登録前に最初に読まれる1文が旧方式の期待（メール内のリンクを探す）を作るため、方針と食い違っている。
- 直し方: signup/page.tsx:29 のリードを「登録後、メールで届く6桁の確認コードをこの画面に入力します。」相当へ直す（email-code-form.tsx が実際に出す文言と同じ語彙にする）。画面の遷移・検証ロジックは変えない。同じ語彙が2画面に散るのを避けるため、桁数は既存の EMAIL_CODE_LENGTH（src/lib/auth/email-code.ts）から描く。
- 反証を試みた結果: 反証できなかった。src/app/signup/page.tsx:29 のリード文は実際に「登録後、確認メールからメールアドレスを認証してください。」のままで、git履歴でも同ファイルの最終更新は bd316ab（2026-08-16, T-M8-111）＝コード方式へ移行した a970c7e（2026-08-18, T-M8-121）より前であり、移行時に文言が更新されていない。さらにこのリードは page.tsx のheader内（SignUpFormの外）にあり、signup-form.tsx が成功時に EmailCodeForm へ差し替えてもheaderは残るため、「確認コードを入力してください」「<email> に6桁の数字を送信しました」（email-code-form.tsx:38-43）の直上に旧方式の一文が並ぶ。src/ 内で利用者に見える文言のうち、メール側で認証を完了させるよう促しているのはこの1文だけで、他（auth.ts:38・email-code-form.tsx）はすべて6桁コードの語彙。docsは実装側と一致しており（06_screens_onboarding_posting.md:14,433 の「6桁コード入力」「メールのリンクを追わせない」、03_auth_billing_usage.md:25 の「確認メールは6桁コード方式」「登録画面から離れずに入力させる」）、旧リード文を指定している節も「未実装」注記も無いため、別節に正しい記述があるという反証も成り立たない。旧文言をassertしているテスト・E2Eも無い（rg "登録後" は page.tsx:29 と無関係な execution-prereqs.ts:215 のみ）ので、文言修正は外部の振る舞いを変えない。桁数を EMAIL_CODE_LENGTH（src/lib/auth/email-code.ts）から描く提案も、同モジュールがclient専用依存を持たないためserver componentから参照可能で実行可能。唯一の誇張は「リンク追跡を促している」という表現

### 58. [low] `reserveIfPremium` 用のJSDocが `settleIfPremium` の直前に取り残され、reserve側は無説明になっている

- 種別: リファクタ ／ 領域: 認証・課金・利用量
- 場所: `src/lib/usage/reserve-if-premium.ts:32`
- 根拠: reserve-if-premium.ts:31-40 の JSDoc は「premium のときだけクレジットを押さえる。BYOK（standard / md）は消費しないので何もしない」「**例外は握らない**。上限到達（`usage_limit_exceeded`）を失敗確定へ回すか、画像なしで確定するかは job ごとに違う」と reserve の契約を説明しているが、その直後（:41-44）に `settleIfPremium` 用の JSDoc が続き、実際に注釈が付くのは `settleIfPremium`（:45）。`reserveIfPremium`（:65）には注釈が無い。呼び出し側が最も知る必要のある「上限到達例外を握らない」という契約が、エディタのホバーでは精算関数の説明として出る。
- 直し方: reserve用のJSDocブロックを `reserveIfPremium` の直前へ移す。コメント移動のみで振る舞いは変わらない。
- 反証を試みた結果: 反証できず、指摘は実在する。src/lib/usage/reserve-if-premium.ts を読んで確認: :31-40 の JSDoc の直後に :41-44 の別 JSDoc が続き、注釈が付くのは :45 の settleIfPremium。reserveIfPremium（:65）には JSDoc が無い。:31-40 の内容が reserve のものであることは実装で裏付けられる——generation-reserve.ts:73 の通り usage_limit_exceeded を投げるのは reserveUsage の limit チェックのみで、settleUsage は自身の JSDoc（:101-104）で「上限チェックしない——既に発生した実費は拒否できない」と明記しているため、「例外は握らない。上限到達（usage_limit_exceeded）を…」は settleIfPremium の説明になり得ない。原因も特定できた: git show 6a8eafb（T-M8-109）の差分で、既存の reserve 用 JSDoc の `*/` 直後に settleIfPremium とその JSDoc が挿入され、元は reserveIfPremium に付いていた注釈が取り残された。以降このファイルへのコミットは無く（git log は 6a8eafb が最新）未対処。提案はコメントブロックの移動のみで、識別子・シグネチャ・export・実行経路のいずれも変えず、外部の振る舞い・契約への影響は無い。

### 59. [low] schema-doc-sync.db.test.ts が docs を cwd 相対で読み、§11 の「走査の基点は import.meta.url」に反する（サブディレクトリ実行で必ず落ちる）

- 種別: リファクタ ／ 領域: 空振り検査
- 場所: `src/lib/db/schema-doc-sync.db.test.ts:20`
- 根拠: 20行目が `const DOC = "docs/requirements/02_data_model.md";` で、24行目の `readFileSync(DOC, "utf8")` は `process.cwd()` 基準。docs/operations/development-and-testing.md §11「走査の基点は `fileURLToPath(new URL(..., import.meta.url))` にする。`process.cwd()` 基準だと実行ディレクトリで結果が変わる」（T-M8-51・R19）に反する。**実際に確認した**: `cd src && npx vitest run --root <repo> db/schema-doc-sync` → `ENOENT: no such file or directory, open 'docs/requirements/02_data_model.md'` で3件すべて失敗。同種の他の検査（`warning-docs-sync.test.ts:19-21`・`vercel-crons.test.ts:19-21`・`landing-page.test.ts:17-21`）はいずれも `import.meta.url` 基準に揃っており、この1本だけ取り残されている。落ち方は例外なので「静かな緑」ではないが、規約違反として揃えるべき。
- 直し方: `const DOC = fileURLToPath(new URL("../../../docs/requirements/02_data_model.md", import.meta.url));` に置き換える（`warning-docs-sync.test.ts` と同じ形）。挙動は同じで、実行ディレクトリ依存だけが消える。
- 反証を試みた結果: 反証できなかった。実在を確認。(1) src/lib/db/schema-doc-sync.db.test.ts:20 は `const DOC = "docs/requirements/02_data_model.md";` で 24行目の readFileSync は process.cwd() 基準（import は node:fs のみ、node:url 未使用）。(2) 規約は docs/operations/development-and-testing.md:412 に実在（§11「走査の基点は fileURLToPath(new URL(..., import.meta.url)) にする。process.cwd() 基準だと実行ディレクトリで結果が変わる」）。(3) 実測で再現: `cd src && REQUIRE_DB=1 npx vitest run --root <repo> db/schema-doc-sync` → ENOENT で 3 failed（リポジトリ直下からは 3 passed）。(4) 取り残しの裏付け: 本ファイルは 9013090（2026-08-03, T-M8-33）で追加され以後未変更。cwd依存を一斉是正した R19（cc38d86, 2026-08-11）が触ったのは tabs / env-secret-usage / outbound-channels / server-boundary / launchd / resolve-account の6本だけで、既存だった本ファイルが漏れている。docs を読む他の検査（warning-docs-sync.test.ts:19-21・vercel-crons.test.ts:18-21・landing-page.test.ts:17-20）は全て import.meta.url 基準で、cwd 基準はこの1本のみ。(5) 提案の解決先を node で実測 → /Users/kotamatsumoto/private-workspace/x-system/docs/r

### 60. [low] news_fetch 周辺のコメントが「6分野・毎時起動」のまま（実装は3分野・2時間おき）

- 種別: リファクタ ／ 領域: jobs・cron・スケジュール
- 場所: `src/lib/jobs/news-fetch.ts:9`
- 根拠: news-fetch.ts:9 の docblock『6分野を最大3並列でリサーチし』、src/app/api/cron/news-fetch/route.ts:9『毎時起動・6分野最大3並列・分野別commit』、同:41『6分野settle後』が残っている。実際は runNewsFetch(:165) が既定で NEWS_FETCH_CATEGORIES（ai・investment・sns の3分野。T-M7-55）を使い、vercel.json の schedule は `0 1-11/2 * * *`（JST 2時間おき）。要件04 §6 は3分野・2時間おきで正しいので、コードコメントだけが古い前提を語り、費用見積り（分野数×起動回数）を読み違える材料になっている。
- 直し方: 3ファイルのコメントを『定時取得は NEWS_FETCH_CATEGORIES（現在3分野）を最大3並列』『2時間おき起動（要件04 §6）』へ直し、分野数を数字で書かずコード定数を参照する形にする（次に分野を増減しても取り残されない）。コメントのみの修正。
- 反証を試みた結果: 反証できず、指摘は実在する。

実装の確認:
- src/lib/news.ts:20 `export const NEWS_FETCH_CATEGORIES = ["ai", "investment", "sns"]`（3分野）。src/lib/jobs/news-fetch.ts:165 `const categories = deps.categories ?? NEWS_FETCH_CATEGORIES` で既定が3分野。並列数は news-fetch.ts:65 `DEFAULT_CONCURRENCY = 3` なので「最大3並列」だけは正しい。
- vercel.json:4 `{ "path": "/api/cron/news-fetch", "schedule": "0 1-11/2 * * *" }`（JST 10/12/14/16/18/20時＝2時間おき・1日6回）。ops/launchd/com.spaceai.news-fetch.plist の StartCalendarInterval も 10/12/14/16/18/20時。

docs は正しい（＝doc_driftではなくコードコメントのみの古い前提）:
- docs/requirements/04_jobs_and_automation.md:103「`news_fetch` | **10:00〜20:00の2時間おき**（10/12/14/16/18/20時） | `0 1-11/2 * * *` | **3分野**（ai・investment・sns）」、同:116「取得対象の3分野（`NEWS_FETCH_CATEGORIES`＝ai・investment・sns）を最大3並列で実行」。

指摘の3箇所はいずれも実在を確認:
- src/lib/jobs/news-fetch.ts:9「6分野を最大3並列でリサーチし」
- src/app/api/cron/news-fetch/route.ts:9「毎時起動・6分野最大3並列・分野別commit」（毎時も6分野も誤り）
-

### 61. [low] judgePortal の nextAction 合成で、error のとき判定側の nextAction が必ず捨てられる

- 種別: リファクタ ／ 領域: 運営コマンド・診断・メール
- 場所: `src/lib/ops/portal-status.ts:64`
- 根拠: portal-status.ts:60-78 は `...(judged.nextAction ? { nextAction: judged.nextAction } : {})` の後に `...(judged.level === "error" ? { nextAction: "`npm run stripe:portal:setup -- --target <local|staging|production>`…" } : {})` を展開するため、error では後者が常に勝つ。64行のコメント「判定側が次の一手を持っていればそれを使う（T-M8-128）」は error では成立しない。現状 judgePortalFeatures が nextAction を返すのは warn 分岐だけなので実害は出ていないが、error 分岐（configurationNotFound・機能無効）に固有の次の一手を足した瞬間に黙って消える。
- 直し方: `nextAction: judged.nextAction ?? (judged.level === "error" ? 既定文言 : undefined)` の形に組み替える（現在の出力は完全に同一）。
- 反証を試みた結果: 反証できず、指摘は実在。src/lib/ops/portal-status.ts:60-74 は `...(judged.nextAction ? { nextAction: judged.nextAction } : {})` の後に `...(judged.level === "error" ? { nextAction: 既定文言 } : {})` を展開しており、spread の後勝ちで error 時は判定側の nextAction が必ず捨てられる（同一パターンを node で実行し error=DEFAULT / warn=JUDGED を実測確認）。src/lib/stripe/portal-features.ts の judgePortalFeatures が nextAction を返すのは `!snapshot.features` の warn 分岐のみで、error 分岐（configurationNotFound・機能無効の2本）は持たないため現状の出力は変わらず実害は未発生。ただし portal-status.ts:63 のコメント「判定側が次の一手を持っていればそれを使う（T-M8-128）」は error では成立せず、portal-features.ts の docコメント「判定ごとに書けるようにする」という T-M8-128（commit 010f125）の意図に対して error 経路だけ受け取り口が塞がっている。提案の安全性も確認: 呼び出しは diagnostics.ts:643 の1箇所のみ、judgePortal 専用テストは無く、Check.nextAction の消費側は scripts/doctor.mjs:266 の `if (c.nextAction)` という truthy 判定で `"nextAction" in`・Object.keys・toStrictEqual を使う箇所はリポジトリに存在しないため、`judged.nextAction ?? (error ? 既定文言 : undefined)` へ組み

### 62. [low] 存在しない環境変数を指すコメントが呼び出し側に残っている（R30の取りこぼし）

- 種別: リファクタ ／ 領域: 運営コマンド・診断・メール
- 場所: `src/lib/ops/diagnostics.ts:626`
- 根拠: diagnostics.ts:626 「// データベースの使用量（T-M7-43）。上限は無料プラン既定で、Proなら env で上書きする。」。同ファイル437行の FREE_DB_SIZE_LIMIT_BYTES の注記は「以前ここに『Proへ上げた場合は SUPABASE_DB_SIZE_LIMIT_MB で上書きする』と書いてあったが、**その環境変数は repo に存在しない**（R30）」と明記しており矛盾する。実際 SUPABASE_DB_SIZE_LIMIT_MB は tasks/REFACTOR_PLAN.md（R30の説明）以外に1件も無く、collectDiagnostics は limitBytes に FREE_DB_SIZE_LIMIT_BYTES を直接渡している。
- 直し方: 626行のコメントから「Proなら env で上書きする」を削り、「上限は無料プランの既定値（437行の定数）を使う。env での上書きは無い」へ直す。コードは変えない。
- 反証を試みた結果: 反証できなかった。指摘は実在する。(1) src/lib/ops/diagnostics.ts:626 に「上限は無料プラン既定で、Proなら env で上書きする。」が現存（実読確認）。(2) 同ファイル434-441行の FREE_DB_SIZE_LIMIT_BYTES の NOTE は「その環境変数は repo に存在しない（R30）…env で上書きできるようにするのは機能追加なので別タスクで扱う」と明記し、626行と正面から矛盾する。(3) rg（hidden含む・repo全体）で SUPABASE_DB_SIZE_LIMIT は diagnostics.ts:437 の NOTE 自体と tasks/REFACTOR_PLAN.md（R30説明:60/215/216）のみ。.env.example・src には1件も無い。(4) 上書き経路も存在しない: DiagnosticsOptions（485-492行）は schedulerExpected/captcha/portal のみで dbSizeLimitBytes は削除済み、diagnostics.ts に process.env は0件、633行は FREE_DB_SIZE_LIMIT_BYTES を直渡し、もう一方の呼び出し元 daily-summary.ts:294 も同じ定数を直渡し。(5) git でR30の取りこぼしと確定: 620-636行を最後に触ったのは commit 22aefbd「refactor(R30,R31)」で、その diff は定数のdocblock（- * Proへ上げた場合は SUPABASE_DB_SIZE_LIMIT_MB で上書きする。→ NOTE）と - dbSizeLimitBytes?: number / - limitBytes: options.dbSizeLimitBytes ?? … → + limitBytes: FREE_DB_SIZE_LIMIT_BYTES を直しているが、626行のコメントは無変更。docsの抽象表現ではなく同一ファイル

### 63. [low] check.ts を正本にしたのに diagnostics.ts 経由の再exportが残り、import 経路が2つある

- 種別: リファクタ ／ 領域: 運営コマンド・診断・メール
- 場所: `src/lib/ops/diagnostics.ts:19`
- 根拠: R31 で型と共通文言を src/lib/ops/check.ts へ切り出したが、diagnostics.ts:19 が `export { approxYen, FAILED_EMAIL_WINDOW_DAYS, summarize, worstLevel }` と Check/Level 型を再exportしている。この再exportを使うのは diagnostics.test.ts のみで、daily-summary.ts:7 は `from "./check"` を直接使う。一方 portal-status.ts:5 は `import type { Check } from "./diagnostics"` と再export経由で型を取る（server-only を持つ重いモジュール経由）。同じ記号に2つの入口がある状態で、check.ts が正本という意図が読み取りづらい。
- 直し方: portal-status.ts の型importを `from "./check"` へ変更し、diagnostics.ts の再exportは diagnostics.test.ts の import を check.ts へ向けたうえで削除する（型のみ・値の実体は変わらないので挙動不変）。
- 反証を試みた結果: 反証できなかった。指摘は実在する（かつ指摘より強い根拠がある）。

【実ファイルで確認した事実】
1. src/lib/ops/diagnostics.ts:18-20 は「型と全体まとめは check.ts が正本」というコメントの直後で `export { approxYen, FAILED_EMAIL_WINDOW_DAYS, summarize, worstLevel }` と `export type { Check, Level }` を再exportしている。

2. 再export6記号のうち3つは消費者ゼロ＝完全なデッドコード。
 - approxYen 経由: 0件（daily-summary.ts:7 が `from "./check"` を直接使う）
 - FAILED_EMAIL_WINDOW_DAYS 経由: 0件（同上）
 - Level 経由: 0件
 - summarize / worstLevel 経由: diagnostics.test.ts:14,16 のみ
 - Check 経由: portal-status.ts:5 のみ

3. 同一ディレクトリ内で入口が2つある不整合は実在。auth-url-status.ts:21 は `import type { Check } from "./check"`、portal-status.ts:5 は `from "./diagnostics"`。scripts/doctor.mjs:270 も check.ts を直接読む。

4. 指摘より強い根拠: この再exportは循環参照を作っている。diagnostics.ts:16 が portal-status から**値**（judgePortal, probePortalFeatures）をimportし、portal-status.ts:5 が diagnostics へ戻る。戻り側が type-only なので実行時は安全だが、ソースグラフ上は実際の循環で、提案はこれを解消する。

【検討した反証と却下理由】
tasks/RE

### 64. [low] 説明の120字上限はDBのCHECKに存在するのに、コードのコメントは「DBに CHECK は無い」と書き、docs の Constraints にも載っていない

- 種別: リファクタ ／ 領域: 投稿パターン
- 場所: `src/lib/post/post-patterns-store.ts:278`
- 根拠: post-patterns-store.ts:278 は `/** 説明の上限（DBに CHECK は無いが、画面が破綻しない長さで止める）。 */`。実際には migration 20260818000001:44 に `constraint post_patterns_desc_len check (description is null or char_length(description) <= 120)` がある。要件02 §3.21 の Constraints 段落（598行）も desc_len を挙げていない（description 行にも 120 字の記載が無い）。
- 直し方: コメントを「DBの post_patterns_desc_len と同値」へ直し、要件02 §3.21 の description 行と Constraints 段落に 120 字上限を追記する（3か所で同じ値を言うので、どれが正か迷わない書き方にする）。
- 反証を試みた結果: 反証できなかった。指摘の4点すべてが実ファイルで確認できた。(1) post-patterns-store.ts:278 は実際に `/** 説明の上限（DBに CHECK は無いが、画面が破綻しない長さで止める）。 */`。(2) migration 20260818000001_post_patterns.sql:44 に `constraint post_patterns_desc_len check (description is null or char_length(description) <= 120)` が存在し、migrations 全体の `drop constraint` を検査したが post_patterns_desc_len は一度も削除・再定義されていない（削除されているのは post_patterns_max_posts_range と post_patterns_edit_limit_range のみ）。制約は生きており、値120は PATTERN_DESCRIPTION_MAX_CHARS = 120 と完全一致。(3) 要件02 §3.21 の description 行（02_data_model.md:582）は制約欄が `nullable` のみで120字の記載が無い。(4) Constraints 段落（同598行）は name・prompt・max_posts・max_posts_edit・web_search_max_uses・never/0の対応・requires_quote_url×include_news_digest と当該テーブルのCHECKをほぼ網羅列挙しており、desc_len だけが欠けている。つまり「docsが意図的に仕様レベルの抽象で書いている」という反証は成立せず、網羅を意図した列挙の抜けである。さらに直前の276行は `/** 名前の上限（要件02 §3.21 の CHECK と一致）。 */` とDB CHECKを参照する書き方をしており、278行はそのファイル自身の慣習を破って逆の（虚偽の）

### 65. [low] 移行完了後に意味が反転した／古くなったコメントが投稿パターン周辺に複数残っている

- 種別: リファクタ ／ 領域: 投稿パターン
- 場所: `src/lib/post/post-patterns-store.ts:135`
- 根拠: (1) post-patterns-store.ts:135「旧 enum（p1〜p6）から現在の行を引く。**移行の間だけ使う**（U5 で撤去）」— 実際には generation-jobs.ts:232 の createDraftFromNews が恒久的に使い、ADR-0008 も「ニュースから作るときは既定の『ニュース解説』を使う」と恒久仕様として書いている。(2) gen-prompts.ts:4-5「PT-P1〜P6・PT-IMGは system default として prompt_templates へseedし…編集UIは別マイルストーン」— p1〜p6 の行は migration 20260818000002 で削除され、編集UIも実装済み。(3) pattern-fields.tsx:28-30「分量は**スレッド数**で聞く…DBは総ポスト数で持ち、ここで変換する」— スレッド数の入力欄は無く、この部品は変換を一切していない（プロンプトから読む）。(4) create-post-form.tsx:216「選択中のパターン。入力欄の出し分け（意見の入力）と表示名に使う」— 意見の固定欄は廃止済み。(5) generation-validation.ts:72 の `/** 出典必須パターン: P-1/P-4/P-6は常に… */` は直後の INSTRUCTION_MARKERS と無関係で、関数が sourceRequiredForSpec へ移ったあとの孤児。
- 直し方: 5か所を現状に合わせて書き換える。(1) は「ニュース起点生成が既定パターンを引くための関数」へ、(3) は「分量はプロンプトから読み、この部品は読み取り結果を表示するだけ」へ、(5) は削除（判定の説明は pattern-spec.ts の sourceRequiredForSpec 側にある）。
- 反証を試みた結果: 反証できなかった。5か所すべて HEAD の実ファイルで陳腐化を確認した。(1) post-patterns-store.ts:134-137 の「移行の間だけ使う（U5 で撤去）」は、U5 完了後（migration 20260818000005 が存在・ADR-0008 は「U5 で撤去した」と過去形・T-M8-129 は done）も関数が残り、generation-jobs.ts:232 の createDraftFromNews が恒久的に使っている（直上のコメント自身が恒久仕様として書く）。実装は enum ではなく恒久列 seed_key を引くだけ。(2) gen-prompts.ts:3-5 の「PT-P1〜P6 は prompt_templates へseed／編集UIは別マイルストーン」は、migration 20260818000002:41,46 が kind <> 'image' の行を削除しており、現在は SYSTEM_DEFAULT_TEMPLATES をコード定数として post_patterns.prompt is null の既定に使う（prompt-templates.ts:155-181・pattern-spec.ts:127）。編集UIも settings/page.tsx:240 のパターン管理として実装済み（prompt-templates-editor.tsx:81 が「投稿の型はパターン管理へ移った」と明記）。(3) pattern-fields.tsx:28-31 の「分量はスレッド数で聞く／ここで変換する」は、同部品に分量の入力欄が無く PatternDraft に maxPosts も無い。同ファイル236-241行のコメントが逆に「分量はプロンプトから読む（T-M8-132）」と書き threadCountFromPromptLabel(draft.prompt) を表示するだけで、ファイル内で矛盾している（36行の maxPosts の説明も存在しないフィールド）。(4) create-post-form.t

### 66. [low] smoke:live のシナリオが利用者が変更できる表示名でパターンを引いている

- 種別: リファクタ ／ 領域: 投稿パターン
- 場所: `src/lib/smoke/scenarios.ts:199`
- 根拠: createJob（scenarios.ts:164-176）は `from post_patterns p where p.x_account_id = $1 and p.name = $2` でパターンを引き、呼び出しは 199行「週次まとめ」と 255行「自分の考え・意見」のハードコードされた既定名。名前は利用者が編集できる（applyUpdatePattern の name 更新・要件02 §3.21 は name を「画面に出る唯一の名前」として1〜30字で自由に変更可とする）。CLAUDE.md の「変更影響 → 必須の検証」表は AI provider・プロンプト・出力schema を触ったとき smoke:live を必須にしているので、名前を1つ変えるとこの必須検証が題材を見つけられず止まる（黙って別パターンにはならず例外で落ちるので原則1は満たすが、必須検証が実行できなくなる）。
- 直し方: createJob を seed_key（'p6' / 'p2'）で引く形に変え、見つからないときのメッセージだけ既定パターン名を添える。seed_key は利用者が変更できないので、名前の変更で必須検証が止まらない。
- 反証を試みた結果: 反証を試みたが成立せず、指摘は実在すると確認した。

確認したこと（すべて実ファイル）:
1. `/Users/kotamatsumoto/private-workspace/x-system/src/lib/smoke/scenarios.ts:164-176` の `createJob` は `from post_patterns p where p.x_account_id = $1 and p.name = $2` で引き、見つからなければ `throw new Error("パターン「…」がこのアカウントにありません（削除された可能性）")`。呼び出しは 199行 `"週次まとめ"`、255行 `"自分の考え・意見"` のハードコード既定名。`git show 9e2cab8`（T-M8-129 U5）で旧enum `'p6'/'p2'` から名前引きへ意図的に変えたことも確認（コミットメッセージ「`smoke:live` はパターンを名前で指し…」）。

2. `name` は既定パターンでも利用者が変更できる。`src/lib/post/post-patterns-store.ts:449-487` の `applyUpdatePattern` は `set name = $3` を `seed_key` の有無で分けず、docコメントにも「既定パターン（`seed_key` あり）も編集できる」と明記。`validatePatternInput`（同314-346行）にも既定名の変更を禁じる分岐は無い（長さ1〜30字と `\n<>` 禁止のみ）。`docs/requirements/02_data_model.md` §3.21 も `name` を「画面に出る唯一の名前」1〜30字とする。

3. `seed_key` はアプリコードから一切更新されない（`grep` で `post_patterns` の `seed_key` を書く箇所は migration の seed 関数だけ。`applyCreatePattern` は insert 列に含

### 67. [low] 認証3画面の外枠（ページ背景・カード・ロゴ＋アプリ名ヘッダ・h1・LegalFooter）が3ファイルへ逐語重複している

- 種別: リファクタ ／ 領域: 画面・UI共通部品
- 場所: `src/app/login/page.tsx:37`
- 根拠: src/app/login/page.tsx:35-52、src/app/signup/page.tsx:17-31、src/app/reset-password/page.tsx:19-30 が同じ構造を逐語で持つ: `bg-page flex min-h-screen flex-col` → `flex flex-1 items-center justify-center px-4 py-12` の main → `w-full max-w-[400px] space-y-6 rounded-card border border-hairline bg-surface p-6 shadow-[var(--shadow-pop)] sm:p-7` のカード → `inline-flex items-center justify-center gap-2` の中に LogoTile size={28} と `text-[16px] font-bold tracking-tight text-ink` のアプリ名リンク → `text-[22px] font-bold tracking-tight text-ink` の h1 → `text-body leading-5 text-ink-2` のリード → 末尾の LegalFooter。signup/page.tsx:20 のコメント自身が「ヘッダはlogin/page.tsxと同じ構成（ロゴマーク＋アプリ名・T-M8-60）」と重複を自認している。card-surface.test.ts は面＋`shadow-[var(--shadow-card)]` の並びだけを禁止しており、ここは shadow-pop なので検査の対象外。要件06 §2.2（06_screens_onboarding_posting.md:139）はロゴの表示点をログイン・新規登録・パスワード再設定の3画面と数えており、器の見た目が3箇所に散っているとロゴ寸法だけ共通・器は手書きという不揃いが残る。
- 直し方: src/components/auth/ に `AuthPageShell({ title, lead, children })` を置き、3画面から呼ぶ（LegalFooter とロゴ＋アプリ名ヘッダも中に入れる）。現行3ファイルの class 文字列は完全一致なので描画結果は変わらない。h1 の `text-[22px] font-bold tracking-tight text-ink` は同部品が唯一の持ち主になる。
- 反証を試みた結果: 反証できず、指摘は実在。(1) 3ファイル（src/app/login/page.tsx:35-69、src/app/signup/page.tsx:16-35、src/app/reset-password/page.tsx:18-38）の className を全抽出して比較したところ、bg-page flex min-h-screen flex-col → main の flex flex-1 items-center justify-center px-4 py-12 → カード w-full max-w-[400px] space-y-6 rounded-card border border-hairline bg-surface p-6 shadow-[var(--shadow-pop)] sm:p-7 → header space-y-2 text-center（span inline-flex items-center justify-center gap-2 + LogoTile size={28} + Link text-[16px] font-bold tracking-tight text-ink）→ h1 text-[22px] font-bold tracking-tight text-ink → p text-body leading-5 text-ink-2 → 兄弟の LegalFooter が、文字列も入れ子順も完全一致。(2) max-w-[400px]・shadow-pop のカード並び・text-[22px] font-bold tracking-tight text-ink・inline-flex items-center justify-center gap-2" はこの3ファイルのみに出現（plan-comparison-table.tsx:62 は font-extrabold で別物）。(3) 共有部品は未存在（src/components/auth/ は email-code-form / field-error / 

### 68. [low] 画面h1のclass文字列が6ファイル7箇所へ直書きされ、cardTitleClassName と同型の集約が h1 だけ抜けている

- 種別: リファクタ ／ 領域: 画面・UI共通部品
- 場所: `src/app/app/page.tsx:151`
- 根拠: `text-[20px] font-bold tracking-tight text-ink` が src/app/app/page.tsx:151、src/app/app/settings/page.tsx:278、src/app/app/schedule/page.tsx:112、src/app/app/posts/page.tsx:253、src/app/app/news/page.tsx:72、src/app/app/analytics/page.tsx:33 と :73 の7箇所に直書きされている。カード見出しは src/components/ui/card.tsx:63 の `cardTitleClassName` に集約され card-title.test.ts:36 がリテラル直書きを禁止しているのに、その1段上の画面h1（20px）には正本が無い。type-scale.test.ts は15px未満の任意値だけを見るため（同 :62 `if (px >= 15) continue;`）20px は検出対象外で、globals.css:91 のコメントも「見出し系は 15px（カードタイトル）/ 20px（h1）」と2段あることを認めている。同様に `text-[26px] font-bold tracking-tight text-ink` が src/components/legal-document.tsx:36 と src/app/plans/page.tsx:77 の2箇所にある。
- 直し方: card.tsx（または新規 components/ui/page-title.ts）に `pageTitleClassName = "text-[20px] font-bold tracking-tight text-ink"` を置き、7箇所から参照する。card-title.test.ts と同型の「リテラル直書き禁止＋定義元に存在すること」検査を1本追加する。文字列は同一なので描画は変わらない。
- 反証を試みた結果: 反証できなかった。実ファイルで確認: (1) `text-[20px] font-bold tracking-tight text-ink` はバイト一致で7箇所（app/page.tsx:151, settings/page.tsx:278, schedule/page.tsx:112, posts/page.tsx:253, news/page.tsx:72, analytics/page.tsx:33と:73）に直書きされ、いずれも「アプリ画面のページ見出し h1」という同一の役割・同一の判断（card.tsx が統合を戒める「偶然共有している汎用の組み合わせ」ではない）。(2) 集約先は本当に無い: `rg 'pageTitle|page-title|PageTitle|PageHeader|page-header'` はリポジトリ全体で0件、src/components/ui/ にも該当モジュールなし。(3) 同型の正本は実在し機能している: card.tsx:62（指摘の:63は1行ずれ）の `cardTitleClassName` は card.tsx 外の4ファイルから参照され、card-title.test.ts がリテラル直書き禁止＋定義元にリテラルが在ることの空振り検査まで持つ。(4) 20px は本当にどの検査にも掛かっていない: type-scale.test.ts の `if (px >= 15) continue;` は52行目（指摘の:62はファイル件数ガード）で、両テストを実行したところ7箇所が現状のまま5件緑。(5) docs は禁じていない: ADR-0006 §5 が「同じものを2か所で描かない」の正本一覧（Card/CardTitle/cardTitleClassName/Badge/notice/link-button 等）を挙げるがページh1は不在。ADR-0007 §1 の「見出し系は…任意値で許す」はサイズ段の話で文字列の重複とは別軸（15pxのカードタイトルも同じく「任意値許可」でありながら集約済みで、両者が直交するこ

### 69. [low] 参照0件の共通部品と、削除済み画面（ai-settings のタブナビ）向けに残された TabNav の第3引数・テストケース

- 種別: リファクタ ／ 領域: 画面・UI共通部品
- 場所: `src/components/ui/link-button.ts:16`
- 根拠: リポジトリ全体（src / e2e / scripts / docs）で参照が0件のexport: src/components/ui/link-button.ts:16 `secondaryLinkClassName`（「副操作（枠線のみ）」と説明があるが呼び出し無し。一方 src/components/app-shell/page-state.tsx:145 の ErrorState の再試行ボタンは `min-h-9 rounded-card border border-hairline px-4 text-body font-medium text-ink hover:bg-black/[0.03] focus-visible:…` を手書きしており、これは secondaryLinkClassName とほぼ同一の見た目）、src/components/ui/card.tsx:78 `CardDescription`、src/components/app-shell/navigation-items.ts:23 `AppNavigationIcon`。加えて src/components/app-shell/tab-nav.tsx:28 の `inactiveExtra` / :57 の `inactiveLinkClassName` prop は呼び出し側3箇所（settings/page.tsx:281・:455、posts/page.tsx:256）のいずれも渡しておらず未使用。src/components/app-shell/tab-nav.test.ts:12-14 は `AI_LINK_EXTRA`（「ai-settings のリンク固有クラス（現行 page.tsx の値）」）と `tabNavClassName("mt-7 gap-1 overflow-x-auto")`・`tabLinkClassName(false, AI_LINK_EXTRA, "hover:text-foreground")` を固定し続けているが、src/app/app/ai-settings/page.tsx は T-M8-104 以降 redirect のみでタブナビを持たず、`mt-7` も `hover:text-foreground` もどの呼び出し元にも存在しない（settings/page.tsx:280 は `gap-1 overflow-x-auto` のみ）。同ファイル :17 と :44 の関数コメントも「settings/posts/ai-settings 共通」と実在しない画面を挙げている。
- 直し方: secondaryLinkClassName・CardDescription・AppNavigationIcon を削除する（参照0件）。ErrorState の再試行ボタンは削除の代わりに secondaryLinkClassName を使う形へ寄せてもよいが、その場合は寸法差（min-h-9 と h-9）を確認したうえで1系統にする。TabNav の `inactiveLinkClassName` / `tabLinkClassName` の第3引数を削り、tab-nav.test.ts から ai-settings ケース2件を落として現存する呼び出し（settings の linkClassName 付き・posts の素）だけを固定する。tab-nav.tsx:17,44 のコメントから ai-settings を外す。呼び出し元が誰も使っていない引数なので描画は変わらない。
- 反証を試みた結果: 反証できなかった。全ての下位主張を実ファイルで確認し、すべて実在した。

【参照0件の3つ（rg でリポジトリ全体を検索、定義行1件のみヒット）】
- src/components/ui/link-button.ts:16 secondaryLinkClassName — 同ファイルの primaryLinkClassName は14ファイルから import されているのに対し、secondaryLinkClassName は定義行以外0件（src/e2e/scripts/docs/tasks すべて）。src/components/ui/ にバレルindexは存在しないので再エクスポート経由の利用もない。
- src/components/ui/card.tsx:78 CardDescription — 0件。git log -S "CardDescription" -- src は追加コミット eecf00f（T-M8-04）だけを返し、導入以来一度も使われていない。同ディレクトリの card-title.test.ts / card-surface.test.ts も参照しない。
- src/components/app-shell/navigation-items.ts:23 AppNavigationIcon — 0件（APP_NAVIGATION_ITEMS 本体は app-navigation.tsx と navigation-items.test.ts が使うが、この型エイリアスは誰も使わない）。

【TabNav の第3引数】
src/components/app-shell/tab-nav.tsx:28 inactiveExtra / :53,:61,:74 inactiveLinkClassName は、呼び出し3箇所すべてを読んで未使用を確認した。settings/page.tsx:281（className="gap-1 overflow-x-auto" ＋ linkClassName="shrink-0 focus-visible:outl
