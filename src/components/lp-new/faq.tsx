import { PLANS } from "@/lib/plans";
import { cn } from "@/lib/utils";

import { BODY } from "./tokens";

/**
 * /new 専用の FAQ 8問（T-M8-419）。既存の `FaqList`（12問）は再利用しない——
 * 「今後YouTubeやTikTokも」など docs に根拠のない将来約束を含むため。
 * 折りたたまない（開閉式にしない。LPで最も読まれるべき内容を隠さない）。
 * 開示語「下書きまで」「同意」「はじめて（初回）」「カード登録」を必ず残す。「退会」とは書かない。
 * 分析は「分析を開始」を押したときだけ（1日1回・表示専用）——「自動で分析」と書かない（PRD K-2）。
 * 画像生成の提供元はアプリ画面と同じ表示名「OpenAI／Gemini」（「ChatGPT」と書かない）。
 * 回答は 2026-09-05 に簡潔化（運営者の依頼「説明しすぎ・冗長」）: 答えを先頭に、1文は短く、
 * 仕様書の語（表示専用・従量課金）は使わない。開示語と「有料期間中なら」（トライアル解約は即時終了・T-M8-278）は残す。
 * 同日のレビューで Q1「最初は」→「ふだんは」（時間が経つと勝手に自動投稿へ変わるように読めた）、Q6 は
 * `TRIAL_NOTE` とほぼ同文だったので答えだけに短縮（カード登録の条件は上下の TRIAL_NOTE が3回言う。
 * 「はじめてのお申し込みに限り」は landing-page.test.ts が固定する初回限定の開示なので残す）。
 */
const FAQ_ITEMS: [question: string, answer: string][] = [
  [
    "本当に全部自動ですか？",
    // ボタン名「分析を開始」はここでは出さない（文節折りは「」の中も切るので 390px で「分析を／開始」と割れた。
    // 名前は画面ツアー停止04と「使うほど」が言う。「ボタン」は記法チップ「ボタン1つ」と同じ語）。
    "集める・作る・投稿・記録は自動。分析はボタンを押したときだけ。ふだんは「下書きまで」で、投稿はあなたが押します。自動投稿は、あなたが同意してオンにした後だけ動き、いつでも止められます。",
  ],
  [
    "始めるのに何が必要ですか？",
    // 共通で要る3つを先頭に列挙し、手軽なプランを先に置く。
    // 括弧は文末に置く（「（…Gemini）／のAPIキー」と閉じ括弧の直後の助詞が行頭に来た・390px）。
    `Xアカウント・メールアドレス・カード登録。${PLANS.premium.displayName}・${PLANS.expert.displayName}はそれだけで始められます。${PLANS.standard.displayName}はさらに、X Developer Appと生成AIのAPIキーが要ります（Claude／OpenAI／Gemini）。`,
  ],
  [
    "1件どのくらいで作れますか？",
    "リサーチ込みで60〜90秒。画面を離れても生成は続きます。",
  ],
  [
    "画像は作れますか？",
    `各ポストに1枚ずつ（OpenAI／Gemini）。自分の画像への差し替えもできます。${PLANS.standard.displayName}はキー登録後に使えます。`,
  ],
  [
    "スマホや、X以外のSNSでも使えますか？",
    "X（旧Twitter）のみ。PC・スマホのブラウザからそのまま使えます。",
  ],
  [
    "無料期間はありますか？",
    "はじめてのお申し込みに限り、7日間無料。期間中に解約すれば0円、続けるなら選んだプランで自動課金。",
  ],
  [
    "解約はどうすればいいですか？",
    // 「有料期間中なら」は落とさない（トライアル中の解約はその場で終了・T-M8-278。Q6 と並べて読んで矛盾しないため）。
    "設定のお支払い管理から、いつでも。有料期間中なら期間末まで使えます（日割り返金なし）。",
  ],
  // 旧「安心して任せるために」3カードの要点をここへ集約（3周目・個人開発のサービスとして冗長だったため）。
  [
    "APIキーやXの連携情報は安全ですか？",
    // 「Xへの書き込みは」を残す（「Xで行うのは投稿だけ」だと記録＝読み取りと矛盾して読める）。
    "どちらも暗号化して保存。Xへの書き込みは投稿だけで、自動いいね・フォロー・リプライはしません（凍結を避けるため）。",
  ],
  [
    `${PLANS.standard.displayName}の実費はどのくらいですか？`,
    `月額とは別に、X APIと生成AIの利用料がご自身の契約で発生します。目安は1投稿あたり数円〜数十円。${PLANS.premium.displayName}・${PLANS.expert.displayName}は追加費用なし。`,
  ],
];

export function NewFaqList() {
  return (
    <dl>
      {FAQ_ITEMS.map(([question, answer]) => (
        <div className="border-t border-hairline py-6" key={question}>
          <dt className="text-[length:clamp(17px,calc(13px_+_0.7vw),21px)] font-medium tracking-[-0.01em] [font-feature-settings:'palt'] [word-break:auto-phrase]">
            {question}
          </dt>
          {/* 回答も文節で折る（dt だけ auto-phrase で、回答は「ス／タンダード」「使え／ます」と語の途中で折れていた・レビュー 2026-09-05）。 */}
          <dd className={cn(BODY, "mt-2 text-ink-2")}>{answer}</dd>
        </div>
      ))}
    </dl>
  );
}
