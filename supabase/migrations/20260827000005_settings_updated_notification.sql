-- 学習がアカウント設定を書き換えたことを知らせる通知種別（T-M8-341・運営者の指示 2026-08-27）。
--
-- 参考ソースの学習がアカウント設定（フォームの各項目）を上書きするようになったため、
-- **利用者が何もしていないのに設定が変わる**。黙って変えると
-- 「設定した覚えのない値になっている」という、画面から説明できない状態になる（原則1）。
--
-- 通知の既定は**ON**にする（`notification_config` に行が無い場合も出す）。
-- 自分のデータが自動で書き換わった事実は、購読設定に関係なく伝わるべき。

alter type notification_type add value if not exists 'settings_updated';
