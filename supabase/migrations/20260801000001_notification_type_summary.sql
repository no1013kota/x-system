-- 要件04 §14 / CLAUDE.md 原則1（黙って壊れない）/ T-M7-29:
-- 日次サマリ用の通知種別を追加する。
--
-- 「成功として記録される失敗」は運営者が気付けない。2026-07-28、web3のニュース取得は
-- `ok:true fetched:0` を返し続け、誰も気付かないまま分野が空だった。ダッシュボードは1人運用では
-- 見なくなるため、1日1通のまとめを push（通知＋メール）で届ける。
--
-- 既存の `error` を流用しない。毎日届くものが「エラー」だと、本物のエラーが埋もれる。
alter type notification_type add value if not exists 'summary';
