# x-system 開発ガイド

Claude Code 中心で開発するリポジトリ。プロダクトの要件・仕様は `docs/` 配下のドキュメント群で定義する（正本）。

> **現在の状態（2026-07-19）**: 旧ドキュメント一式（旧PRD「Space AI」・各仕様書・ADR・バックログ）は `old/` に退避済み。`docs/` と `tasks/` は新しいドキュメントの格納待ちで空。**新ドキュメントを格納したら、下記「新ドキュメント格納後のセットアップ」を実施すること。** それまでは `/dev-loop`・`/doc-sync` は正常に機能しない（参照先の `docs/README.md`・`tasks/BACKLOG.md` が未作成のため）。

## 最重要ルール：ドキュメントとコードの同期

`docs/` 配下は常に実装の現状と一致していなければならない（Single Source of Truth）。

1. 仕様に影響する変更（機能追加・挙動変更・データモデル変更・API変更・画面変更）を実装したら、**同じ作業単位の中で**該当ドキュメントを更新する。ドキュメント更新までがタスクの完了条件。
2. どのドキュメントを更新すべきかは `docs/README.md` のドキュメントマップに従う（新ドキュメント格納後に作成する）。
3. 要件そのものが変わる場合は要件ドキュメントを更新し、変更履歴に追記する。実装詳細は要件ドキュメントではなく設計ドキュメント（アーキテクチャ・データモデル等）に書く。
4. コミット前に `/doc-sync` を実行して同期漏れを検出する。
5. ドキュメントに書かれていない仕様判断を実装したら、必ずドキュメント側に書き戻す。チャット上だけの決定を残さない。重要な技術判断は `docs/decisions/` にADRとして記録する。

## ディレクトリ構成

| パス | 内容 |
|---|---|
| `docs/` | 仕様ドキュメント一式（正本）。**新ドキュメント格納待ち**。構成は格納後に `docs/README.md` で定義する |
| `docs/decisions/` | ADR（技術・設計判断の記録）。1判断 = 1ファイル |
| `tasks/BACKLOG.md` | 開発バックログ（エージェントループの作業キュー）。**新ドキュメントから作成予定** |
| `.claude/skills/` | 開発用スキル（doc-sync / dev-loop） |
| `old/` | 旧ドキュメント・バックログのアーカイブ（参照用。ここは同期対象外・編集しない） |
| `src/` ほかアプリ本体 | 技術スタック確定後にスカフォールドする（現時点では未作成） |

## 新ドキュメント格納後のセットアップ

`docs/` に新しいドキュメントが入ったら、以下を実施して開発を開始できる状態にする。

1. 新ドキュメント群を読み、`docs/README.md`（ドキュメントマップ：どの変更をどのファイルに反映するか）を作成する。
2. このファイル（CLAUDE.md）と `README.md` の記述を新しい構成・プロダクト内容に合わせて更新する（本セクションの暫定注記も解消する）。
3. 技術スタックが未確定なら `docs/decisions/` にADRを起こして提案・合意する。
4. `tasks/BACKLOG.md` に新ドキュメントからマイルストーン・タスクを起こす（書式は同ファイル参照）。

## 開発の進め方（エージェントループ）

- タスクは `tasks/BACKLOG.md` で管理する。ステータス運用ルールは同ファイル冒頭に記載。
- `/dev-loop` で「次のタスク選択 → 実装 → 検証 → ドキュメント同期 → コミット」を1サイクル実行する。
- 連続で自動開発する場合は `/loop /dev-loop`。
- 1タスク = 1コミット。コミットメッセージにタスクIDを含める（例: `feat(T-M1-01): メール認証を実装`）。
- ユーザー判断が必要な事項は勝手に決めず、`tasks/BACKLOG.md` の「要決定」セクションに追記して先へ進む（可能なら暫定案を添える）。

## Definition of Done（全タスク共通）

- [ ] 実装が完了し、テストが通る
- [ ] `/doc-sync` 実行済み（該当ドキュメントを更新した、または影響なしを理由付きで確認した）
- [ ] `tasks/BACKLOG.md` のステータスと実装メモを更新した
- [ ] タスクIDを含むメッセージでコミットした

## 技術スタック

未確定。決定・変更は `docs/decisions/` にADRとして記録する（旧提案 ADR-0001 は `old/document/decisions/` に退避済み。流用する場合はコピーして起こし直す）。**技術スタックが確定するまでアプリ本体のスカフォールドを開始しない。**

## 規約

- ドキュメント・コミットメッセージ・ユーザーへの応答は日本語。コードの識別子・コード内コメントは英語。
- 秘密情報（APIキー・トークン等）は `.env`（gitignore済み）のみに置く。コード・ドキュメント・コミットに実キーを書かない。
- 外部API（X API・各生成AI・Stripe等）の仕様は変更が頻繁なため、実装時は公式ドキュメントで最新仕様を確認する。
- `old/` は参照専用のアーカイブ。同期対象に含めず、編集しない。
